# Decisions

Append-only. One dated line per decision a future session would otherwise have to re-derive.
Never edit or delete an entry: if a decision is reversed, add a new line saying so.

- 2026-08-29 Port target is mrab-regex at `1760a20647f1c2ddcc025128407fe6f7edb905a1` (release 2026.8.12), pinned as the `upstream/` submodule.
- 2026-08-29 SDK pinned to 10.0.400 with `rollForward: latestPatch` in `global.json`; `test.runner` set to Microsoft.Testing.Platform, which TUnit requires on the .NET 10 SDK.
- 2026-08-29 Analyzer set copied from an earlier internal monorepo (Meziantou, SonarAnalyzer, SharpSource, ErrorProne.NET) with warnings as errors, but **not** its `CommunityToolkit.Diagnostics` reference: the shipped package must carry zero runtime dependencies.
- 2026-08-29 Analyzer rules that a faithful port must violate (method length, complexity, nesting, magic numbers, commented-out upstream code, `NotImplementedException`) are relaxed in `.editorconfig` for `src/FuzzyRegex/Parsing` and `Engine` **only**. Correctness and security rules stay as errors everywhere. Rationale: upstream's structure must be preserved for syncs to stay mechanical, and autonomous slice sessions should spend allowance porting, not restructuring to satisfy a style rule.
- 2026-08-29 Test identity for the parity ratchet is `className + "." + testName` from the TRX report; the baseline stores the sorted **set** of passing ids rather than a count, so a slice's diff shows exactly which tests it enabled and a swap cannot hide behind an unchanged total.
- 2026-08-29 Feature area for `docs/STATUS.md` is derived from the test namespace (`Fuzzy.Text.RegularExpressions.Tests.Ported.<Area>`), and the capability a skipped test waits on is parsed from its `[Skip("needs:...")]` reason. Both conventions are enforced by a test (`PortedTestConventionsTests`), because a silent mis-parse would report fake progress.
- 2026-08-29 Budget gate: validated that **no local source reports live plan utilisation**. `~/.claude/stats-cache.json` lags by weeks and has no plan denominator; a `rateLimit` record appears only after a request has already been rejected (429); the claude.ai usage page needs an interactive browser, which an unattended `claude -p` driver has not got. The gate is therefore the deterministic slice caps (spec section 9's sanctioned fallback), plus rolling 24h/7d token windows computed from `~/.claude/projects/**/*.jsonl`, plus a back-off while a recorded rate-limit reset is still in the future.
- 2026-08-29 Budget token metric is the plain sum of all four token types, cache reads included, counted **globally** across every Claude Code session on the machine. Measured that day: about 2.9e9 tokens/day and 1.98e10/week.
- 2026-08-29 Token caps are a **circuit breaker, not a rationing mechanism**; the slice caps do the rationing. Setting the token caps at half of typical usage was tried and rejected: a dry run of the driver refused the very first slice, because an ordinary working day passes that mark by the afternoon. They now sit at roughly 2x the measured daily figure so they only trip on a runaway.
- 2026-08-29 `check-ratchet.ps1 -AcceptRemovals` is the only sanctioned way to retire a baselined test. A rename and a lost test look identical from outside, so removals go red by default; accepting them lists each one. Hand-editing `tests/parity-baseline.json` is forbidden because it would let real coverage loss through unnoticed.
- 2026-08-29 Skip reasons name a **capability** (`needs:lookbehind`), not a slice id, and Phase 1 opens with an API surface stub (slice S01). Both resolve contradictions in the design spec as originally written; the spec is amended inline and the changes are listed in its section 15.
- 2026-08-29 Per-slice cost is measured from `claude -p --output-format json`, which returns `usage`, `total_cost_usd`, `is_error` and `permission_denials` - more reliable than scraping session logs.
- 2026-08-29 The driver passes a scoped tool allowlist (Read/Write/Edit/Glob/Grep/TodoWrite/Skill/Task/Agent and Bash limited to dotnet, git, pwsh and python) with `--permission-mode acceptEdits`. No blanket permission bypass: an unattended session must not be able to run arbitrary commands on the machine.
- 2026-08-29 Python oracle runs `regex` **2026.7.19** from PyPI locally; the pinned submodule commit is tagged 2026.8.12, which upstream never published. **The gap is immaterial and this is measured, not assumed**: `git diff 0525aff..1760a20` (the 2026.7.19 tag to the pin) touches only `pyproject.toml`, `changelog.txt`, `.github/workflows/main.yml` and the `__version__` string in `regex/_main.py`. `src/_regex.c`, `src/_regex_unicode.c`, `regex/_regex_core.py` and `regex/tests/test_regex.py` are byte-identical, so matching behaviour is the same. Re-run that diff before trusting this at the next sync.
- 2026-08-29 The oracle in CI is **built from `upstream/`**, not installed from PyPI, so the ground truth is exactly the pinned commit; the Linux runners already have a compiler, so it costs nothing. `oracle.yml` asserts the built version equals the version in `upstream/pyproject.toml` - a silently mismatched oracle would report version drift as port defects. Verified the assertion has teeth by running it against the local 2026.7.19: it fails.
- 2026-08-29 **No C compiler installed on the Windows dev box**, deliberately. There is no MSVC at all, so a local from-source build means 3-7 GB of VS Build Tools, and for the current pin it would buy nothing but a different version string. Install it at the first upstream sync where the pinned commit genuinely differs from a published release - `sync-upstream` says how and when.
- 2026-08-29 Pester 6.1.0 installed for the PowerShell tooling tests (only the Windows-bundled Pester 3.4.0 was present, whose syntax is incompatible).
- 2026-08-29 `docs/STATUS.md` carries no timestamp and is written with LF endings, and `.gitattributes` sets `eol=lf` repo-wide. CI regenerates the board and fails on any diff; a wall-clock stamp made that check fail on every run, and a CRLF/LF flip rewrote every line on the Linux and macOS legs.
- 2026-08-29 `docs/plan/slice-log.jsonl` is gitignored. The budget gate counts slices from it, and the failed-slice rollback runs `git clean -fd`, which would have deleted it and reset the driver's daily cap to zero. Verified that `git clean -fd` (no `-x`) leaves ignored files alone.
- 2026-08-29 The driver launches `claude` through `ProcessStartInfo.ArgumentList`, not `Start-Process -ArgumentList`. The latter joins arguments with spaces and quotes nothing, so `Bash(dotnet *)` reached the CLI as two tokens and the scoped allowlist - the whole safety boundary for an unattended session - silently did not apply.
- 2026-08-29 A failed slice rolls back to the head recorded before the session, not to `HEAD`. A session can commit its work and move its slice file to `done/` and still leave the ratchet red; resetting to `HEAD` kept that commit and stranded the slice where it would never be retried.
- 2026-08-29 The editorconfig section for the port relaxations is `[src/FuzzyRegex/{Parsing,Engine}/**.cs]`. The obvious `**/*.cs` form requires an intervening directory, so it matched nothing directly inside `Parsing/` or `Engine/` - exactly where the ported engine lives - and every relaxation was inert. Verified with a probe file before and after.
- 2026-08-29 Phase 0 closed with a blind review that found eight defects, all confirmed and fixed. Five were in the slice driver and the ratchet, none would have been caught by the tests as they stood, and each fix that had testable logic gained a test. Worth repeating per slice as the spec requires.
- 2026-08-29 The root namespace is `Fuzzy.Text.RegularExpressions`, not `FuzzyRegex` (design spec amendment 8). A type cannot be named after its containing namespace and stay reachable: with namespace `FuzzyRegex` and entry type `FuzzyRegex`, a consumer writing `using FuzzyRegex;` gets `CS0118: 'FuzzyRegex' is a namespace but is used like a type` on `new FuzzyRegex(...)` and CS0234 on `FuzzyRegex.IsMatch(...)`. Measured with a throwaway project against the built library, not reasoned about; MA0049 flagged it and the suppression was rejected in favour of the fix. Assembly, NuGet id and every directory keep the name `FuzzyRegex`; test and benchmark namespaces moved too, because `FuzzyRegex.Tests.*` declared the colliding namespace itself.
- 2026-08-29 `FuzzyRegex.Match` keeps the .NET meaning (search anywhere, upstream's `search`); upstream's anchored `match` is `MatchAtStart` and `fullmatch` is `FullMatch`. Naming upstream's `match` `Match` would have silently given a .NET caller anchored behaviour - a trap no compiler catches. Static conveniences take `(input, pattern, options)`, `Regex`'s argument order rather than upstream's.
- 2026-08-29 S01's surface is wider than its slice file listed, by owner approval: the listed members could not express `fullmatch` (71 uses in the upstream suite), `partial` (91), `overlapped` (15), `pos`/`endpos` (22/14), `subn` (17) or `expand` (8), and S01's whole purpose is to be a compilation target for S02-S05. Counting the real usage first turned "is the stub wide enough?" from an opinion into a number.
- 2026-08-29 `FuzzyRegexOptions` uses upstream's `RegexFlag` bit values (`_regex_core.py:73-90`) rather than `RegexOptions`'s, so the parser port compares against upstream's tables directly. Nothing casts between the two enums, so the numbers need not agree with the built-in engine's.
- 2026-08-29 The pre-commit hook (slice S01b) formats and does not run tests; the suite belongs on pre-push and in CI. `run-slices.ps1` commits at the end of every slice moments after `check-ratchet.ps1` has run the whole suite, so a test-running pre-commit hook would run it twice per slice for no new information - and mid-slice TDD commits are *meant* to have failing tests. A local hook is also one `--no-verify` away from irrelevant, so it cannot be the gate; CI and the ratchet are.
- 2026-08-29 .gitignore negates `*.trx` for tools/tests/fixtures/. The Pester fixture sample.trx is an input that PortTools.Tests.ps1 reads, not test output, and the blanket `*.trx` rule meant it had never been committed - so the CI "Tooling tests (Pester)" step had been running against a file that does not exist on a fresh clone since Phase 0. `TestResults/` already covers the real test output on its own line. Found while committing S01, whose fix to that fixture was otherwise uncommittable.
- 2026-08-29 Review discipline is an evidence gate, not a pass count (spec amendment 9). The old wording, "one pass, no loops", conflated a critique loop (reviewer opines, code changes, reviewer opines again - genuinely harmful, stays banned) with repair against execution feedback (a red ratchet or an oracle divergence - ground truth, and two rounds capture 76-95% of achievable improvement across seven models with no model regressing, arXiv:2604.10508). It also read as one pass per *slice* rather than per *unreviewed change*, which is how S01 shipped ~200 lines of public API no reviewer had seen. Reviewers now hand over a reproduction rather than prose, and are not asked for explanations or proposed corrections, which measurably raise misjudgement (arXiv:2603.00539).
- 2026-08-29 Every review finding is reproduced before it is acted on. An adversarial gate of this shape killed ~79% of 171 candidate findings before advancement (arXiv:2604.19049), and removing execution-grounded validation from an agentic repair pipeline increased unnecessary repairs by 131.7% (arXiv:2604.10800). Automated code review is imprecise enough to need this: four techniques benchmarked in arXiv:2509.01494 scored between 2.79% and 9.22% precision. In S01 both reviews were reproduced finding by finding before any code changed.
- 2026-08-29 The differential oracle moves from Phase 6 to the start of Phase 3 (spec amendment 10). Scheduling the third correctness leg after the entire engine left Phases 3-5 resting on the ported suite and the ratchet alone, and those cannot carry it: in the closest published analogue only 72% of LLM-transpiled functions were semantically equivalent despite compiling and passing the existing tests (56.3-92.1% by codebase), with differential testing against the reference catching the rest at 85.7-88.2% precision and 100% recall (arXiv:2510.07604). Infrastructure already exists from Phase 0, so this changes when the harness is written, not what has to be built. The dialect-divergence caveat that normally weakens cross-engine differential testing (arXiv:2603.00311) does not apply: we compare against the exact upstream commit we are a port of, not across dialects.
- 2026-08-29 There is no `.csharpierrc`; `.editorconfig` is the only formatting configuration. CSharpier 1.3.0 reads `indent_style`, `indent_size` and `max_line_length` from `.editorconfig` directly, so a second config file would only be a place for the two to drift apart - the exact failure S01b exists to prevent. Measured, not assumed: a 112-character line wraps under CSharpier's default `printWidth` of 100 and survives untouched once `max_line_length = 120` is present. `endOfLine` is left at `auto`, which preserves what is there, and `.gitattributes` already pins that to LF repo-wide.
- 2026-08-29 CSharpier formats `src/FuzzyRegex/Parsing` and `Engine` like everything else - no ignore entry. The concern was that reflowing a hand-port would break the by-eye mapping onto `_regex_core.py` and `_regex.c`. Probed against the shapes those ports will actually take (dispatch dictionaries, opcode-name tables, a `case`-per-opcode switch, an if/elif chain, quoted upstream reference comments): CSharpier never reorders a statement, never touches a comment, and left the switch alone. It only rewraps, and for a table that means one entry per line, which makes an upstream entry appearing or disappearing a one-line diff rather than a three-row reflow. Where a specific grid must survive verbatim, `// csharpier-ignore` above it holds - verified on an aligned numeric table.
- 2026-08-29 There is no `.csharpierignore`. CSharpier already skips `obj`, `node_modules`, `.git` and `*.g.cs` by default and honours `.gitignore`, which covers `bin/` and `TestResults/`; `upstream/` contains no C# at all. `csharpier check .` reports 20 files, which is exactly the tracked source set. Add the file when something needs excluding, not before.
- 2026-08-29 CSharpier 1.3.0 also formats MSBuild XML, which was not expected. Accepted rather than ignored: the whole diff across seven `.csproj`/`.props` files is the removal of blank lines just inside `<Project>`, and an ignore list for it would be more configuration than the thing it buys.
- 2026-08-29 The tool manifest lives at `.config/dotnet-tools.json`, though the .NET 10 SDK's `dotnet new tool-manifest` now writes `dotnet-tools.json` at the repository root. `dotnet tool restore` resolves both; the `.config/` location keeps the root uncluttered. Verified restore from `.config/` after moving it.
- 2026-08-29 The Husky hooks are plain shell, not `task-runner.json` tasks. Both needed shell anyway: pre-push is a single `pwsh` line, and pre-commit needs `git add` after formatting, which the task runner has no concept of. `task-runner.json` is committed with an empty task list only because `dotnet husky install` recreates it on a fresh clone if it is absent.
- 2026-08-29 The pre-commit hook refuses a commit when a staged `.cs` file also has unstaged edits. It formats staged files and re-stages them with `git add`, and `git add` on a partially staged file sweeps the unstaged work into the commit as well - content nobody chose to commit. Refusing is a two-line guard; the stash-and-reapply dance that would handle it properly is not worth it for a case the slice driver never hits. Verified both paths: a fully staged mis-formatted file is fixed and re-staged, and adding an unstaged edit on top makes the hook exit 1.
- 2026-08-29 PowerShell 7 is documented as a hard prerequisite, and the `pre-push` hook gets no fallback to Windows PowerShell 5.1. Found by S01b's review: the hook calls `pwsh`, which is absent from an out-of-the-box Windows 11. A fallback would be worse than the failure, because 5.1 cannot run this tooling at all - `powershell.exe -Command "Import-Module ./tools/PortTools.psm1"` dies parsing `PortTools.psm1:387`, which uses null-conditional and ternary syntax that only PowerShell 7 has. The requirement predates the hook and was simply never written down; the hook is only what made it bite.
- 2026-08-29 Thread safety is asserted for `FuzzyRegex` (spec section 4, and the class doc) but was never asserted either way for `Match`. `Match` is **not** thread-safe, matching `System.Text.RegularExpressions.Match`, because `Groups` will be populated lazily. The gate for the pattern object is structural, not a concurrency test: a passing race test proves nothing, so the phase 2 slice that adds the first instance field to `FuzzyRegex` must add a reflection test asserting every instance field is `initonly`, which fires the moment a cache field appears - the actual failure mode, along with holding an `ArrayPool` rental on the instance instead of per-call. A multi-threaded stress test comparing against single-threaded results is a smoke test alongside it, never the gate. Not written in S02: every member throws `NotImplementedException`, so the field test would be vacuously green.
- 2026-08-29 Replacement templates speak **upstream's** language, not `Regex`'s: `\1`, `\g<name>`, `\n`, `\x41` and `\N{...}` are escapes and `$` is ordinary text. The two cannot both be honoured, because they disagree about `\` - measured against both engines, not reasoned: for the template `\n`, `regex.sub('.', r'\n', 'x')` gives a newline where `Regex.Replace("x", ".", @"\n")` gives a backslash followed by `n`. Choosing upstream's ports ~55 assertions in S02 verbatim (`test_sub_template_numeric_escape`, `test_symbolic_refs`, `test_basic_regex_sub`, `test_expand`) where choosing `Regex`'s would have made ~40 of them "not ported: different template language" - a permanent parity hole in the one area a fuzzy-regex user is most likely to have upstream code to move across. The cost accepted is that a .NET caller writing `$1` gets the literal text `$1`, which is exactly what upstream does. S01's `Match.Result` doc comment claimed `$1`/`${name}` and is corrected.
- 2026-08-29 `FuzzyRegex.Split` returns `string?[]`, not `string[]`, and puts `null` where a capturing group did not take part in a match. Upstream puts `None` there; the built-in `Regex.Split` instead omits the entry entirely, which both shortens the array and loses the difference between a group that matched empty and one that never ran. Verified against both engines: `regex.split('(x)|(1)', 'a1b')` is `['a', None, '1', 'b']` where `Regex.Split("a1b", "(x)|(1)")` is `["a", "1", "b"]`. Keeping upstream's shape ports all 8 affected assertions in `test_re_split` and `test_qualified_re_split` with identical positions and lengths.
- 2026-08-29 A `needs:<tag>` names the capability whose absence would make the test fail *and* that is least likely to land first; where a test needs two, the skip prose must name the combination, and **a slice must read the prose before un-skipping by tag alone**. Found in S02: five `Split` tests using `(?r)` and three using `\b`/`\m`/`\M` were tagged `needs:splitting`, so the slice delivering `Split` would have un-skipped tests that cannot pass without right-to-left search or word-boundary anchors. Retagged `right-to-left` and `anchors`. Two-capability tests were *not* retagged where both capabilities already have their own tests on the board (case-insensitive backreferences stay `ignore-case`; `\g<name>` in a template stays `substitution`), because nothing is hidden there - whereas `right-to-left` had no other test anywhere, so the mis-tag made the capability invisible to the status board and no slice would ever have been prompted to deliver it.
- 2026-08-29 A ported test's `#n` provenance counts **every** `self.assert` line in the upstream method, in source order, **including the branches of a `sys.version_info` guard this port does not use**. S02 set that convention implicitly (`test_qualified_re_split#4` is the `>= 3.7` branch, #5 the unused one); two of S03's three porting agents instead numbered only the taken branch, which silently shifted every later number in `test_zerowidth`, `test_unmatched_in_sub` and `test_bug_10328`. Renumbered, and the rule is now stated in each affected file's `<remarks>`. A provenance number that does not name the same upstream line every time is worse than none, because it is trusted.
- 2026-08-29 A `(?V0)`/`(?V1)`/`(?rV1)` pattern is ported as its **own** test and never folded into its unflagged sibling, even when upstream asserts the identical expected value, and it is tagged `needs:version-flags`. Upstream writes both because the parser has to accept the flag; folding them keeps the assertion and throws away the only thing being tested. Found in S03 review of the first agent's output, which had dropped all 13 version-flagged patterns in `test_search_reverse` and `test_zerowidth` as "incidental". This is what put `version-flags` (11 tests) on the status board - S02 had reserved the tag and left it unused, so before this no slice would ever have been prompted to deliver `(?V0)`/`(?V1)`.
- 2026-08-29 **Upstream test lines 1008-1740 contain no character above U+FFFF** - no `\U` escape, no astral literal, no surrogate escape; the only non-ASCII data is U+0300/U+0301 combining marks, U+00E0/U+00E1/U+00E9/U+00BD, U+0393/U+03B3, U+0410 and the Devanagari of `test_word_class`. Measured by scanning the range for code points > 0xFFFF and resolving every `\N{...}` name and `\uXXXX` escape, not assumed. So every Python codepoint index in S03 equals its UTF-16 index, the slice's headline risk did not arise, and its `Gaps/Surrogates` done-criterion ("one gap test per distinct surrogate situation encountered") is discharged as vacuous rather than skipped. The first real surrogate data is expected in S04/S05.
- 2026-08-29 Non-ASCII characters may be written as literal UTF-8 in a test source where a `const` is required (an `[Arguments]` value cannot be built from `(char)` casts, and C# `\uXXXX` escapes cannot be authored reliably through the agent tool path - they decode before reaching disk). Proven safe rather than assumed: no repo source carries a BOM, so the question was whether Roslyn reads BOM-less UTF-8 correctly on Windows. Read the `[Arguments]` values back out of the built `FuzzyRegex.Tests.dll` by reflection - all 18 rows of `PosixAndPropertyClassTableTests` round-trip as U+0393, U+03B3 and U+00E1, matching the Python oracle row for row. Elsewhere `(char)0xNNNN` casts or named constants stay preferred, because precomposed and decomposed forms are indistinguishable by eye.
- 2026-08-29 Oracle invocations set `PYTHONDONTWRITEBYTECODE=1` and `PYTHONIOENCODING=utf-8`. Without the first, running Python against `upstream/` leaves a `__pycache__` directory that makes the submodule show as dirty in the parent repo, which a slice could commit as a submodule pointer change; without the second, the Windows console codepage cannot print most of what these tests check (`UnicodeEncodeError: 'charmap' codec can't encode characters`). Both measured: with no env var `upstream/regex/__pycache__` appears on the first import and with it set it does not. Recorded in the `port-tests` skill.
- 2026-08-30 S04 widened the public stub with named lists, by owner approval: a `namedLists` parameter (`IReadOnlyDictionary<string, IReadOnlyCollection<string>>`) on the `FuzzyRegex` constructor and on the static `Match`, `MatchAtStart`, `FullMatch` and `Matches`, plus the `NamedLists` property S01 had deferred. Without it 41 assertions across `test_named_lists`, `test_fuzzy` and `test_partial` could not be *written*, let alone skipped: upstream passes named lists as Python keyword arguments (`regex.match(r"333\L<bar>444", s, bar=["one","two"])`) and no existing parameter could carry them, so the tests would not compile. This is the same argument that widened S01, and the same shape of evidence - count the affected assertions first. It is not sugar for an alternation: the oracle shows `\L<name>` composing with `(?fi)` full case-folding and with `{e<=1}` fuzzy budgets, so rewriting it as `(?:one|two)` would test a different code path. The widening added public API inside a test-porting slice, so the closing review covered `src/FuzzyRegex/FuzzyRegex.cs` explicitly.
- 2026-08-30 A `#n` provenance for `test_various` counts **table rows**, not `self.assert` lines. The method is a 524-row data table driven by a single loop holding one `assertEqual`, one `assertRaisesRegex` and one `fail`, so the normal convention would give every one of the 524 ported tests the same index and destroy the traceability it exists for. Rows are numbered from 1 in source order and the range is written compressed (`#21,24,30,35-37`). Every file generated for the method states the exception in its `<remarks>`, because a provenance number that means different things in different files is worse than none.
- 2026-08-30 A commented-out `#self.assert...` line **is** counted in provenance numbering (`test_recursive#29`, upstream line 2869). The numbering rule has to be mechanically reproducible by a future session, and "every line matching `self.assert`" is; "every line matching `self.assert` that is not commented out, and not inside a string, and ..." is a judgement call that two sessions will make differently. The index is counted and left unported, recorded in `docs/PORTMAP.md`.
- 2026-08-30 **Upstream test lines 1741-3083 contain no character above U+FFFF** - no astral literal, no `\U` escape. Measured by scanning the range for code points > 0xFFFF and for `\U000` escapes, as S03 did for its own range, before any index was ported. So every Python codepoint index in S04 equals its UTF-16 index and no span needed recomputing. The non-ASCII data that is there is BMP and confusable by eye - U+00DF (ß), U+0130 (İ), U+0131 (ı), U+FB06 (ﬆ) - so those are written as `(char)0xNNNN` casts rather than literal UTF-8, and the Turkic table's 16 rows were read back out of the compiled `FuzzyRegex.Tests.dll` by reflection and compared code point by code point against the oracle. **The prediction in S03's closing notes that S04 would bring the first real surrogate data was wrong**; the first non-BMP data, if any, is in S05.
- 2026-08-30 The fuzzy tests carry nine tags - `fuzzy-syntax`, `fuzzy-budget`, `fuzzy-bestmatch`, `fuzzy-enhancematch`, `fuzzy-insertion`, `fuzzy-substitution`, `fuzzy-deletion`, `fuzzy-counts`, `fuzzy-changes` - alongside the existing `fuzzy-matching`. The S04 slice file had suggested `fuzzy-charset`, which is unused because nothing in `test_fuzzy` puts an error budget on a character class, and had not anticipated `fuzzy-syntax`, `fuzzy-counts` or `fuzzy-changes`, which are three things that land separately from matching (constraint parsing, the reported edit distance, the reported edit positions). Where a `(?b)` or `(?e)` flag is present it decides the tag, so each flagged assertion sits beside a note naming its unflagged sibling's index and span. Which of insertion, substitution or deletion a subject needs was **measured** from `fuzzy_counts` against the oracle, not inferred from the subject's shape.
- 2026-08-30 The ported-test area for `test_partial` is `PartialMatching`, not `Partial`. CA1716 rejects a namespace segment that is a reserved language keyword (`partial`), and the analyzer is not disabled to get a build. The area name is what `docs/STATUS.md` prints, so the row reads `PartialMatching`.
- 2026-08-30 `test_named_lists` #15-16 are ported under the `CaseFolding` area, not `NamedLists`. They sit inside that upstream method but use no named list at all - they are `(?fi)\b(\w+) +\1\b` full-case backreference assertions - and the area name is the row heading on the generated status board, so filing them under `NamedLists` would misreport what has coverage. The provenance still names `test_named_lists`, and both files cross-reference each other.
- 2026-08-30 `test_various`'s 46 invalid-pattern rows are one test method, not one per upstream error constant. Twelve methods with identical bodies trip Sonar S4144, and the port does not assert error message text anyway (the convention set by `Ported/CharacterClasses/BadSetTests.cs` in S02), so the twelve constants and the rows belonging to each are recorded in the class `<remarks>` instead. All 46 were confirmed against the oracle to raise `regex.error`.
- 2026-08-30 **Upstream test lines 3084-4540 contain exactly two non-BMP sites**, the first in the whole port: `test_hg_bugs` #373 (the ZWJ-joined family emoji, U+1F468/U+1F469/U+1F467/U+1F466, upstream line 4161) and #433-434 (U+1F63A SMILING CAT FACE WITH OPEN MOUTH, line 4305). S03 predicted the first surrogate data would appear in S04 and S04 predicted S05; S04 was right. **The measurement method S03 and S04 used is not sufficient on its own and is corrected here**: scanning for literal astral characters and `\U` escapes finds #373 but misses #433-434 entirely, because upstream writes that one as the *name* escape `\N{SMILING CAT FACE WITH OPEN MOUTH}`. Every future range scan must resolve `\N{...}` names through `unicodedata.lookup` as well. The miss was caught by a porting agent reading its own assertion, not by the scan, which is luck and not a process.
- 2026-08-30 Where upstream passes `flags=regex.A` or `flags=regex.U`, the port writes a **leading inline `(?a)` / `(?u)` in the pattern** instead, because `FuzzyRegexOptions` has no ASCII or UNICODE member (PORTMAP records both as not surfaced). Proven equivalent against the local oracle on 2026-08-30 for every one of the 28 affected assertions rather than assumed, e.g. `regex.findall(r'(?u:\d)', '9\uFF19', flags=regex.A)` and `regex.findall(r'(?a)(?u:\d)', '9\uFF19')` both give `['9', '\uFF19']`, and all nine `\p{L}`-count rows of Git issue 575 match their inline form. This matters because Git issues 467, 572 and 575 exist *to test* how a scoped `(?a:...)` interacts with the global flag: rewriting the scoped part would have destroyed the thing under test, whereas moving only the global flag into a leading inline flag preserves it.
- 2026-08-30 `test_hg_bugs` #58 is ported as a plain `Replace` with no options, not as a WORD-flag test. Upstream writes `regex.sub(r'(\w+)', r'[\1]', subject, regex.WORD)`, and the fourth positional parameter of `regex.sub` is **`count`, not `flags`** - so `regex.WORD` (0x800) is used as a replacement count and the WORD flag is never applied. Measured on 2026-08-30: the call as written, the call with no fourth argument, and the call with `flags=regex.WORD` all return the identical string. Recorded because the assertion reads like a WORD test and a future session would otherwise 'fix' it by adding a flag the port does not have and upstream never exercised here.
- 2026-08-30 `test_hg_bugs` is filed under a new `Regressions` area, split across 29 files by the capability each assertion waits on (`RegressionsFuzzyTests`, `RegressionsPartialTests`, and so on), mirroring how S04 split `test_various`. One file per upstream method would have been a single 475-assertion class, and one file per Hg issue would have been 139 files; splitting by capability means the file a future slice opens is the file whose tests that slice un-skips. The upstream bug identifier ('Hg issue 38', 'Git issue 415') is kept in a comment above each test, so the grouping never costs traceability back to the original report.
- 2026-08-30 S05 coins five capability tags: `backtracking-verbs` ((*SKIP)/(*PRUNE)/(*FAIL), 34 tests), `define-groups` ((?(DEFINE)...), 16), `ascii-flag` (28), `posix-matching` ((?p) leftmost-longest, 8) and `keep-marker` (`\K`, 6). The 49-tag vocabulary was checked first and none of the five had an existing home: `posix` looked available but the three hits were the conventions test's own fixture data (`needs:posix0`), not a real tag, so grepping `docs/STATUS.md` alone would have been misleading. Each names a construct that lands as one piece of engine work, which is what makes it useful to a slice author.
- 2026-08-30 `Match.allcaptures`, `Match.allspans`, `Match.groupdict` and `Match.capturesdict` are **not** added to the public surface; the eleven assertions that use them are ported through `Groups` and `Group.Captures`, which already carry the same information. This follows S02's rule that a test-porting slice must not widen the public API, and unlike S04's named lists the tests compile and read fine without the addition - the S04 widening was justified by 41 assertions that could not otherwise be *written*, which is not the case here. Phase 2 can add them for their own sake if the shape is wanted.
- 2026-08-30 `test_hg_bugs` #74-81 are eight upstream assertions but one C# test body, written as a single `[Arguments]`-driven test carrying the upstream index as a row value. `Matches` is the one C# call behind both `finditer` and `findall`, and upstream's literal-versus-variable repeat is a Python object-identity concern with no C# counterpart, so all eight collapse to the same body - which trips Sonar S4144 (identical method implementations), and the analyzer is not disabled to get a build. Written as eight `[Arguments]` rows rather than folded into two tests so the status board still counts eight, matching upstream's assertion count. The index has to be a row value because eight identical rows would not be distinguishable from one another.
- 2026-08-30 U+0085 NEL, U+2028 LINE SEPARATOR and U+2029 PARAGRAPH SEPARATOR must be written as `\uXXXX` escapes in C# source, never as literal characters, even inside a string literal. C# treats all three as **source line terminators**, so a literal one ends the line mid-string: `error CS1010: Newline in constant`. Hit in `RegressionsUnicodePropertyTests` porting Git issue 477's `\p{VertSpace}` subject, which contains all three. This is a different failure from the confusable-character problem DECISIONS already records - those compile and read wrong, these do not compile at all.
- 2026-08-30 A `\uXXXX` escape written through the agent Edit/Write tool path is decoded to the character before it reaches disk, confirmed again in S05 (an edit writing `"\u200D"` landed as a literal ZWJ, checked with `cat -A`). The reliable route for escape text is a short Python script that writes the file, and the reliable check is `cat -A`, not the Read tool, which renders `\\X` and `\X` identically. S03 recorded the decoding hazard; what is new is that **`cat -A` is the only cheap way to see the difference**, and that `char.ConvertFromUtf32(0x1F63A)` sidesteps it entirely for astral characters.
- 2026-08-30 Porting agents running in parallel must be told **not to build the whole test project and not to touch a file they did not create**. Nine agents shared one working tree in S05; several ran `dotnet build`, saw compile errors from other agents' half-written files, and one renamed eight of its siblings' files to `*.bak` to get a clean build - removing finished work from the build. No work was lost (it was restored on instruction and verified against `git status`), but the shared tree is the hazard, not the agent. The brief now says the coordinator fixes the build centrally after everyone finishes, which is also what STATE.md already recommended for a different reason: a whole-project build in one place caught every analyzer failure that nine isolated builds did not.
- 2026-08-30 **A Python `span()` is `(start, end)`; our `Capture` exposes `(Index, Length)`. Converting one to the other by copying the pair is wrong for every capture that does not start at 0**, and it is invisible in review because the common cases (a match at index 0, a single-character match at index 0) coincide. S05's blind review caught two: `test_hg_bugs` #435-436 asserted captures of `(.)+` over `"abc"` as `(0,1),(1,2),(2,3)` where the lengths are all 1, and #60 asserted `(1,2),(0,2)` for `spans("x")` where the inner group captures one character at index 1. Both would have demanded wrong behaviour from a correct engine. The rest of the slice writes `(m.Index, m.Index + m.Length)` when it wants a span, which is the form to prefer, and the sweep for the `(Index, Length)` form found only these two genuine cases out of four sites. Phase 3 onward will meet this constantly - every upstream `span`, `spans`, `allspans` and `regs` value is end-relative.
- 2026-08-30 **A control assertion gets the tag for what it actually needs, not for the construct it is contrasted with.** Upstream repeatedly writes a pair: a pattern without the feature, then the same thing with it. S05 initially tagged four such controls after their sibling - `(?:fe)?male` as `conditionals`, `(?:(?![a-d]).)+` as `define-groups`, `(?:(?=\d)\d+\b|\w+)` as `conditionals`, `t(es){i<=1,0<e<=1}t` as `fuzzy-syntax` - none of which contain the construct named. This is the S03 mis-tag failure in a new guise: the slice delivering conditionals would un-skip a test that needs lookahead, and it would fail. Retagged to `quantifiers`, `lookaround`, `lookaround` and `fuzzy-budget`. Same fix for `test_hg_bugs` #222, which the port had given a `RightToLeft` option upstream does not pass at all - it is the forward control for #223.
- 2026-08-30 **Phase 2 opens with a compile-parity corpus (S06), not with parser code.** Intercepting `regex._regex.compile` while upstream's own `test_regex.py` runs records the exact bytecode, group index, required-string data, parse errors and compiled replacement templates for every pattern the suite compiles: 1534 compiles, 46 errors, 62 templates, 29,219 integers, 0.2 seconds. Measured, and the interception proven, before the slices were written. Every Phase 2 slice is verified against it because nothing in Phase 2 can match a string, so the ported suite and the Phase 3 match oracle both have nothing to say about the parser.
- 2026-08-30 **84 of those 1534 compiles are nondeterministic upstream**: `RegexBase.__hash__` hashes the node's class object, whose identity is per-process, and two sites turn a `set` into an ordered list that reaches the bytecode (`_check_firstset`, `Branch._reduce_to_set`). Set-member order has no matching semantics. Both the corpus recorder and the port sort at exactly those two points; the deviation goes in PORTMAP's "Where we diverge". Proof is two recorder runs under different `PYTHONHASHSEED` values producing identical fixtures.
- 2026-08-30 **Phase 2 slices are vertical** (parse function, node class, bytecode, per construct family), not upstream's horizontal order (all parse functions, then all node classes), so that every slice's output is checkable against the corpus the day it lands. `Parsing/` still mirrors `_regex_core.py` file for file and name for name; only the order of filling it in differs.
- 2026-08-30 **Unicode tables are transliterated from `upstream/src/_regex_unicode.c`, not regenerated from UCD data, and land in Phase 2 (S09), not Phase 6** (spec amendment 11). The parser consults them at fourteen call sites, so leaving them to Phase 6 would have left most of the corpus unverifiable. The C file is entirely generated (about 250 arrays, three struct arrays, 104 lookup functions of roughly twenty statement shapes), so copying its numbers gives parity by construction. `src/FuzzyRegex.UnicodeGenerator/` (a two-line stub) is deleted. Options and measurements: `docs/plan/2026-08-30-phase2-decisions.md`.
- 2026-08-30 **`\N{name}` gets a generated character-name table at Unicode 17.0.0**, the version of upstream's tables, not the 16.0.0 that CPython 3.14's `unicodedata` (which upstream calls) reports. .NET has no character-name API and 41 ported tests use the construct. 34,137 names need storing after the algorithmic ranges (CJK, Hangul, Tangut and others, 114,716 codepoints) are computed; expected 200-300 KB. Names new in 17.0.0 resolve here and raise under upstream on Python 3.14; CPython 3.15 moves to 17.0.0 and the divergence disappears.
- 2026-08-30 **`FuzzyRegexOptions.ExplicitCapture` and `FuzzyRegex.Unescape` are removed** (S07 and S12). Both were S01 analogies with `Regex`; neither has an upstream counterpart, neither is used by any test, and neither can be verified by the corpus, which would have made them the only unverified logic in the parser.
- 2026-08-30 **No deferred `Match` or option members are added in Phase 2**: `Match.pos/endpos/string/re` (4 reads in the upstream suite, all in `test_getattr`), `allcaptures/allspans/groupdict/capturesdict` (15 lines, all ported through `Groups` and `Captures`), and public `Word`/`Ascii` options (the tests use the inline forms). All are additive and Phase 3 is the first point at which a real `Match` exists to design against.
- 2026-08-30 **The compile-parity corpus is 1547 compiles, 50 errors, 62 templates and 29,322 bytecode integers**, correcting the 1534/46/62/29,219 measured when S06 was authored. Two causes, both deliberate: the recorder forces `cache_it=False`, so a pattern a later test compiles under different named lists gets its own row instead of a cache hit; and it records the rejections upstream raises as a plain `ValueError` rather than `regex.error` (four `(pattern, flags)` pairs, all "ASCII, LOCALE and UNICODE flags are mutually incompatible", `upstream/regex/_main.py:559-568`). Catching only `regex.error` had left those four with no row of any kind, so a port that happily compiled `(?au)\w` would have turned nothing red.
- 2026-08-30 **The "84 nondeterministic compiles" figure is wrong and unmeasurable as stated**; the number varies per run. `RegexBase.__hash__` hashes the node's class *object*, whose hash is its address, so `PYTHONHASHSEED` is not what varies - every fresh process is a fresh sample. Measured with the two sort patches disabled: one pair of runs disagreed on 51 rows, another pair on 82, and eight runs between them touched 111 of the 1547. Any single pair undercounts. With the patches in place all eight runs were byte-identical. `--verify-determinism` therefore does four runs, and is a smoke test rather than a proof: a leak affecting one row could survive a handful of runs.
- 2026-08-30 **There is a third ordering leak, and it is in the caller's container, not upstream's code.** `StringSet.__init__` (`_regex_core.py:4088-4100`) iterates `info.kwargs[name]` and sorts the branches by length only - a stable sort, so two equal-length members keep the caller's iteration order - and upstream's own suite passes a `set` (`test_regex.py:2578-2579`). Found by the two-seed check, not by reading. Fixed at the input: the recorder sorts each named list before handing it over, so the fixture's sorted order is the order upstream compiled in, and **the port sorts nothing** - it keeps whatever order its caller gave. Consequence for S07: the seam takes named lists as `IReadOnlyList<string>`, but the public `FuzzyRegex` surface types them as `IReadOnlyCollection` (S04), which does not carry an order that reaches the bytecode. Decide there whether to tighten the public signature.
- 2026-08-30 **`--check` compares the recorded values, not the file bytes, because `regexVersion` legitimately differs between the dev machine and CI.** The oracle job builds `regex` 2026.8.12 from the pinned submodule; this machine has 2026.7.19 installed. A whole-file `filecmp` failed on that one line and would have reported "stale" about a fixture whose every recorded value was correct - on the very first CI run. Everything else, `upstreamCommit` included, still has to match, and the committed file is separately round-tripped through the writer so a hand-edited or reformatted fixture is rejected. Caught by the S06 blind review; it had never run, because the local `--check` passes for the wrong reason.
- 2026-08-30 **`BeEquivalentTo(..., WithStrictOrdering())` recurses into nested collections**, which made it the wrong tool for `named_list_indexes`: the outer position is the parser-assigned index and must match, but each entry is a `frozenset` upstream whose order the recorder invented when it wrote JSON. Measured: 18 of the corpus's 30 named-list rows have an entry with more than one member and would have demanded that invented order from a correct port, while 0 rows have more than one named list, so the ordering it was added for is not exercised at all. Both the seam and the fixture now type these as `IReadOnlySet<string>`, and the assertion is `Equal(expected, (a, b) => a.SetEquals(b))` - outer order strict, inner set equality. The outer index order remains unverified by the corpus; the named-lists slice needs its own gap test for it.
- 2026-08-30 **`Source` reads whole codepoints but counts UTF-16 code units.** Upstream's `Source.pos` is a codepoint offset; ours is a `char` offset, because AGENTS.md makes every public index in this port a UTF-16 code unit and `FuzzyRegexParseException.Offset` is public. `Get`/`Peek` still return whole codepoints, so a surrogate pair is one character to the parser and the **bytecode is identical to upstream's**: measured 2026-08-30, `'\U0001F63A'` compiles to `[12, 1, 128570, 1]` (one `CHARACTER`, not two surrogate ones) and `'a\U0001F63Ab'` to a `STRING` of length 3, not 4. Only the reported offset differs, and only for a pattern with a non-BMP character before the failure: `'\U0001F63A('` is `pos=2` upstream and `Offset=3` here. No corpus row contains a non-BMP character, so this costs no parity; `Gaps/Parsing/SourceScannerTests.cs` pins all four facts.
- 2026-08-30 **`is_cased_i` is ASCII-only in S07, and that is what unblocked `(?i)` patterns a phase early.** Upstream asks `_regex.get_all_cases`, a four-level lookup into the generated Unicode tables (`_regex_unicode.c:30627`), and `Sequence._flush_characters` calls it for **every** `IGNORECASE` literal run - so without an answer, no `(?i)` pattern compiles and the five `test_getattr` assertions S07 was told to turn on could not have been. Measured against the built oracle: over codepoints 0-127 both the `UNICODE` and `ASCII` encodings report exactly A-Z and a-z as cased, and they first disagree at U+00B5, so 0x80 is the exact boundary below which no guessing is involved. Above it, and under `LOCALE`, the port throws `needs:unicode-tables` rather than guess. Same treatment for Python's Unicode-aware `str.isdigit`, `str.isidentifier` and `str.isalpha`, which the port answers only for ASCII. **S09 deletes all of this**; PORTMAP's "Where we diverge" carries the rows to remove.
- 2026-08-30 **Upstream's `ValueError` rejections become `FuzzyRegexParseException`, not `ArgumentException`.** The corpus pins `Message` against upstream's exact text, and `ArgumentException` appends "(Parameter 'x')" when given a parameter name and trips Sonar S3928 when given none; there is no third option that keeps the message intact. It costs nothing today because none of the three sites is reachable through the public constructor - `FuzzyRegexOptions` has no `ASCII`, `LOCALE` or `UNICODE`, and `Version0|Version1` is rejected earlier by `RegexFlags.DefaultFlags`, which is itself the port of a `KeyError` upstream lets escape (verified: `regex.compile('x', regex.V0|regex.V1)` raises `KeyError: regex.V0|V1`, not the "mutually incompatible" `ValueError`, because `Info.__init__` runs before that check). Revisit in the slice that surfaces those flags as options.
- 2026-08-30 **A `needs:` throw must go at the line that actually needs the missing thing, not at the top of the branch.** S07's blind review found `\p`, `\P`, `\N` and `\g` throwing `needs:unicode-properties` / `needs:named-characters` / `needs:backrefs` on sight, when upstream reaches an ordinary literal through all four whenever the delimiter is absent (`\p` is the literal `p`, and so on): `parse_property` and `parse_named_char` rewind and fall through, and `parse_escape` catches `parse_group_ref`'s `error` and falls back. Six corpus rows and two ported tests were skipping for capabilities they do not need. The general rule for the remaining Phase 2 slices: port the whole function's control flow and throw only where upstream consults a table or builds a node this slice does not have.
- 2026-08-30 **`Gaps/Parsing` as a test namespace shadows `Fuzzy.Text.RegularExpressions.Parsing`** for any file whose own namespace sits under `Tests.Gaps`, so `Parsing.PatternCompiler` in `Gaps/CompileParity` stopped resolving the moment the folder was added. Fixed by qualifying that one reference as `RegularExpressions.Parsing.PatternCompiler` rather than by renaming the folder, so the obvious home for parser gap tests stays available.
- 2026-08-30 **`parse_name` and `Info.is_open_group` compare the group number as a `BigInteger`, not an `int`.** Python's `int()` has no fixed width, so upstream does not overflow on `\g<99999999999`: it parses the number, finds it is not below `min_group`, and carries on to fail on the missing `>` - which `parse_escape` catches and degrades to literals. `int.Parse` threw `OverflowException` straight through that catch, turning a pattern upstream compiles into an error. Found by S07's second blind review, reproduced against upstream 2026.7.19. The value is only ever compared against 0 or 1, so the cost is one parse of a string that is already known to be all digits, at parse time. **Both call sites needed it**: the S07 review of the first fix found `Info.IsOpenGroup` parsing the same name with `int.Parse` one call later, so the delimited `\g<99999999999>` still threw `OverflowException` where upstream raises "invalid group reference". **One divergence is deliberate**: CPython 3.11+ caps `int(str)` at `sys.get_int_max_str_digits()` (4300 by default) and raises `ValueError`, so upstream rejects `\g<` followed by 4301 nines while we compile it. Measured 2026-08-30. Not ported, because that cap is an interpreter setting - changeable at runtime with `sys.set_int_max_str_digits`, raised as `ValueError` rather than `regex.error`, and with no .NET equivalent - so it is a property of the host Python, not of the regex grammar this port mirrors.
- 2026-08-30 **A `needs:` seam that blocks a fallback path is left in place, not widened for the one example that would pass.** `\g<` followed by a non-ASCII name degrades to literals upstream, and reaching that path in the port means getting past `IsDigitName`'s `needs:unicode-tables` throw. Upstream's answer here does not depend on the Unicode tables - no definition of "digit" contains e-acute - but deciding that in general is S09's work, and relaxing a seam because one case looks obvious is how a port acquires a guess it cannot later find. Recorded as a skipped test (`Gaps/Parsing/GroupReferenceFallbackTests.cs`) that S09 turns on.
- 2026-08-30 **The unattended driver must never let a session end its turn with a review outstanding, and must never destroy uncommitted work.** S07 took three attempts: two `claude -p` runs reached a green ratchet and then died without committing, because the session posted a "review still running" message and ended its turn - and under `-p`, ending the turn ends the process. `Undo-FailedSlice` then treated "no commit was made" the same as "the ratchet is red" and `git reset --hard`ed a green tree, twice, at ~50M tokens a time. Three fixes: `port-slice` now states that the reviewer is a blocking call read in the same turn and that a session may not end with uncommitted work; `Undo-FailedSlice` stashes with `-u` before resetting, so a failure is recoverable rather than final; and the driver allowlist was corrected on two axes. Claude Code decomposes a compound command and requires every part to match, so `dotnet build ... | Select-Object -Last 60` is denied on the filter rather than on `dotnet`; and **`Bash(...)` rules do not govern the `PowerShell` tool**. This machine sets `CLAUDE_CODE_USE_POWERSHELL_TOOL=1` globally and the child session inherits it, so a Bash-only allowlist silently denied every PowerShell call the session made. The list now carries both families: `Bash(head|tail|grep *)` for the Bash side, `PowerShell(Select-Object|Select-String|Get-Content|Get-ChildItem *)` for the cmdlets, and `dotnet`/`git`/`pwsh`/`python` mirrored on both. Verified 2026-08-30 by driving `claude -p` with the committed array: `dotnet --version | Select-Object -Last 1` returned `10.0.400` with zero permission denials, where the Bash-only version was refused.
- 2026-08-30 **`Undo-FailedSlice` moved from `run-slices.ps1` into `PortTools.psm1`, for the same reason `Get-SliceFailureReason` lives there: it is testable only where the Pester suite can reach it.** It takes `-RepoRoot` as a parameter rather than closing over the driver's script scope, and returns the stash label instead of writing to the host, so the tests can assert on it and the driver keeps the console output. Six cases against real scratch repositories, not mocks - every bug this function has had was in what git actually did. Both mutations checked: deleting the stash line fails 2 tests, and `clean -fdx` in place of `-fd` fails the one that pins the gitignored slice log surviving. That log is what the budget gate counts, so destroying it would silently reset the driver's own daily cap.
- 2026-08-30 **Upstream's generated bytecode is readable without upstream's C compiler, and that is the only way to see a maximal repeat count.** `regex.compile('a{4294967294}')` spends about twenty minutes in `_regex.compile` and then raises `MemoryError`; everything before that call is O(1) (measured: parse, optimise, compile and flatten all 0.000s at a count of 8,000,000, code length 8). So the oracle for such a pattern is a monkeypatch of `regex._regex.compile` that captures its arguments and **raises instead of calling through** - the dead S08 session's script called through, which is why it appeared to hang. With that, `(?:a{4294967294}){0,4294967294}a` reports `req_offset = -1` and `(?:a{65535}){0,65535}a` reports `req_offset = 4294836225`.
- 2026-08-30 **`max_width` saturation at `long.MaxValue` is confirmed indistinguishable from upstream's unbounded `int`, and the claim is now pinned by a test.** Saturation can only happen above `long.MaxValue`, which is 2^31 times `UNLIMITED`, and every consumer either takes `min(w, UNLIMITED)` or asks `w >= UNLIMITED` - so both the saturated and the true value give the same answer. `Gaps/Parsing/RepeatWidthOverflowTests.cs` pins the saturating case, a non-saturating control that would catch a `Multiply` that always saturated, and the single maximal count. No corpus row can cover any of them, because upstream's own compiler cannot compile them.
- 2026-08-30 **`CompiledPattern.ReqOffset` is a `long`, not an `int`.** Found by writing the test above: the offset is a repeat's maximum width and a count runs to `UNLIMITED - 1` = 4294967294, so `(int)reqOffset` truncated `(?:a{65535}){0,65535}a`'s offset of 4294836225 to -131071. Upstream hands the value to C as a `Py_ssize_t`. No corpus row has an offset above `int.MaxValue`, which is why 693 rows passed with the bug in place.
- 2026-08-30 **The four `RegressionsParseErrorTests` were transcribed in Phase 1 as `"<msg> at position N"`, which is Python's `str(error)` and not this port's shape.** S01 split the two apart: `Message` carries upstream's `error.msg` and `Offset` carries its `pos`. Upstream's own assertion is `assertRaisesRegex(regex.error, MULTIPLE_REPEAT)` where `MULTIPLE_REPEAT` is the bare string `"multiple repeat"` (`test_regex.py:41`), so asserting the message and the offset separately is stricter than upstream, not weaker. Verified against `regex` 2026.7.19: `.???` gives `error: multiple repeat at position 3` with `pos=3`. The three still-skipped tests in that file carry the same wrong shape and each slice that turns one on must split it the same way.
- 2026-08-30 **The Unicode tables are transliterated from upstream's generated C, and the transliterator has to consume the file exhaustively or the whole approach is worthless.** `tools/transliterate-unicode.py` walks a cursor over `upstream/src/_regex_unicode.c` and every byte must match a known construct - a comment, a `#define`, a typedef, a table or a function - so a shape a future upstream introduces fails loudly instead of being skipped. Measured: 31,587 lines, 253 tables (402,293 values), 104 lookup functions built from 44 distinct statement shapes, of which 11 rules cover the 100 generic ones. On top of the cursor, four assertions: every `extern` the header declares must be emitted at the length the header declares, there must be 253 tables, there must be 104 functions, and the hand-ported set must be exactly the four known names.
- 2026-08-30 **Four of the 104 generated lookup functions are hand-ported rather than transliterated, and pinned by digest.** `re_get_all_cases`, `re_get_simple_case_folding`, `re_get_full_case_folding` and `re_get_script_extensions` do not share the three-level-walk shape of the other hundred: they index struct arrays or walk a zero-terminated run. Auto-generating their sixty lines of C# would need about twenty single-use translation rules. Instead they live in `src/FuzzyRegex/Unicode/UnicodeCasing.cs` and the transliterator carries the SHA-256 of each one's whitespace-normalised C body, so an upstream edit fails the script with the new digest to paste in rather than leaving the C# quietly stale.
- 2026-08-30 **The C# element type for a generated table is chosen by what the runtime can put in RVA data, not by uniformity.** A `RE_UINT8` table becomes `static ReadOnlySpan<byte> X => [...]`, which Roslyn emits as a metadata blob with no allocation and no static constructor. A `RE_UINT16` table cannot: RVA spans are byte-only. It becomes `static readonly ushort[]`, which Roslyn still emits as a blob plus `RuntimeHelpers.InitializeArray` - and that path byte-swaps on a big-endian runtime, where `MemoryMarshal.Cast` over a byte blob would silently return the wrong numbers. Every constant is `int`: the widest is `RE_PROP_C_MASK` at 0x30020003, and `int` is what keeps `1 << PropZl` legal, since C# demands an `int` shift count where C takes any integral type. Same reason for the `(int)` casts on every table index: `ReadOnlySpan<T>`'s indexer takes an `int` where a C array takes any integral type.
- 2026-08-30 **A struct array in the C becomes one flat C# array per field, named after the field.** `re_all_cases_table_4` becomes `AllCasesTable4Delta` and `AllCasesTable4Others` (stride 3); `re_properties` becomes `PropertiesName`/`PropertiesId`/`PropertiesValueSet`. A transposed field order is the classic transliteration bug in a struct table, and this makes it unwriteable. C zero-fills a partly initialised aggregate, so the flattening pads: `{ 6310, { 1090, 7301}}` has two `others` in the source and three slots in the declaration.
- 2026-08-30 **`re_get_property[86]` is NULL upstream and throws here.** `RE_PROP_SCX` is many-valued, so `unicode_has_property` branches to `re_get_script_extensions` before it ever indexes the table; calling through the NULL would be undefined behaviour in C. The transliterator asserts the NULL slot set is exactly `[86]`, so a future upstream that adds another many-valued property fails the script.
- 2026-08-30 **The transliterated tables are checked against a second, deliberately naive parse of the same C file, run in Python.** `tools/transliterate-unicode.py` agreeing with itself proves nothing; `tools/record-unicode-fixtures.py` re-reads the C with a flat `re.finditer` sweep that shares no code with it and records a length and a SHA-256 per table. `UnicodeTables.AllTables()` - a generated accessor - lets the C# test walk all 253 and compare. That is what makes 402,293 transliterated numbers a checked quantity rather than a hope.
- 2026-08-30 **Casing parity is exhaustive over every codepoint and stored as one hash per combination, not as rows.** `get_all_cases` and `fold_case` for all 1,114,112 codepoints across (Unicode, ASCII) x (simple, full), plus `fold_case` without `IGNORECASE`, is nine SHA-256s in the fixture and about two seconds of C#. `get_all_cases.ascii.full` and `get_all_cases.ascii.simple` are the same digest, which is correct rather than a bug: `ascii_full_case_fold` never returns more than one codepoint, so the `None` marker upstream appends when a character expands under full folding is never appended for ASCII. Narrow any mismatch by plane and then by block in a throwaway script - a hash says only that something is wrong.
- 2026-08-30 **Property *lookups* are sampled here, not exhaustive, and that is deliberate.** The property dictionary itself - all 185 properties, 5,200 property/value pairs, `munge_name` and `init_property_dict` - is hashed whole, because a wrong property id means `\p{...}` silently matches the wrong set. The lookups behind it are checked at 1,595 codepoints (dense over U+0000-U+01FF, then a 1021-codepoint stride), because exhaustive would be 5,200 pairs times 1.1M codepoints in Python. Exhaustive property parity is Phase 3's job, through the match oracle, where `\p{...}` can actually be run - design spec section 6 says so.
- 2026-08-30 **CPython's `str.isalpha`, `str.isdigit` and `str.isidentifier` are exactly three of upstream's own properties, measured over every codepoint.** `isalpha` is `\p{General_Category=L}`; `isdigit` is `Numeric_Type` of `Decimal` or `Digit` (which is CPython's rule too - its `DECIMAL_MASK` and `DIGIT_MASK` come from UnicodeData.txt fields 6 and 7); `isidentifier` is `XID_Start`-or-`_` for the first codepoint and `XID_Continue` after, with no normalisation. Zero disagreements over all 1,109,309 codepoints CPython 3.14.6 can answer for - everything except the 4,803 added in Unicode 17.0, which its 16.0.0 `unicodedata` does not know. This is what let S09 delete the four ASCII-only seams S07 left, rather than shipping a second Unicode dataset for the Python side.
- 2026-08-30 **`\N{...}` needs a character-name table, it is the one place this port reads a UCD file, and it is generated at Unicode 17.0.0 rather than at the host's version.** .NET has no character-name API. `tools/build-character-names.py` reads `UnicodeData.txt` and `NameAliases.txt` for 17.0.0 - the version `_regex_unicode.h` declares - and emits 40,951 stored names; the four kinds of algorithmic range (Hangul 11,172, CJK about 100,000, Tangut about 6,200) are computed. **Verification is in Python, against the host's own `unicodedata`**, which is an implementation independent of those data files: all 148,853 names it knows must resolve to the same codepoint, and every algorithmic name we compute must resolve back through `unicodedata.lookup`. Both hold. The only unverifiable part is CJK Extension J (U+323B0-U+33479), which is entirely new in 17.0 and is counted instead. The contract was measured, not inferred: the lookup is case-insensitive but normalises nothing else (`LATIN_SMALL_LETTER_A` and `LATINSMALLLETTERA` both raise), aliases resolve, a real name beats an alias (`BELL` is U+1F514, not U+0007), and an algorithmic name takes the codepoint in upper-case hexadecimal with no leading zeros (`CJK UNIFIED IDEOGRAPH-04E00` raises).
- 2026-08-30 **Named sequences are not carried, and the cost of that is one error message.** `unicodedata.lookup("KEYCAP DIGIT ZERO")` returns three codepoints, and `parse_named_char` immediately calls `ord()` on the result - so upstream raises `TypeError` where we say "undefined character name". Adding about a thousand more names to change which error a caller gets is not worth the size.
- 2026-08-30 **The character-name table is stored whole - 40,951 string literals, 1,044,804 characters - and that is 2.1 MB of a 3.5 MB assembly.** Measured after the slice: `dotnet build -c Release` of a clean tree takes 4.0s with the 1.5 MB and 1.85 MB generated files in it, so compile time is not the constraint; package size is. The upgrade path, if the owner wants it, is CPython's: the names are built from a vocabulary of a few thousand words, so storing word indices and tokenising the query would cut it to a few hundred kilobytes. Left until phase 7, where size and speed get measured rather than guessed.
- 2026-08-30 **The parity ratchet could not tell apart two tests whose ids differ only in case, and S09 is the first slice to write any.** PowerShell's default hashtable comparer and `Sort-Object -Unique` are both case-insensitive (`@('Abc','abc','ABC') | Sort-Object -Unique` returns one element), so `Update-Baseline` collapsed `Known_names_resolve(LATIN SMALL LETTER A)`, `(Latin Small Letter A)` and `(latin small letter a)` into one entry and `Test-Ratchet`'s `$byId` lookup kept only whichever came last. Measured: 860 passing tests were recorded as 855, four collision groups, all of them tests of case-insensitive behaviour. The ratchet is the merge gate, so a test it silently stops watching is lost coverage: `Update-Baseline` now sorts `-CaseSensitive` and `Test-Ratchet` keys on an ordinal `Dictionary[string, object]`. **Pester's own `-Contain` and `-Be` are case-insensitive too**, so the two regression tests use `-BeExactly` and `-cmatch`; written first, watched fail, and re-checked by reverting the fix with the suite still in place (2 failures back, then 0). S10 is the case-folding slice and will write many more such names.
- 2026-08-30 **`src/FuzzyRegex/Unicode` was added to the faithful-port `.editorconfig` relaxations, and the `_regex.c` Unicode helpers were put there rather than in `Engine/`.** The relaxation block already covers `Parsing` and `Engine` for the stated reason that a hand-port must keep upstream's shape; `Unicode` is the same kind of directory and the same two rules fire on it (`MA0025` for the `LOCALE` seam, `MA0051` for `unicode_has_property`'s length). The `*.g.cs` files are excluded from analysis entirely by the existing `generated_code` rule, which is why they are named `.g.cs` rather than the folder being marked. Putting `unicode_has_property` and the five module functions under `Unicode/` rather than `Engine/` keeps every table-facing symbol in one directory the parser can use before a VM exists; PORTMAP's file-level map records the split.
- 2026-08-30 **`Sequence._fix_full_casefold` calls Python's `str.lower()`, and that call changes the bytecode - so the port needs a lowercase table, its second and last UCD file.** Folding does not subsume lower-casing: `fold_case` leaves `I` alone, because `I`/`i`/`ı` are the Turkic triple, so `fold("fI")` is `"fI"` and only `.lower()` turns it into `"fi"` - which is the folded form of the ligature U+FB01, and therefore the thing that makes the literal a full-case-folding chunk. Measured against the oracle: `'fI'` under `I|F|U` compiles to `STRING_FLD` and under `I|U` to `STRING_IGN`. Upstream's own generated tables carry no lowercase mapping and .NET's is a different Unicode version (67 codepoints disagree with CPython 3.14.6, measured), so `tools/build-lowercase.py` reads `UnicodeData.txt` field 13 and the *unconditional* rows of `SpecialCasing.txt` for 17.0.0 and emits 1,488 mappings, verified in Python against the host's own `str.lower()` for all 1,109,309 codepoints it knows, with zero disagreements. The 16 conditional `SpecialCasing` rows are skipped: 15 are locale-conditional and CPython does not apply them either, and the 16th is final sigma, which CPython *does* apply but which cannot fire here - the text being lower-cased has already been through `fold_case`, and no codepoint folds to U+03A3 over all 1,114,112.
- 2026-08-30 **Giving every node an ignored `in_set` parameter would have compiled a pattern upstream rejects, and the differential wave caught it.** Upstream declares `in_set` on six classes only, so `AnyAll.optimise(info, reverse, in_set)` is a `TypeError` - and that is reachable, not theoretical: `(?V1)[a--[\s\S]]` reduces its first operand to an `AnyAll` and `SetDiff.optimise` then passes it straight on. The first draft of S10 had one virtual with the parameter, and compiled the pattern. `RegexBase` now has two methods: `Optimise(info, reverse)`, which every node has, and `Optimise(info, reverse, inSet)`, which only `Character`, `Property`, `Range` and the four set types override and whose base throws. Pinned by `Gaps/Parsing/UpstreamInternalErrorTests.cs`, along with the two neighbouring cases where upstream also raises a Python internal error rather than `regex.error` (`[^\s\S]` and an inline `(?V1)` under the `V0` flag).
- 2026-08-30 **`(?L)` on a `str` pattern does nothing until casing is consulted, which is what S09 left this slice to settle.** Measured: the resolved flags come out `LOCALE|VERSION0` - `_main._compile` stops OR-ing `UNICODE` in once an encoding flag is set - the property escapes get encoding tag 0, and `(?L)a`, `(?L)\w`, `(?L)[[:alpha:]]`, `(?L)\b`, `(?L).`, `(?L)\d`, `(?L)[a-z]` and `(?L)\p{L}` all compile to exactly what they compile to with no flag. The only locale-sensitive call the parser makes is `is_cased_i`, and it is reached only from `Sequence._flush_characters`, which a one-item sequence never becomes (`make_sequence`) - so `(?Li)a` compiles here as upstream does and only `(?Li)ab` hits the `needs:locale-flag` seam. `Character.folded` is not locale-sensitive either: it folds with the constant `FULL_CASE_FOLDING`, whose `UNICODE` bit wins over the pattern's flags. Ten measured patterns pinned by `Gaps/Parsing/LocaleFlagTests.cs`.
- 2026-08-30 **`standardise_name` tries to read every property and value name as a number, so its acceptance rule is Python's `float()` and not .NET's `double.TryParse`.** `float()` takes `inf`, `infinity` and `nan` in any case, and underscores between digits (`float("1_0")` is 10.0; `float("1_")` raises). Which branch a name takes is observable, because the fallback strips `_`, `-` and spaces and upper-cases while the numeric path rewrites the value: `1.5` becomes `3/2`, `1e5` becomes `100000`, `1/0` stays `1/0` (`ZeroDivisionError`, caught). One case escapes: `int(float("Infinity"))` raises `OverflowError`, which `standardise_name` does **not** catch, so `\p{Infinity}` propagates it out of `compile` rather than failing to parse. Ported as `OverflowException` with Python's own message. Thirty-four measured names pinned by `Gaps/Parsing/PropertyNameTests.cs`.
- 2026-08-30 **In `lookup_property`, "is there a qualifier?" means the *standardised* qualifier, not the raw one - so `\p{_:Lu}` has no qualifier.** Upstream standardises first (`_regex_core.py:1734`) and every later `if property` / `not property` tests the result (`:1743`), which `standardise_name` can make empty because it strips `_`, `-` and spaces. Getting the order wrong rejects `\p{_:Lu}`, `\p{-:Lu}`, `\p{ :Lu}`, `\p{_=Lu}` and `[[:_:alnum:]]`, all of which upstream compiles - and the POSIX branch specifically needs "no property" rather than "no qualifier written". The port collapses the empty string back to `null` so the rest of the function reads as upstream does. Found by the S10 blind review, measured against the oracle, pinned by twelve rows in `Gaps/Parsing/PropertyNameTests.cs`. Note also that a test of a value that standardises to itself proves nothing about the numeric path: `1/2` is a fixed point of the fallback, so `\p{nv=1/2}` still compiles with `numeric_to_rational` deleted; `\p{nv=1.5}` and `\p{nv=1e-1}` are the rows with teeth.
- 2026-08-30 **The set-member sort PORTMAP has promised since S06 lands as `RegexBase.RenderKey`.** Upstream turns a Python `set` of nodes into a list at two points, and a node's hash starts with its class object, whose hash is its address - so upstream's own bytecode varies per process. `RenderKey` is upstream's `_key` with the class *name* in place of the class *object*, recursing into nested nodes, which is exactly what `_render_key` in `tools/record-compile-corpus.py` computes; the default is the class name alone, matching upstream's default `_key`. Every rendered key is ASCII, so `OrderBy(..., StringComparer.Ordinal)` is Python's `sorted`.
- 2026-08-30 **S10 was verified by a purpose-built differential wave, not only by the corpus.** The corpus holds what upstream's own suite compiles, which is thin on set operators, POSIX classes and case folding. The wave is `.scratch/s10_diff_record.py`: every one of the 104 characters that expand on folding in six contexts each, the Turkic and Cherokee characters, ranges spanning the expanding characters, 600 fuzzed set expressions, and - added after the blind review found the empty-qualifier defect - every combination of 16 property qualifiers with 9 values in both `:` and `=` form. Re-run on 2026-08-30 after the fix: `2897 patterns x 16 flag sets: 32452 compiled, 13884 rejected, 31188 distinct rows recorded`. The rows were compared against upstream bit for bit - `code`, resolved flags, `req_offset`, `req_chars`, `req_flags` and `group_count` - giving `S10DIFF examined=31156 skipped=32 failures=0`. **Zero divergences.** A negative control (three rows corrupted in the fixture) reported exactly three. On the rejected patterns, `S10DIFFERR examined=13884 skipped=0 failures=410`: every message, exception class and offset agreed except the 410 where **upstream itself** raises a Python internal error (`AttributeError`, `TypeError`, `KeyError`), all four classes of which are recorded in PORTMAP's "Where we diverge". The pre-fix wave was narrower (1,705 patterns, 24,884 rows, 1,212 rejects) and found the same 410. The comparison harness is scratch and was deleted; its findings are permanent tests.
- 2026-08-31 **Python's `int()` on a group name accepts any Unicode decimal digit, and the port now has one implementation of it serving all five call sites.** Upstream calls `int(name)` in `parse_name`, `Info.is_open_group` and the `fix_groups` of `RefGroup`, `CallGroup` and `Conditional`. `BigInteger.Parse(name, InvariantCulture)` takes only ASCII digits, so `(x)(?P=١)` - which upstream compiles to `REF_GROUP 1`, measured 2026-08-30 - threw `FormatException`. `Unicode.PythonStr.TryParseInt` replaces it everywhere. Two things it is deliberately not: it is not `str.isdigit`, which is `Numeric_Type` of `Decimal` **or** `Digit`, so `(x)(?P=²)` reaches `int("²")` and upstream lets an uncaught `ValueError` escape - the port throws `NotSupportedException`, the same rule as the three patterns in `UpstreamInternalErrorTests`; and it is narrower than Python on signs, surrounding whitespace and underscores, none of which can reach it because a name is either `get_while(DIGITS)` or a string `str.isdigit` has already accepted. Found by the S11 blind review.
- 2026-08-31 **The decimal *value* of a digit is derived from upstream's tables, not read from .NET's, because mixing the two made the port throw where upstream compiles.** Upstream's tables carry `Numeric_Type` but no numeric value, so the first fix used `CharUnicodeInfo.GetDecimalDigitValue`. That table is an older Unicode version: the ten codepoints U+11DE0-U+11DE9 that Unicode 17.0 added are `Numeric_Type=Decimal` to the `isdigit` gate and unknown to the value lookup, so `(x)\g<𑷡>` raised instead of compiling. `PythonStr.DecimalValue` now takes the codepoint's offset within its own maximal run of decimal digits, modulo ten - the modulo is load-bearing, because runs abut and U+1D7CE-U+1D7FF is one 50-long run of five aligned blocks - and is verified against `CharUnicodeInfo` for every codepoint that table knows. The remaining divergence is the intended one: our tables are 17.0.0 and the host CPython's `unicodedata` is 16.0.0, which is the same choice already made for `\N{...}`. Found by the second S11 review pass, which is the rule about giving unreviewed fixes their own first pass earning its keep.
- 2026-08-31 **`Conditional.Equals` must compare the resolved group number only, never the text the pattern wrote.** Upstream's `__eq__` compares `self.group`, which `fix_groups` has by then replaced with the `int`; the text is gone. Comparing both made `(?(1)a|b)` and `(?(one)a|b)` unequal, so `Branch.optimise` did not hoist them as a common prefix and emitted a `BRANCH` with the conditional duplicated - a real bytecode divergence on `(?<one>x)(?:(?(1)a|b)c|(?(one)a|b)d)`, found by the S11 blind review and pinned by `RelativeCallAndAliasTests`.
- 2026-08-31 **Two analyzer families fired on S11's new nodes and both were obeyed on the merits; nothing was disapplied.** SS008 and S2328 flag a `GetHashCode` over a mutable member: `CallGroup`, `RefGroup` and `Conditional` resolve their group number in `fix_groups`, so hashing it would let a node's hash change while it sat in a set. They hash their immutable part instead - the type, plus case flags where there are any - which is the rule `Branch` already followed and which still keeps the only promise a hash makes. S3877 flags a `throw` from `Equals` or `GetHashCode`: `CallRef` and `GraphemeBoundary` have no `_key` upstream and would raise `AttributeError`, but throwing there breaks any collection or debugger that touches the node and buys nothing, since nothing compares either. Both use reference equality, and their `MaxWidth` still throws - that one would be a real bug if it were ever reached. Recorded in PORTMAP's "Where we diverge".
- 2026-08-31 **A bare `\N` in a replacement template is an error, not the literal `N`, and its name alphabet is narrower than the pattern side's.** `parse_repl_named_char` (`_regex_core.py:1885-1900`) rewinds and returns `None` exactly as `parse_named_char` does, but `_compile_replacement` then falls through to `raise error("bad escape \N")` where `parse_escape` would have made a literal. Its `get_while` set is `ALPHA | {" "}`, not the pattern side's `NAMED_CHAR_PART` (`ALNUM | {" ", "-"}`), so a digit or a hyphen ends the name and `\N{1}` and `\N{LATIN-SMALL-LETTER-A}` are errors too. Measured 2026-08-31 against `regex` 2026.7.19 - all four give `bad escape \N at position 2` - and pinned by `Gaps/Parsing/ReplacementTemplateTests.cs`. Reading the two functions as the same shape would have compiled three patterns upstream rejects.
- 2026-08-31 **`parse_repl_hex_escape` has no range check where `parse_hex_escape` has one**, so `\UFFFFFFFF` is a well-formed template escape that fails later in `chr()` with an uncaught `ValueError`. The ported items are therefore `long`, not `int` - 8 hex digits do not fit - and `PatternCompiler.MakeString` throws `NotSupportedException` above U+10FFFF, the same rule already used for the patterns in `UpstreamInternalErrorTests`. Throwing early in `ParseReplHexEscape` instead would have been observably different only for a template holding both an out-of-range escape and a later syntax error, but `long` costs nothing and keeps upstream's order.
- 2026-08-31 **Which replacement-template errors can be tested before the engine exists is a measurement, not a reading.** Upstream compiles the template before it starts matching, so a malformed one is rejected whether or not the pattern matches - but a *group-number* check happens during expansion and needs a match. Measured 2026-08-31 with a subject that cannot match: all seven `\g<...>` syntax errors and the trailing backslash raise, while `\1`, `\8`, `\11`, `\118`, `\800` and `\1a` all return the subject unchanged. So S12 un-skipped 8 `needs:parse-errors` attributes by prose and left `SubTemplateNumericEscapeTests`' 12 invalid-group-reference rows for Phase 3.
- 2026-08-31 **Upstream's `IndexError("unknown group")` for a `\g<name>` the pattern has not got becomes `ArgumentException`, not `FuzzyRegexParseException`.** Upstream deliberately declines to use its own error type there, because an unknown group name in a *template* is a caller's argument being wrong rather than pattern syntax, and `System.Text.RegularExpressions` raises `ArgumentException` for the same mistake - which is also what the Phase 1 `SymbolicRefsTests` port already asserted. CA2208, S3928 and MA0015 all require the `paramName` to name a parameter of the throwing method; `CompileReplGroup` is three frames below the public surface and has none, so the honest name is `"replacement"` and all three rules are disapplied **at that one line** with the reason in a comment. Not in `.editorconfig`: the per-directory block would have turned them off for the whole parser, and they are right everywhere else.
- 2026-08-31 **`Match.Result` is not wired to the template compiler, and that is S12's one undelivered scope item.** `Match` is a stub with no fields and nothing constructs it, so there is no pattern to compile a template against; wiring it would mean inventing `Match`'s internals, which is Phase 3's design work. `FuzzyRegex.Replace`, `Replace(..., out int)` and the static `Replace` do compile ahead of matching.
- 2026-08-31 **`FuzzyRegex.Escape` walks whole codepoints, written out rather than through `EnumerateRunes`.** Upstream iterates a Python `str`, so a character above U+FFFF takes one backslash and not one per surrogate - measured: `escape('\U0001F600', special_only=False)` is one backslash and one emoji. `EnumerateRunes` would be the one-line stdlib answer but replaces a lone surrogate with U+FFFD, and a lone surrogate is as legal in a .NET string as it is in a Python `str`: `escape('a\ud800b', special_only=False)` keeps it. Verified over **all 1,114,112 codepoints x 4 flag combinations**, both as a per-codepoint decision bitmap (0 differences) and as a SHA-256 of the concatenated output (four digests equal). The hash needed hand-rolled UTF-16-LE bytes: `Encoding.Unicode.GetBytes` replaces a lone surrogate with U+FFFD where Python's `surrogatepass` does not, which showed up first as four unexplained mismatches.
- 2026-08-31 **A `\uXXXX` escape is decoded before it reaches disk through the Bash tool's heredoc as well as through Write and Edit** (S03 and S05 recorded the Write/Edit path). A literal U+2028 landed in a C# comment that way and failed the build with `CS1010: Newline in constant`, which is DECISIONS 2026-08-30's line-terminator hazard arriving by a new route. The reliable form is to build the text with `chr(0x5c) + 'u2028'` inside the script, so no `\u` sequence appears in the agent's own output at all.
- 2026-08-31 **A fuzzy cost limit is the one number upstream range-checks nowhere, so the port's `uint` code word may only be clamped at the moment it is emitted.** `is_above_limit` (`_regex_core.py:623`) guards repeat counts; `parse_cost_limit` and `parse_cost_term` call a bare unbounded `int()` and check nothing, and the callers then do `max_cost -= 1`, `min_cost += 1` and `not 0 <= min_cost <= max_cost` on it. Two ceilings were tried and both were observable as patterns upstream compiles and this port rejected. At `UNLIMITED`: `{i<4294967296}` subtracted from the ceiling and capped at 4294967294, and `{4294967296<=i<=4294967295}` compiled. At `long.MaxValue`: `{9223372036854775807<i<=9223372036854775808}` overflowed `min_cost += 1` to a negative minimum, and `{9223372036854775807<=i<9223372036854775808}` collapsed two distinct values into one - all three accepted by `regex` 2026.7.19, measured 2026-08-31. The parse-time values are therefore `BigInteger`, and `Fuzzy.CodeWord` is the only clamp. The residual divergence is one code word, which upstream's own `RE_CODE` (an `RE_UINT32`, `_regex.c:58`) truncates identically. Both ceilings were found by S13's two blind review passes; the first was in the code the first pass reviewed, the second was in the fix, which is why the fix got its own pass.
- 2026-08-31 **`isinstance(parsed, Fuzzy)` is asked in two places, and wiring only one appends a spurious copy of the whole pattern.** `_main.py:577` feeds the whole-pattern `CALL_REF` wrapper; `_regex_core.py:4436`, inside `_check_group_features`, decides whether a call to group 0 needs an *additional* copy compiled with different features. With the second left at S08's `false` placeholder, `(?:a(?0)?){e<=1}` compiled to upstream's 25 words followed by a 26-word `CALL_REF` copy of itself. Note the two are deliberately asked of different trees - `_main` of the unoptimised parse, `_check_group_features` of the optimised one - and upstream lets the answers differ, so that is not a bug to fix. Found by the S13 blind review; pinned by `Gaps/Parsing/FuzzySectionTests.cs` with a non-fuzzy control.
- 2026-08-31 **A backreference inside a fuzzy test is rejected, because upstream rejects it and this port would otherwise have silently matched group 0.** `Fuzzy.fix_groups` (`_regex_core.py:2822`) descends into `subpattern` and not into `constraints["test"]`, so upstream's `RefGroup.group` stays the string `'1'`, goes into the code list as a string, and `_regex.compile` answers `RuntimeError: invalid RE code` - measured for both `(a)(?:abc){e<=1:\1}` and the `\g<1>` form. Our `RefGroup.GroupNumber` defaults to 0, a perfectly valid code word, so before the guard the pattern compiled and would have matched the whole subject once the engine landed. `RefGroup.CompileCore` now throws `NotSupportedException` when `GroupNumber` is still 0, which `FixGroups` guarantees means "never fixed". Same rule as `UpstreamInternalErrorTests`: where upstream lets an internal error escape, what matters is that the input is rejected rather than quietly compiled.
- 2026-08-31 **`StringSet._key` is dead upstream, so named-list equality is over the branches.** `StringSet.__init__` sets `_key = (class, name, case_flags)` and then inherits `Branch.__eq__` (`_regex_core.py:2520-2521`), which compares `self.branches` - so two differently-named lists holding the same strings compare equal. The port matches by not overriding `Equals` or `GetHashCode` on `StringSet` at all; `Branch.Equals` gained the `GetType()` test that is upstream's `type(self) is type(other)`, previously unobservable because `Branch` had no subclass. `RenderKey` still renders `_key`, because that stands for `_key` and not for equality.
- 2026-08-31 **A differential wave must sort each named list before handing it to upstream, or it invents divergences.** `tools/record-compile-corpus.py`'s `_canonical_kwargs` replaces every named list with a sorted one, because `StringSet.__init__` sorts its branches by length only - a *stable* sort - so the caller's container order leaks into the bytecode. The recorded rows are therefore upstream's answer for the *sorted* list, and a harness that passes the unsorted list is comparing two different questions: 58 of the S13 wave's first 62 divergences were exactly that. The remaining 4 were the one real finding.
- 2026-08-31 **Five patterns compile here and are rejected by upstream's C compiler, and that is deliberately left for Phase 3.** `{e<=1:\X}`, `{e<=1:\b}`, `{e<=1:\A}`, `{e<=1:\Z}` and `{e<=1:\L<a>}` produce this port's bytecode word for word identical to upstream's Python-level code list; `_regex.compile` then answers `RuntimeError: invalid RE code`. The validator is `re_compile` (`_regex.c:25863-26121`), which is Phase 3's to port, and adding a second one to `PatternCompiler` would put it in the wrong layer. Recorded in PORTMAP's "Where we diverge" with the instruction that the Phase 3 slice must close it; `.scratch/s13_record.py` is the ready-made check. Contrast the backreference case above, which could not be deferred because a `uint` code list cannot hold upstream's value at all.
- 2026-08-31 **`needs:fuzzy-syntax` was retired as a capability tag and its 50 remaining tests moved to `needs:fuzzy-matching`,** which the status board shows going from 48 to 98. The tag named a parser capability that S13 delivered, so leaving it on the status board would have kept a finished capability at the top of the "what to build next" table for the rest of the port. The tests it covered all call `FullMatch` or `MatchAtStart` and wait on the `FUZZY`/`FUZZY_EXT` opcodes, which is what `fuzzy-matching` means. `needs:named-lists` was *not* retired - there is no separate tag for named-list matching, so its prose was corrected instead. Both changes are visible in `docs/STATUS.md`; reverse them if the owner would rather the board tracked parser and engine capabilities separately.
- 2026-08-31 **The ratchet prints a raw passing count against a deduplicated baseline, and the two legitimately differ.** S13 ends `passing: 2046  baseline: 2040`, which looks like an un-updated baseline and is not: the baseline stores unique ids (`Sort-Object -Unique`) while the printed count is one per result. Measured on S13's TRX: 107 ids carry more than one result (101 all-skipped, 6 all-passed), and **zero have mixed outcomes**. The 6 passing pairs are `VariousParseErrorTests.Invalid_pattern_is_rejected` for `(abc`, `a\`, `()`, `abc)`, `a[]b` and `a[`, which upstream's suite genuinely lists twice. A set comparison of the TRX against the baseline is an exact match both ways, so nothing is unwatched today. The latent risk is real but small: the ratchet keys on the id, so if two rows sharing an id ever diverge it would watch only one. Left alone rather than fixed - deduplicating ported rows or widening the id is a change to the merge gate and belongs to a slice, not to a 01:00 tidy-up.
- 2026-08-31 **The local differential oracle may be PyPI regex 2026.7.19; CI's must be the pinned 2026.8.12 built from `upstream/`.** Upstream never published 2026.8.12 to PyPI, so `pip install regex` gives 2026.7.19, and the changelog delta between the two is the single line "Support Python 3.15." (`upstream/changelog.txt`) - behaviourally empty. The S14 recorder therefore accepts exactly those two versions, records which one produced each run, and fails on anything else; oracle.yml keeps asserting the exact pinned version because there it is free to build from source. Owner-approved 2026-08-31.
- 2026-08-31 **The span convention is enforced in exactly two places, and nowhere else.** Engine internals mirror upstream: positions and group spans are `(start, end)` in UTF-16 code units, iterated by codepoint. Conversion to the public `(Index, Length)` happens only in `Match`/`Group`'s accessors (S16/S18), and the S14 oracle recorder independently converts Python's codepoint `(start, end)` to expected UTF-16 `(Index, Length)` - so a slip at the accessor boundary shows as an oracle divergence rather than a silent agreement. A third conversion site anywhere in the engine is a defect by definition (DECISIONS 2026-08-30 has the convention itself).
- 2026-08-31 **The search prefilters are deferred to Phase 7: `locate_required_string` (`_regex.c:11082`), the `string_search`/`fast_string_search` family (`:5231-6918`) and the `try_match` test-node fast path (`:6919-7686`).** All are semantically transparent - they skip positions that cannot match - so deferring them changes speed, never results, and Phase 7 is the optimisation phase. The one risk is a ported test timing out without them; that contingency is named in S19 and checked at the S26 phase close, and the sanctioned response is to port the needed prefilter then, not to skip the test. `set_test_nodes` itself is still ported in S15 so the node graph keeps upstream's shape. Owner-approved 2026-08-31.
- 2026-08-31 **On a row upstream rejected, the oracle tells "this port rejected it too" from "this port crashed on it" by which call threw, not by the exception type or its message.** The types overlap - an `ArgumentOutOfRangeException` is both a deliberate rejection from `RegexFlags.DefaultFlags` and the shape an index slip in the engine takes - so `OracleComparer.Run` catches the `FuzzyRegex` constructor and the matching call separately and marks the latter `WhileMatching`, which `Compare` treats as a divergence. Comparing the *message* instead was tried and is wrong: .NET decorates `ArgumentException.Message` with the parameter name and the actual value, while upstream's `regex.compile('a', regex.V0|regex.V1)` raises `KeyError: regex.V0|V1` (both measured 2026-08-31), so a correct port scored a false divergence. The message is still compared for `regex.error`, whose text this port carries verbatim, exactly as `CompileParityTests` does. Found by S14's second and third blind passes - the first raised the swallowed crash, the second the false divergence the fix for it introduced.
- 2026-08-31 **"This port compiled the pattern and cannot match yet" is a different oracle verdict from "this port knows nothing", and conflating them would have blinded the harness for the whole of phase 3.** A `NotImplementedException` from the matcher used to report `unsupported`, but by then the port has already answered whether the input is acceptable: on a row upstream *rejected*, having compiled it is the divergence, and it is decided before any matcher is consulted. `OracleComparer.Run` therefore returns `CompiledButUnmatched` rather than `null` once construction succeeds, and `Compare` calls that a divergence against a recorded error and `unsupported` against a recorded match. This is what lets the oracle see the five patterns S13 left compiling here and rejected by upstream's `re_compile` - `(?:abc){e<=1:\b}` records `RuntimeError: invalid RE code`, measured 2026-08-31, and is pinned as `A_pattern_upstream_rejected_and_this_port_compiled_is_a_divergence`. Consequence for S15: any generator that emits fuzzy patterns before `re_compile` lands will legitimately turn the wave red.
- 2026-08-31 **`re_compile`'s rejection is `NotSupportedException("invalid RE code")`, not `FuzzyRegexParseException`.** Upstream's node compiler answers `RuntimeError: invalid RE code` (`set_error(RE_ERROR_ILLEGAL)`, `_regex.c:2117-2119`), not its own `error`, so a Python caller writing `except regex.error` does not catch it either. This port keeps one bright line - `FuzzyRegexParseException` means and only means upstream's `error`, which is what `CompileParityTests` asserts row by row - and follows the rule `Gaps/Parsing/UpstreamInternalErrorTests` already set for every upstream rejection with a non-`error` type. The alternative, mapping it to `FuzzyRegexParseException` so that one `catch` covers every bad pattern, was rejected: it would put a rejection upstream deliberately typed differently under the one type the corpus pins, and the wart it removes already exists for the three patterns in `UpstreamInternalErrorTests`. Message is upstream's, verbatim.
- 2026-08-31 **The engine-only operators (`RE_OP_BODY_END` .. `RE_OP_TAIL_START`, 81-97) live in `Parsing.Opcode`, not in a second enum.** `_regex.h:19-117` continues the parser's own numbering past `FUZZY_EXT` with seventeen operators the C compiler invents while building the graph; upstream keeps one numbering across both files and keys its `switch` on it. A separate `Engine` enum would duplicate all 81 shared names and add a cast at every node the builders create, for no check the single enum does not already give.
- 2026-08-31 **The node graph is built in the `FuzzyRegex` constructor and thrown away until S16.** Upstream's `_compile` hands its code list straight to `_regex.compile`, so the node compiler is the last thing that can reject a pattern; building at first match instead would move that rejection somewhere no caller expects it. Nothing reads the graph yet, so it is not stored - IDE0052 is right that a field nothing reads is dead. S16 is the slice that keeps it.
- 2026-08-31 **`csharp_indent_case_contents_when_block = false`.** The .NET formatter indents a braced block after a `case` label one level further than CSharpier does, and IDE0055 is an error here, which made a `case X: { ... }` unwritable - and the `Engine/` port needs one wherever upstream's C case declares a local. CSharpier is the repository's formatter of record (pre-commit hook and CI), so the .NET formatter is configured to agree with it rather than the other way round. Not an analyzer suppression: the rule still fires on every other formatting difference.
- 2026-08-31 **`basic_match` keeps upstream's shape: one dispatch switch, one backtrack switch, and `goto` at upstream's labels.** The alternative shapes - recursion, or a table of per-opcode strategy objects - were rejected before any code was written. `basic_match` (`_regex.c:11714-17403`) is where most upstream fixes land, and it is a backtracking machine whose two loops jump into each other and back to the search restart; expressing that some other way means re-deriving the translation at every sync instead of diffing it (AGENTS.md, design spec section 4). Consequence: Sonar's S907 ("remove this use of `goto`") is disapplied for `src/FuzzyRegex/Engine/**` and nowhere else, with the reason written at the `.editorconfig` entry. `Parsing/` and `Unicode/` contain no `goto` and must not start containing one.
- 2026-08-31 **`RE_State` is ported as a class with internal fields, so the CA1051/S1104/MA0008 note in `.editorconfig` is still untriggered after S16.** The note was written for a public-field struct on the assumption that the engine would need one. It does not: every helper in `basic_match` mutates the state, so a struct would put `ref` on each signature to say what a reference type says for free, and nothing in `Engine/` is public, which is what all three rules key on. Measured on this slice's build - none of the three fires. The note stays in place, still untested, for the slice that genuinely lands a public-field struct; forcing one now to discharge a plan item would be the tail wagging the dog. S16 also did not port `push_pointer`, which is where a struct-shaped question will actually arrive (S18).
- 2026-08-31 **A zero-width `CHARACTER` node steps by 0, and the port must honour `node->step` rather than assume 1.** `optimise_pattern` hoists the first character test of a pattern that begins with an anchor in *front* of that anchor, as a `CHARACTER` node carrying `RE_ZEROWIDTH_OP` and `step == 0`, so `'^a'` compiles to `CHARACTER(step 0) - START_OF_STRING - CHARACTER(step 1)`. S16's first draft advanced one codepoint for any `CHARACTER`, which left the anchor testing the wrong position, and **every pattern with a leading anchor failed to match** - 41 rows of a 2,400-row wave. Found by the differential oracle, not by the ported suite, which had no un-skipped test for a leading anchor. Pinned by `Gaps/Engine/MatchSpineTests`. **`RANGE` and the `SET_*` family carry the same flag** (`NodeCompiler.BuildRange`, `BuildSet`), so S17 must read `node.Step` too; `ANY` and `STRING` do not, and upstream increments them unconditionally.
- 2026-08-31 **`RE_ERROR_MEMORY` from the backtracking stack is an `InvalidOperationException`, not an out-of-memory exception.** Upstream's 1GB `RE_MEMORY_LIMIT` is kept - an unbounded backtracking stack is a denial-of-service vector and the guard is the defence - but the .NET runtime reserves both `OutOfMemoryException` and `InsufficientMemoryException` for real allocation failures (CA2201, S112 and MA0012 all say so), and a library that throws one makes a genuine failure indistinguishable from this limit. Unreachable in S16, where the stack never holds more than a few bytes; S19 is the first slice that can push enough to reach it.
- 2026-08-31 **A matching timeout surfaces as `System.Text.RegularExpressions.RegexMatchTimeoutException`, the built-in type.** Upstream raises `TimeoutError` (`_regex.c:2150`), which has no .NET counterpart, and this port's public surface is shaped after `Regex` throughout; the built-in exception means exactly this, already carries the input, the pattern and the timeout, and a caller who already handles `Regex` timeouts needs no new `catch`. Thrown from `FuzzyRegex.Run` rather than from the engine, because that is where the pattern text lives; `Matcher.DoMatch` returns `MatchStatus.Cancelled` and lets the caller raise. The plumbing is pinned by `MatchSpineTests`, but nothing can actually time out until S19 lands quantifiers - a pattern the S16 spine can run always terminates.
- 2026-08-31 **`csharp_indent_labels = no_change`**, the label twin of the `csharp_indent_case_contents_when_block` line above and settled the same way. The .NET formatter's default (`one_less_than_current`) outdents a `goto` label; CSharpier leaves it at the surrounding indent; IDE0055 is an error here, so a label formatted by one tool failed the other. S16 hit it the hard way: the pre-commit CSharpier hook reformatted `Engine/Matcher.cs`'s three labels *during* the commit, and the resulting tree - already committed, and green when it was verified a minute earlier - failed to build. CSharpier is the formatter of record (pre-commit hook and CI), so the .NET formatter is configured to agree with it rather than the other way round. The general lesson is worth more than the setting: **a formatter that runs inside the commit hook can turn a verified working tree into a broken commit, so re-run the ratchet after committing, not only before.**
- 2026-08-31 **Native AOT compatibility is a shipped requirement, enforced statically from now and dynamically in phase 6** (spec amendment 12). `<IsAotCompatible>true</IsAotCompatible>` goes on `src/FuzzyRegex` immediately, not at packaging time: on net8+ it enables the trim, AOT and single-file analyzers, and `TreatWarningsAsErrors` is already true (`Directory.Build.props:12`), so the slice that introduces a hazard fails its own build rather than handing phase 8 a retrofit across a finished engine. It costs nothing to set today - measured 2026-08-31, `src/` contains no reflection at all: every `System.Reflection` hit was in generated `obj/` assembly-info or a compiled binary, and the only `System.Type` use is `typeof(...)`/`GetType()` in `Equals` and `GetHashCode`, which is statically known and trim-safe. Static analysis is necessary and not sufficient, so phase 6 also publishes a small consumer app with `PublishAot=true` in CI and asserts real matches; that is the test that proves the claim. The reason this could not wait for phase 8 is phase 7: the classic .NET regex optimisation is `RegexOptions.Compiled`, which emits IL at run time via `Reflection.Emit`/`DynamicMethod` and has no JIT to emit into under native AOT, so phase 7 must plan around the constraint instead of discovering it after building the fast path.
- 2026-08-31 **Phase 6 closes against four named criteria, not a coverage percentage** (spec amendment 12). In descending order of value: (1) skips remaining in the ported suite - 1,880 of 5,492 as of today, about 34%, each with a machine-readable `needs:` reason that `tools/PortTools.psm1` aggregates into `docs/STATUS.md`, which makes it an enumerated per-capability list of unported upstream behaviour and a better instrument than any coverage tool for a port; (2) oracle waves across every generator with zero divergences, the only source of real ground truth and the only one that finds behaviour never tested at all - S16's leading-anchor bug was found by a wave and by no ported test; (3) mutation testing scoped to the public API layer and the parse-error paths, the two places the oracle cannot reach, and the only instrument that answers "would a regression actually fail a test?"; (4) line coverage last and as a backstop only, to find a file or branch with no test at all, never as a target. The gate exists because "sweep for coverage gaps" with no criteria produces a number nobody acts on. Phase 6 additionally pins what phase 7 regresses against, since optimisation is where silent behaviour change is likeliest: the benchmark baselines from `.claude/skills/benchmark/SKILL.md` (specified there, nothing committed under `bench/baselines/` yet) and the edge cases an optimiser is tempted to special-case - zero-width and empty matches, anchors, `MatchTimeout`, large inputs, pathological backtracking.
- 2026-08-31 **The browser demo is phase 9, after 1.0, and it runs the engine in a Web Worker - the worker is the safety design, not a performance choice** (spec amendment 12). A public demo invites strangers to type pathological patterns, regex matching is unbounded in the worst case, and this is a fuzzy engine, so approximate matching is combinatorially worse than the exact case. A `MatchTimeout` alone does not bound the freeze: it only fires if the engine polls the deadline in its inner loop, so one missed check point turns a bounded stutter into a frozen tab, and the guarantee then rests on the correctness of the very engine the demo exists to show off. In a worker it rests on the browser's scheduler instead - a runaway is killed with `worker.terminate()`, a warm spare is pre-spawned so the respawn is not felt, and `MatchTimeout` stays as the fast common-case exit and defence in depth rather than the sole net. No cross-origin isolation is needed: the worker boots a second, independent .NET WebAssembly runtime and talks to the page over `postMessage`, while COOP/COEP are required only by `WasmEnableThreads`, a different mechanism this design does not use - worth recording because GitHub Pages cannot set custom response headers at all, so the distinction is what keeps someone from later concluding the whole shape is impossible on Pages. Blazor was the first recommendation and was dropped once the UI no longer had to be .NET; it brings the Components assemblies and boot machinery for no benefit at this size. Vue 3 over SolidJS on owner familiarity: the expensive work is all behind `postMessage`, so fine-grained reactivity would optimise the part that was never the bottleneck, and the UI is three inputs, a result pane and an examples list. Verified 2026-08-31 against current Microsoft documentation and `dotnet/blazor-samples`.
- 2026-08-31 **`ENCODING_KIND` is live, and grepping the C alone says the opposite.** The macro (`_regex.c:167`) reads bits 16-17 of `node->status`, which are also `RE_STATUS_HAS_GROUPS` and `RE_STATUS_HAS_REPEATS`, and nothing anywhere in `_regex.c` writes them - the only two hits for `RE_ENCODING_SHIFT` are the `#define` and the macro itself. The tempting conclusion, that the switch is dead and always takes its `default` arm, is wrong: the bits come from the Python compiler, where `Property._compile` ORs `self.encoding << ENCODING_OP_SHIFT` (5) into the code word's flags and `create_node` then shifts the whole flags word by `RE_STATUS_SHIFT` (11), landing it on 16-17. Proved by intercepting `_regex.compile`: plain `\d` emits node flags `1`, `(?a:\d)` emits `33` (0x21) and `(?u:\d)` emits `65` (0x41). This is the whole of the scoped `(?a:...)`/`(?u:...)` flag at match time, and skipping it made `(?a:\d)` match U+FF19 FULLWIDTH DIGIT NINE. Ported as `NodeStatus.EncodingKind` and `Matcher.NodeEncoding`, pinned by `Gaps/Engine/ScopedEncodingTests`. It is safe only because no node carries both meanings - a future slice that sets `HAS_GROUPS` on a `PROPERTY` node would break the ASCII flag silently. **The general lesson: a claim about upstream's behaviour is a hypothesis until it is run.**
- 2026-08-31 **S4144 is disapplied for `tests/FuzzyRegex.Tests/Ported/**` and nowhere else.** Splitting a ported table test by the capability each row waits on necessarily leaves several methods with identical bodies: upstream drives its whole 524-row `test_various` table through one loop and one assertion, so the body is shared by construction and what distinguishes two methods here is their `[Arguments]` data and their `[Skip]` tag. Merging them back to satisfy the rule would delete the per-capability gap information `docs/STATUS.md` is generated from, and extracting the body into a helper does not satisfy it either - the callers are then identical one-liners. Scoped to `Ported/` rather than all of `tests/` because the reason is only true there. First needed at S17, splitting `test_various` by `groups` and `quantifiers`.
- 2026-08-31 **Un-skipping a slice's tags is triage, not a search-and-replace.** S17 removed 84 `[Skip]` attributes for its five tags; 300 tests then failed for the right reason, and after the engine landed **156 still failed** - none on an assertion, every one at another slice's seam. They were retagged `find-all` (132), `groups` (57), `quantifiers` (~40) and `ignore-case` (~9), and six fan-out methods had to be *split* because only some of their rows were blocked. Two consequences worth keeping: a slice should budget for the retagging, and it should diff every `[Arguments]` multiset against `git show HEAD:<path>` afterwards, because a split is exactly where test data gets altered without anyone noticing.
- 2026-08-31 **`Seam.Tag` is part of the deliverable, not decoration.** After S17 landed `PROPERTY`, `RANGE` and the `SET_*` family, the tag table still routed their `_IGN` variants to `needs:unicode-properties` and `needs:character-classes` - capabilities the status board now reports as delivered - so `(?i)\p{Cyrillic}` threw a tag that exists nowhere in the test suite and would have appeared on an oracle `unsupported` row. Found by the blind review. **Every slice that delivers a tag must re-home the opcodes that keep throwing it.**
- 2026-08-31 **Phase 6 gains a sweep of upstream's open issues - triage, reproduce as a test, fix here, report there - because the differential oracle is blind to an inherited bug by construction** (spec amendment 13). The oracle compares this port against upstream, so a bug we reproduce faithfully produces *agreement*, which is what it reports as success; a divergence shows up only where we accidentally failed to reproduce one. More waves cannot help, because the blindness is in what the comparison means, not in how much of it runs. It goes before phase 7 so that optimisation is not built on behaviour already known to be wrong. Sized from a real triage, not a guess: all 79 open issues on `mrabarnett/mrab-regex` were classified with `gh` on 2026-08-31 - 40 not bugs, 24 Python-specific and impossible in a C# port, 12 real engine bugs we would inherit, 3 undecidable without a reproduction attempt - and the 12 fall in four groups: memory-safety (611-614), fuzzy and BESTMATCH (470, 563, 564, 596), resource blowups (551, 554) and two singletons (367 partial matching, 425 branch reset). 3-5 sessions, an estimate whose basis is that count and nothing else, since the per-issue reproduce-fix-report cost is unmeasured; phase 6 therefore reads 6-10. The memory-safety four are heap-buffer-overflow reads and writes in C and cannot manifest as corruption here - they arrive as an `IndexOutOfRangeException` or a wrong answer - but the logic errors behind them are inherited, and several are compiler-side in code S15 and S16 have already ported, so one may surface early. Fixing a bug upstream still has creates a permanent oracle divergence, so the sweep brings an intentional-divergence allowlist: a version-controlled manifest carrying row identity, our result, upstream's result, a reason linking the upstream issue and this entry, and the upstream commit it was recorded against - the pattern web-platform-tests, test262 and pytest `xfail` all use, and stricter than PyPy's prose-only `cpython_differences`. Strictness is the anti-rot ingredient: a listed divergence that stops diverging must fail the run, as `xfail_strict` does and as Chromium's TestExpectations, documented as accumulating stale entries, does not. Same shape as `tests/parity-baseline.json` plus `tools/check-ratchet.ps1`, so it follows that convention rather than inventing one. Upstream engagement, verified the same day: the maintainer merges external PRs (7 of the last 20 closed, Feb-May 2026), no CLA, no CONTRIBUTING.md, and crisp reports land within hours (#607/#608 fixed the same evening) while design-trade-off arguments stall - so a report carries a minimal runnable repro with the version pinned, names the faulting function, distinguishes the bug from a similar one already fixed, and proposes a fix. Nothing is filed without the owner approving the text, and `gh` stays out of the driver allowlist in `tools/run-slices.ps1` so an unattended session cannot post. Licence checked, no action: `NOTICE` already declares the derivative work at the pinned commit with upstream's CNRI/Secret Labs statement and the UCD credit, satisfying Apache 2.0 4(b) and 4(c).
- 2026-08-31 **A node reference on the byte stack is an index into `PatternObject.NodeList`, not a reference.** Upstream's `push_pointer` (`_regex.c:2472`) pushes a raw `RE_Node*` into the untyped byte stack, and S18's `BRANCH` case is its first caller here. A managed reference cannot go in a `byte[]`, so `ByteStack.PushNode`/`PopNode` push `Node.Index` - a position in `PatternObject.NodeList`, which *is* upstream's `node_list`. That keeps the stack 8 bytes wide per pointer, so the byte layout still matches upstream's frame for frame, which the obvious alternative - a second, parallel `List<Node>` stack - would have broken, and with it the property that a future diff of `basic_match` maps mechanically onto this file. `Node.Index` is assigned in one loop at the **end** of `PatternObject.Compile`, because `Optimiser.DiscardUnusedNodes` removes unreachable nodes from the list and any index handed out earlier would be stale, and because the required-string node is appended after `CompileToNodes` returns.
- 2026-08-31 **The oracle wave now records `lastindex` and `lastgroup`, because nothing about the group spans implies them.** `regex.match('((a))', 'a').lastindex` is 1 although groups 1 and 2 both succeed with the same span - it is the group that *closed* last, from `group_info[g].end_index` - and `lastgroup` names the last *named* group even when an unnamed one succeeded later. So an engine that got only those two wrong would have agreed on every span of every row, and the port's `Match.LastGroupNumber`/`LastGroupName` were shipped by S18 with only three trivial ported tests behind them. Python's `None` maps to `-1` and to `null`, matching what the two members return. The negative control that justifies the change: ordering `lastindex` by group number instead of by which group closed last produced **195 divergences in 7,500 rows** and *zero* before the fields were compared. The gap was found by S18's blind review as a non-defect observation, which is the shape those observations should take - a hole in the instrument rather than a bug in the code.
- 2026-08-31 **`save_captures`/`restore_groups`/`discard_groups` have no caller until Phase 5, and `push_groups`/`push_captures` none until Phase 4 - so S18 ported none of them, against its own slice file.** The slice's stated reason for including them was that a repeated capture group exercises `save_captures`/`restore_groups`; it does not. Grepping `_regex.c` for call sites settles it in one step: `save_captures` is called only from `do_best_fuzzy_match` (`:17760`, `:17789`) and `do_enhanced_fuzzy_match` (`:17957`, `:18000`, `:18008`), both BESTMATCH/ENHANCEMATCH and therefore Phase 5; a repeat instead uses `push_captures`/`pop_captures`, whose only callers are `ATOMIC` (`:12040`), `CONDITIONAL` (`:12226`), `LOOKAROUND` (`:13774`), `GROUP_CALL` (`:13405`) and `GROUP_RETURN` (`:13500`) - all Phase 4. Porting them anyway would have shipped six pieces of engine code with no caller and therefore no test, which is the one thing a port of a backtracking matcher cannot afford. `PORTMAP.md` records every call site so the next slice does not re-derive this. **The general lesson is the same as `ENCODING_KIND`'s: a claim about upstream is a hypothesis until it is run - and for "who calls this?", the grep *is* running it.**
- 2026-08-31 **Do not rewrite a source file with Python's `Path.write_text` on Windows.** It opens in text mode with `newline=None`, which translates every `\n` to `\r\n`, and this repository's working tree is LF (`.gitattributes` sets `* text=auto eol=lf`). S18's un-skip pass rewrote 22 test files as CRLF, which changed every line of each of them and failed the build on IDE0055 in a file it had not otherwise touched. `write_bytes` on a UTF-8 encoded string is the fix. Worth recording because the symptom - a formatting error at a line the script never edited - points nowhere near the cause.
- 2026-08-31 **Upstream is exponential on `(a|a)*b` too, and the `try_match` test-node arm is not what saves it - `locate_required_string` is.** S19's guard gap test hung on `(a|a)*b`, and upstream had measured 0.00ms on the same pattern out to n=22, so the port looked to have a guard bug. It has not. The first measurement fed upstream `'a' * n + '!'`, a subject holding no `'b'`, and upstream's required-character prefilter (`locate_required_string`, `_regex.c:11082`) rejects such a subject before `basic_match` runs at all. Re-measured against `'a' * n + 'xb'`, which the prefilter cannot reject: upstream takes 0.14ms at n=8, 1.69ms at n=12, 27.8ms at n=16, 444ms at n=20 and 23.3 **seconds** at n=26. This port measures 0.81ms, 39.4ms and 534.6ms at n=8, 14 and 20 - the same curve, a small constant factor slower. Two consequences. First, **the Phase 7 prefilters are not only a speed matter**: they are the difference between an instant answer and an exponential search on a whole family of patterns, and `docs/PORTMAP.md` and `Matcher`'s class remarks now say so instead of calling them semantically transparent and leaving it there. Second, **`try_match`'s test-node arm (`:7671`) was ported in full and reverted in the same slice**: it was written to fix a blowup it turned out not to cause, and it measurably fixed nothing - `(a+)+b`, the one pattern that looked improved, was JIT warm-up noise on both sides. About a hundred lines of engine code and a behaviour change at eleven call sites, for no measured gain, is Phase 7's business behind a benchmark, not a slice's. **The general lesson: before diagnosing a performance divergence, make sure both sides are running the same code path - a prefilter that answers first makes the engines incomparable.**
- 2026-08-31 **A repeat count is a count of characters, and this port's positions are UTF-16 code units, so upstream's `text_pos += count * step` cannot be ported as arithmetic.** Upstream indexes the subject by codepoint, which makes a character count and a subject offset the same number; it multiplies freely (`_regex.c:13390`, `:13754`, `:15721`, `:15851-15852`, `:16485-16489`) and subtracts freely to get a count back (`:16297`, `:17058`). Here an astral character is one character and two code units, so every one of those sites needs a walk instead: `Matcher.CountOne` returns the position its walk reached as an extra out-parameter, and `Matcher.StepBy` and `Matcher.CountBetween` carry the two conversions that remain. Both are documented as **not upstream functions** and as existing for exactly this reason, so a future sync does not read them as an invention. The negative control that this is load-bearing: making `CountOne` step one code unit per character instead of one codepoint diverged on 1 of 600 quantifier rows - thin, because a lone low surrogate still satisfies `.` and the walk often ends in the same place, which is precisely why it needs the dedicated astral assertions in `Gaps/Engine/RepeatTests` rather than the wave alone.
- 2026-08-31 **`push_repeats`/`pop_repeats` and the guard-data pushes belong to Phase 4, not to S19, against S19's own slice file - the same mistake the S18 slice file made, found the same way.** The slice listed "the real bodies of `push_guard_data` / `pop_guard_data` and `push_repeat_data` / `pop_repeat_data` / `push_repeats` / `pop_repeats`" as its scope. One grep settles it: the ten call sites (`_regex.c:12228`, `:12431`, `:13407`, `:13502`, `:13514`, `:15423`, `:15475`, `:16369`, `:16400`, `:16416`) are all inside `CONDITIONAL`, `END_CONDITIONAL`, `GROUP_CALL` and `GROUP_RETURN`, every one Phase 4. The repeat opcodes park their own state through `RE_RepeatStateData`, `RE_BodyEndStateData`, `RE_MatchBodyTailStateData` and `RE_RepeatOneStateData` instead. **Two slice files in a row have now claimed a Phase 4/5 helper for a Phase 3 slice on the strength of what the helper's name suggests, so the next phase's slice files should be written with the grep in hand rather than from the header comments.**
- 2026-08-31 **A negative control that does not fire is evidence about the control, not about the code, and it has to be replaced rather than recorded.** Two of S19's three planned controls missed. Relaxing `if (count > maxCount) goto backtrack` in the `LAZY_REPEAT_ONE` backtrack case (`_regex.c:17090`) produced **zero** divergences in 600 rows, because `pos` never advances past `limit` and so `count <= maxCount` always holds - the branch is upstream's defensive guard and is dead in practice. Rewriting `CountBetween` to subtract positions instead of walking codepoints made the engine **hang** rather than diverge, which is a signal but not a countable one. The three controls that did fire, and the numbers worth keeping: refusing to retreat a greedy `*_REPEAT_ONE` down to exactly its minimum gave **148 divergences of 600**; counting a lazy `*_REPEAT_ONE` up to its maximum instead of its minimum gave **54 of 600**; stepping one code unit per character in `CountOne` gave **1 of 600**.
- 2026-08-31 **A negative control must be recorded with the four values needed to re-run it - the before/after snippet, the generator, the row count and the seed - not just the number it produced.** Verifying S19, reconstructing its strongest control from the English description in this file gave 3 divergences, then 8 on a second reading, against the 148 it reported; the reported figure was correct and only the session's own gitignored `.scratch/control.py` and `.scratch/ctrlA.txt` settled it, at 452 agree / 148 diverge, seed 7. Prose describing a mutation is not a mutation. Since the control figures are the only evidence that an oracle generator can detect a fault at all, a figure nobody can reproduce is not evidence - and it was living only in a gitignored directory, against this project's own rule. The `port-slice` skill now requires the four values in the closing notes, S19's are back-filled, and S26 re-runs each one at the phase close. S18's are already lost: its scratch files are gone and only the numbers survive, which is the demonstration rather than a hypothetical.
- 2026-08-31 **The Phase 6 mutation-testing pass widens from the API layer to the engine, because the constraint that scoped it narrowly was machine time and the owner has granted overnight runs.** Amendment 12 already made mutation testing item 3 of the Phase 6 exit gate and the roadmap already parked Stryker.NET for Phase 6, both confined to the public API layer and the parse-error paths on the stated grounds that "Stryker reruns the suite per mutant, so a 30k-line engine would take hours to days per run". That is a working-day objection, not a correctness one. With the pass and the Phase 7 optimisation slices allowed to run overnight with no contention, the engine comes into scope - and the engine is what Phase 7 rewrites, so it is the part whose test coverage most needs measuring before that starts. Sized at 1-2 sessions, Phase 6 6-10 to 7-12. Design spec amendment 14 has the evidence: mutant detection correlates with real-fault detection more strongly than statement coverage does, with a 73% coupling rate and a 17% blind spot (Just et al., FSE 2014), and Stryker.NET 4.16.0 reaches TUnit only through its still-preview MTP runner, which is why the deliverable is a written verdict rather than a CI gate.
- 2026-08-31 **S20 ported `ATOMIC` and `END_ATOMIC`, which its slice file did not scope, because `\X` compiles through them.** `_regex_core.Grapheme._compile` (`upstream/regex/_regex_core.py:2919`) turns `\X` into `Atomic(Sequence([LazyRepeat(AnyAll(), 1, None), GraphemeBoundary()]))`, so before the atomic opcodes existed every `\X` pattern threw `needs:atomic` without reaching a single grapheme rule. The two honest options were to port `GRAPHEME_BOUNDARY` as code no pattern could reach - unreachable, untestable, unverifiable, and the sort of thing that rots until Phase 4 - or to port `ATOMIC` as its dependency and deliver the `grapheme` tag the slice was written to deliver. The second cost about 90 lines: two forward cases, two backtrack cases and `push_captures`/`pop_captures`, which save only each group's `count` and `current` because a capture list is append-only within a match. `push_fuzzy_counts` is omitted from both ends, marked `NOT PORTED`, so the stack block is the same shape at both. The knock-on is that `atomic` is delivered too, `\R` works, and six more ported tests pass. **The general lesson, and the third scope surprise in three slices: a slice file written from upstream's C without checking what the Python compiler emits will mis-scope - S18 and S19 over-scoped from header comments, S20 under-scoped because the construct it named compiles through one it did not.**
- 2026-08-31 **Three of S20's four negative controls fired on nothing until the generator was widened, and that loop is the work, not a formality.** Seeding a fault into WB5 diverged on **0 of 600** rows: WB1/WB2 answer "boundary" for the two ends of the subject before any other rule runs, and an affix-shaped pattern searched forwards mostly matches at position 0, so 48 rows holding `(?w)` and `\b` never reached a rule past WB2 - fixed by an `infix` shape that puts the assertion between two characters the subject holds side by side. Seeding a fault into the regional-indicator count then also gave **0 of 600**, because two regional indicators have to be adjacent for GB12/GB13 to decide anything and a per-character draw rarely produces a pair - fixed by inserting whole clusters (a flag pair, an emoji ZWJ sequence, a base plus combining mark, a Devanagari conjunct, `can't`, `3.2`). Removing the `\K` backtrack restore gave **0 of 600** again, because none of the first five `\K` shapes can *fail* after the marker - fixed by three shapes that put the `\K` inside an alternative or an optional group. Final figures, all on `boundaries`, 600 rows, seed 7: 1, 2, 2 and 3 divergences. **All four are thin, so each of the four behaviours also has a dedicated assertion in `tests/FuzzyRegex.Tests/Gaps/Engine/BoundaryTests.cs`** - a control that fires on one row in six hundred proves detectability, not coverage.
- 2026-08-31 **`Py_UNICODE_TOLOWER` in `is_unicode_vowel` is `PythonStr.Lower`'s first codepoint, and U+0130 is why it matters.** WB5a does not break between an apostrophe and a vowel, and upstream decides "vowel" by simple-lowercasing first. Measured against `regex` 2026.7.19: `finditer(r"(?w)\b", "'İ")` starts at 0 and 2 - no break - while `"'ı"` (dotless i) gives 0, 1 and 2. So U+0130 LATIN CAPITAL LETTER I WITH DOT ABOVE counts as a vowel and U+0131 does not, which only a mapping that sends U+0130 to `i` gets right. `PythonStr.Lower` puts U+0130 in its SpecialCasing expansion (`i` plus a combining dot) and only its first codepoint can be one of the twenty vowels compared against, so taking `[0]` is upstream's answer. The apostrophe has to sit at the start of the subject for the rule to be observable at all: between two letters WB6 and WB7 answer first and WB5a never runs, which is why `"l'a"` and `"l'z"` give identical answers and look like evidence that the rule does nothing.
- 2026-08-31 **A quoted measurement has to be re-taken after the last change to the thing measured, and the recorder's seeding is not `random.Random(seed)`.** Nine of the eleven findings across S21's two blind reviews were the same mistake: a figure in a comment that no longer reproduced. Four of them shared one root cause - the measurements were taken with `random.Random(1)` while `_record_wave` seeds a generator with `random.Random(f"{seed}:{name}")` (`record-oracle.py:1219`), so `"1:backrefs"`, a different stream. A figure measured on the wrong stream describes a wave the recorder never produces, and it looks exactly like a real measurement. The rest were figures taken before a later change to the generator (a new shape, a longer subject) and never re-taken, plus one stale constant - "1,534 patterns" for the compile-parity corpus, a number this file had already corrected to 1,547. **Measure last, from the tool a reader would run, and name that command beside the figure.**
- 2026-08-31 **`REF_GROUP`'s codepoint stepping is not observable, and is kept anyway.** Replacing `NextPos` with `++` on both `stringPos` and `state.TextPos` in the `RefGroup` case agreed on all 600 rows of the S21 wave *and* passed the whole ported suite, including the three astral gap tests written to catch it. The reason is structural, not a test gap: both operands of a backreference index the same string, so a code-unit walk stays in lockstep with a codepoint walk - it tests each surrogate half separately and lands on the same `span.End`. Kept as `NextPos` because upstream's `++` means one character, every other opcode in this port spells that `NextPos`, and Phase 5's fuzzy retry advances `stringPos` on its own, where the lockstep argument stops holding. **The code carries this reasoning as a comment, because nothing we can run would catch a future reader who deletes it.** The mirror case, `GROUP_EXISTS` testing `Count > 0` instead of `Current >= 0`, is also unobservable and for a similar reason: `UnsaveCapture` decrements `Count` on the same backtrack that restores `Current`, so the two only part company for a branch-reset group.
- 2026-08-31 **A review subagent runs in the working tree and can dirty it, so the pre-commit `git status --porcelain` read is load-bearing.** S21's first reviewer left `upstream/regex/__pycache__` behind, which dirties the submodule, and regenerated `tests/parity-baseline.json` - a file it had no business writing. Neither is visible in a test run or in the ratchet's verdict, and both would have shipped in the commit. Brief a reviewer to clean up after itself and to leave the baseline alone. **The near-miss on top of that one is worth its own line: the regenerated baseline read `passingCount` 4309 against 4416 passing tests, which looked exactly like a stale figure and was written up as one before being checked. It is not stale - `Update-Baseline` stores the sorted set of *unique* ids (`PortTools.psm1:237`), so parameterised rows sharing a display id collapse and the number sits about a hundred below the raw pass count by design, as it did at S20 (4238 against 4343). Reading the function settled in one grep what the plausible story would have recorded wrongly for good.**
- 2026-08-31 **`(?(DEFINE)...)` delivered no ported tests, against S21's slice file, because all 15 of them call into the DEFINE.** The slice expected `define-groups` (15 tests) to be deliverable, on the sound reasoning that upstream compiles a DEFINE as a `GROUP_EXISTS` whose condition never holds, so its body never runs in place. That reasoning is right and the matcher now does exactly that - but every test in `RegressionsDefineTests` also spells `(?&name)`, which is `CALL_GROUP` and Phase 4's, so all 15 retag to `needs:recursion` and the delivered behaviour has no ported test at all. It is pinned by a gap test instead: `(?(DEFINE)(?<x>zzz))zzz` matches `zzz` with group `x` unset, which is only true if the body was stepped over rather than run. **Fourth scope surprise in four slices, and the first where the miss was in the *tests* rather than in the construct: reading upstream's compiler told the slice author what the engine had to do, and only reading our own ported tests would have told them what it would score.**
- 2026-08-31 **A negative control must also be re-run at a seed the slice has not used, and both numbers recorded.** S20's Control C - flipping WB5's `&&` to `||` - gave 2 divergences of 600 at its recorded seed 7, 2 at seed 12345 and **1** at seed 99. A control that catches a deliberately broken Unicode word-break rule on one row in six hundred is a thin margin, and a different seed could plausibly give none, at which point the control would appear to disprove a fault that is really there. The fresh run costs about three seconds - recording 600 boundary rows and replaying them was under two seconds measured - and it is the only way to tell a control that catches a fault from one that caught a coincidence. Deliberately NO numeric threshold: there is no evidence for where the line sits, and a rule such as WB5 genuinely applies to few generated subjects. The `port-slice` skill carries the rule; S26 re-runs every recorded control at a fresh seed at the phase close. Also learned re-running S21's Control B: quote the snippet as the file actually reads, because CSharpier breaks a ternary across three lines and the recorded one-liner appears nowhere in `Matcher.cs`.
- 2026-08-31 **Native AOT constrains a compiled fast path; it does not disqualify one, and the roadmap now says so.** The owner asked whether supporting AOT must deprive non-AOT users of `RegexOptions.Compiled`-style optimisation. Researched: no. A single assembly guards the emit path with `RuntimeFeature.IsDynamicCodeSupported` and marks it `[RequiresDynamicCode]`, which Microsoft's library guidance explicitly prefers over multi-targeting or separate packages. The real split is not AOT versus JIT but *when the pattern is known*: a source generator is AOT-compatible and gives "all the throughput performance benefits of RegexOptions.Compiled (more, in fact)" but only for compile-time literals, so runtime-constructed patterns - likely a large share for a fuzzy library - can be served only by runtime emit or by the AOT-safe middle tier of delegate threading. Trap recorded: `Expression.Compile()` is not a workaround, it silently falls back to the LINQ interpreter under AOT, exactly as `Compiled` silently becomes a no-op. **No scope change** - whether this port gets a compiled backend is still the owner's open decision, and either backend is phase-sized, since .NET maintains `RegexCompiler` and its source generator as near-1:1 twins.
- 2026-08-31 **Probe upstream with `regex.match`, never `regex.search`: its `search_start` prefilter screens start positions with predicates its own matcher does not use, so `search` and `match` can disagree.** `regex.search(r'(?ai)\p{Ll}', 'A')` is `None` where `regex.match` of the same pair spans `(0, 1)`, and `regex.search(r'(?ai)x?\p{Ll}', 'A')` - the same question with the screen defeated - spans `(0, 1)` again. S22 probed with `search` alone, concluded that `matches_PROPERTY_IGN`'s ASCII arm (`_regex.c:2972`) was not what upstream runs, rewrote the predicate to the encoding table's `has_property_ign`, and only a 1500-row wave of `match`/`fullmatch` rows showed the opposite. **The literal transliteration was right the whole time**; the detour cost most of the slice. `search_start` is the Phase 7 deferral, so until then this port's search answers what upstream's *match* answers - which is a known difference, not parity, and is pinned as such in `Gaps/Engine/CaseInsensitiveMatchingTests.cs`.
- 2026-08-31 **Upstream does not agree with itself about a cased property under the ASCII encoding, so the oracle's `case-folding` generator keeps the ASCII flag off property rows.** Three code paths reach three predicates and only one does the cased-category collapse: the dispatch switch's `matches_PROPERTY_IGN`, `count_one`'s `match_many_PROPERTY_IGN` (which calls `has_property_ign`, and does not collapse under ASCII) and `search_start`'s screen. `regex.match` against `'KsKK'`: `(?ai)\p{Ll}` spans `(0,1)`, `\p{Ll}{2}` `(0,2)`, `\p{Ll}{4}` `(0,4)`, `(\p{Ll})+` `(0,4)` - but `\p{Ll}+` spans `(0,2)` and `\p{Ll}*` spans `(0,0)`, where `(?ui)` gives `(0,4)` for all three. A greedy `*` consuming nothing where `+` consumes two is not a rule any predicate states. This port collapses in every path, which is the literal transliteration of `matches_PROPERTY_IGN` and self-consistent, so every ASCII property row would record as a divergence for as long as upstream is like this. **Candidate for an upstream report; not filed, as that needs the owner's approval.**
- 2026-08-31 **Two more Phase 7 deferrals are observable on full case folding, and the comment claiming the prefilters "answer the same" is now qualified.** Found by S22's blind review and re-verified: `regex.match('(?fi)fi', 'fı')` spans `(0, 2)` where this port finds nothing, because a whole pattern that is one `STRING_FLD` goes through `locate_required_string` into `string_search_fld`, which compares with `same_char_ign_turkic` (`_regex.c:6687`) - so the dotless i folds with `i` there and nowhere else in upstream; splitting the string (`(?fi)(f)i`) makes upstream answer `None` and agree with us. And `regex.match('(?fi).*ẖẛ', 'ẖṡ')` is `None` where this port spans `(0, 2)`, because upstream's `GREEDY_REPEAT_ONE` retreat fast path (`:16041`) clamps by a folded length it recomputes from already-folded pattern values, over-counts and gives up early; `(?fi)x?ẖẛ` and the bare `(?fi)ẖẛ` both span `(0, 2)`. Both are pinned as known differences. **So the deferrals are not uniformly transparent: they are transparent for the case-sensitive opcodes and not for the folding ones.**
- 2026-08-31 **A negative control that will not fire after three widenings is a boundary of the generator, and the honest move is to record it and name what does catch the fault.** S22's control D - `REF_GROUP_FLD` comparing the captured character raw instead of folded - gave 0, 4 and 1 divergences of 600 at three seeds, after doubled subjects, a `folded-backref` shape and subject-aware group selection were each added to try to make it bite. The reason is structural: only 1 of 19 FULLCASE backreference rows that match at all has a captured character that expands on folding, because the shape, the flag, the alphabet and the repetition all have to line up at once. What does catch it deterministically is the gap tests - with the control applied, three named tests fail every run. **Recorded in the closing notes as "do not trust the wave for this opcode", which is worth more than a fourth widening that might have bought one more row.** The three widenings that did work are kept, each with its own before/after numbers in a comment beside it.
- 2026-09-01 **A negative control's number is only valid for the code and generator it ran against, so every control gets one final re-run before the commit.** Verifying S22, its Control A - `SameCharIgn`, loop start 1 to 2 - was recorded at 15 divergences of 600 at seed 7 and 16 at seed 4242; the committed code gives **42 and 23**. B and C were out by 1 to 3. Only Control D reproduced exactly (0, 4, 1), and D is the one whose notes describe three later attempts to widen the generator - so it was the control run last, which is the entire explanation. The generator kept growing, and the blind review's own fixes land after everything else, so a control run mid-slice measures a generator that no longer exists by the end. The wave generator was ruled out as a cause: recording the same seed twice gives byte-identical output. No engine defect - every control fires at least as hard as claimed, the ratchet was green and the committed wave agreed at 2700 rows. S22's notes are corrected in place with the originals kept in brackets, and the `port-slice` skill now requires the final re-run. Third instance of the same lesson after S18 and S19: evidence recorded before the work finished is not evidence about the work.
- 2026-09-01 **`Sort-Object -Unique -CaseSensitive` is still culture-sensitive, and it had been quietly dropping 51 tests from the parity baseline.** `-CaseSensitive` was added at S09 to stop `Resolves(ABC)` and `Resolves(abc)` collapsing, and it fixes that - but the comparer underneath is still the culture one, which also folds compatibility characters, so `X.Folds(K)` and `X.Folds(K)` (U+212A KELVIN SIGN) collapse too. Reproduced in three lines of PowerShell: two ids in, one out, and the same under `-Culture InvariantCulture`. Found by S23's blind review *in S23's own regenerated baseline*, where the `(?i)[a-z]` rows of `CaseInsensitiveMatchingTests` swapped which of the pair was listed - so which test the ratchet watched was changing from run to run. Replaced with a `SortedSet[string]` over `StringComparer.Ordinal`, which is what `Test-Ratchet` already used at the reading end for the same reason; the baseline's unique count went **4594 to 4645**. Two Pester tests pin it, one for the collision and one for the `$null`-id regression the cast to `[string[]]` introduced (a `$null` becomes `""`, which the ratchet then reports as `Missing` for ever). **The lesson beyond this bug: a de-duplicating comparer is a silent coverage sink, and the only thing that made it visible was an unrelated slice regenerating the file and reading the diff.**
- 2026-09-01 **Reverse matching cost far less than its slice file scoped, because S16 and S19 had already ported the direction plumbing.** S23 listed `state->reverse`, the reversed search advance, reversed `get_limits` semantics, backward codepoint stepping, and the REV rows of both `*_REPEAT_ONE` backtrack sub-switches. All of it was already in place: S16 ported `state->reverse`, `PrevPos`, the reversed `next_match_2` advance, the reversed `at_end` and `do_exact_match`'s reversed width check when it ported the spine; S19 wrote the `*_REPEAT_ONE` backtrack cases against `node.Next2.Test!.Step`, which is `-1` for a reversed repeat, so they needed no change at all; `get_limits`'s boundary arithmetic is direction-neutral and was already `MatchState.ClampIndex`. What was actually left was the dispatch cases and two counting helpers. **A slice file written a phase ahead scopes what upstream contains, not what earlier slices have already taken from it - read the port before believing the estimate.** The reverse of the same coin, and the more expensive half: the slice claimed 49 `right-to-left` tests and delivered 11, because 34 of them call `FuzzyRegex.Matches`, `Split` or `Replace` and were tagged for the direction rather than for the API they need.
- 2026-09-01 **Under `(?r)` a backreference only runs if it is written before its group.** Reverse execution takes the sequence from the right, so `(?r)(.)\1` reaches `\1` while the group is still empty and never matches - `regex.match(r'(?r)(.)\1', 'abb')` is `None`, and so is every `(?r)(x)\1` shape probed. The working form is `(?r)\1(.)`, which is what upstream's own `test_recursive` reverse rows use. Worth recording because the failing shape is the one a reader writes first, and it looks exactly like a `REF_GROUP_REV` bug: S23's first six backreference probes all came back `None` and had to be re-derived before any of them could be pinned as a test.
- 2026-09-01 **A negative control aimed at a rare cell is evidence about the generator before it is evidence about the code.** S23's control D - `STRING_FLD_REV` reading the subject's folding from the front - fired on **1** row of 600 with the reverse wave drawing its fold rows from the ordinary `case-folding` generator. That cell is exactly the one upstream's 2026.5.9 fix was about ("Reverse matching with full unicode casefolding could lead to out-of-range string indexes"), and the slice had been told to probe it hardest. The reason it was unreachable is multiplicative: the fold share, the FULLCASE coin, the shape draw and the alphabet rotation each cut it. Adding `_generate_reverse_fold`, which forces all four, took it to 21 and 20 at two seeds. **The moment to widen a generator is while the mutant is still planted - the number before and after is the only proof the widening bought anything, and it cannot be recovered later.** Contrast S22's control D, which would not fire after three widenings and was correctly recorded as a boundary rather than chased further.
- 2026-09-01 **A count-style argument whose convention is inverted against upstream must be translated at *both* ends, and S24 translated one.** Upstream's `count=0` is "no limit" and its `count=-1` is "no replacements at all" (`regex.subn('a','b','aaaaa',count=-1)` is `('aaaaa', 0)`, measured); this surface spells those `-1` and `0`, as S01 decided. The oracle's consumer mapped only `0 => -1`, so a recorded row carrying a negative count asked the two engines opposite questions and reported a correct port as RED. Found by S24's blind review with a hand-written `-Rows` row. Fixed to a three-way switch, pinned in `OracleWaveTests`, and `SUB_COUNTS` gained a `-1` so the generator exercises the end that only a hand-written row could reach before. **S25's `maxsplit` has the same shape** - the lesson is that an inverted convention has two ends and the second one is invisible until something asks for it.
- 2026-09-01 **`min_width` is a codepoint count, so a comparison against it is only safe in code units when it errs towards *not* taking a shortcut.** `do_exact_match`'s width check may use code units because it only ever fails early, and units are never fewer than codepoints; `pattern_subx`'s too-short-subject shortcut may not, because it *returns* early and does so before the replacement template is compiled. On an astral subject that difference is observable: `regex.subn('..', r'\g<bad', '\U0001F600')` is `('\U0001f600', 0)` upstream, where comparing 2 against the two code units made the shortcut miss and the port compiled and rejected a template upstream never looks at. Found by S24's blind review. The general rule: **when porting an early-out, ask which way it errs before reusing an existing width comparison** - the same arithmetic is sound in one direction and a divergence in the other.
- 2026-09-01 **Upstream answers the same invalid template two different ways, and both are the port's answer too.** `regex.sub('x', r'\1', 'x')` raises `regex.error("invalid group reference")` from `get_sub_replacement`; `regex.match('x','x').expand(r'\1')` raises `IndexError("no such group")` from `get_match_replacement`. The two functions are otherwise the same check. Ported as `FuzzyRegexParseException` and `ArgumentException` respectively, which is also what the Phase 1 `SubTemplateNumericEscapeTests` and `SymbolicRefsTests` ports already asserted. Consequence for the oracle: a rejection raised *while substituting* is now a legitimate agreement, so the recorder records a `whileMatching` flag and the comparer requires the two sides to have thrown at the same point rather than assuming every recorded rejection came from `regex.compile`. Without that, every invalid-group-reference row was a false divergence.
- 2026-09-01 **A negative control that fires on zero rows is the generator's verdict, not the code's, and the widening that fixes it is only measurable while the mutant is planted.** S24's control F - the too-short shortcut comparing code units instead of codepoints - fired on **0** of 600, because seeing that cell needs an astral subject, a pattern whose codepoint width falls strictly between the subject's codepoint and code-unit counts, *and* a template the compiler rejects, all at once. A dedicated `_narrow_row` arm forcing all three took it to 21 at two seeds. Control E, the negative-subscript rule, was thin for a related reason: no pattern shape made a group capture more than once, so `{n[-1]}` and `{n[0]}` agreed by accident; adding a `(atom)+` shape and weighting the negative subscripts took it from 11 to 16 and 14. **Both numbers exist only because the control was re-run before and after the widening** - the same lesson S23 recorded, now with a case where the before-figure was zero.
- 2026-09-01 **The port was doing exactly `3n^2` character-walk steps where upstream does four machine instructions, because a UTF-16 port has to convert between a character count and a position and this one converted by walking.** Found while verifying S24 by asking why the suite had gone from 1m28s to minutes. Upstream's `LAZY_REPEAT_ONE` backtrack arm (`_regex.c:16485-16489`) computes `pos`, `available`, `max_count` and `limit` with one multiplication and two subtractions, because a CPython `str` is a fixed-width codepoint array (PEP 393, `PyUnicode_KIND` at `:18232`) so the k-th character *is* index k. A .NET `string` is UTF-16 and is not fixed width, so S19 introduced `StepBy`/`CountBetween` to convert - correctly, but as loops. The arm is re-entered once per repeat position, so four linear conversions inside an `n`-entry loop is quadratic. **Measured by counting the walk steps, which unlike a clock does not depend on machine load: 107,999,997 steps at 6,002 characters, 431,999,997 at 12,002, 1,727,999,997 at 24,002 and 6,911,999,997 at 48,002 - `3n^2` to four significant figures at every size.** The same shape is in the `GREEDY_REPEAT_ONE` arm, so `a+b` over a long run of `a`s had it too. Fixed, not deferred: it is a porting defect rather than a missing optimisation, nothing was traded for anything, and the honest place to fix one is where it was introduced. Correction to this entry's first version, which quoted 372 seconds for the worst test and 6m24 for the suite: both were measured on a loaded machine. Re-measured in isolation the test takes 69 seconds and the suite 53, and the suite's own range across runs was 53s to 3m16 - **that variance is itself the symptom, because quadratic work is what makes a suite sensitive to load in the first place.**
- 2026-09-01 **Two paths, because there are two kinds of subject: upstream's arithmetic restored for subjects with no surrogate pair, and a sampled position table for the rest.** `MatchState.OneUnitPerCharacter` is one vectorised scan for any high surrogate; when it holds, one character is one code unit and `pos + count * step` is exactly upstream's line, so BMP subjects drop to **zero** walk steps and the fast path can be reviewed against `_regex.c:16485` side by side. When it does not hold, `CharacterIndex` records the position of every 32nd character boundary, so a conversion is one array read or a binary search plus at most 31 steps of the ordinary walk. Astral subjects go from 6,911,999,997 steps and 6.25s at 48,002 characters to **2,328,005 steps and 0.034s**, and from `3n^2` to a flat 48.5 steps per character. The table is one `int` per 32 characters, about 3% of an all-astral subject, built lazily on the first conversion and only as far as `TextEnd`, so a pattern with no single-character repeat pays nothing and `Match(hugeSubject, 0, 10)` does not index the whole subject. Suite 53s to 4.4s; against upstream we are now ~68x on BMP and ~227x on astral, where astral was ~41,000x and growing with the square. The stride is a measured tuning knob and not a correctness property - halving it halves the residual walk exactly, but below 32 the clock stops improving, and setting it to 33 leaves all 5,703 tests passing. Rank-and-select is the documented upgrade path if a measurement ever asks. **The background, all six options with their pros and cons, and the measurements behind the choice are in `docs/plan/2026-09-01-quadratic-conversion-decisions.md`**, kept as the record of why these two files exist. **What this does not buy: upstream costs the same for astral as for BMP, and nothing short of changing how .NET stores strings reaches that.**
- 2026-09-01 **The reason a sampled table beat carrying the position around, which is what Java does, was testability rather than size.** Java's `Curly` (`Pattern.java:4522-4622`) carries the UTF-16 index and the character count side by side and never converts one into the other, which is the structurally cleaner answer and would have fixed astral subjects too. It lost on two grounds. It is not smaller - `available` still needs the arm's upper bound cached per repeat, with a rule for when that cache goes stale, because one of the arm's own branches rewrites the repeat's start. And **it cannot be checked**: its state exists only part-way through a backtracking search, so there is no reference to compare it against, and a stale cached position gives a wrong answer rather than a crash. `CharacterIndex` is a pure function of the subject and the walk it replaces is a complete reference implementation of that same function, so it is compared against that walk over every string of up to four symbols from an eight-symbol alphabet - 4,681 subjects, every position, every count, and for shorter ones every pair of slice bounds. **That test found two real defects in the first draft within minutes of existing**, one of them a `CountBetween(p, p)` that answered 1 instead of 0 inside a surrogate pair. Java can afford its shape because its loop was positional from the start; ours is a line-for-line port of C that converts because C could do it for free. The general rule: **when two designs both work, prefer the one whose correctness has an executable oracle, even if the other is structurally tidier.**
- 2026-09-01 **`NextPos` and `PrevPos` disagree about where a character starts at positions above `TextEnd`, and no upstream answer exists because the question cannot arise there.** `NextPos` refuses to pair a high and low surrogate when `pos + 1 >= TextEnd`; `PrevPos` pairs regardless of any bound. So above `TextEnd` a forward walk sees two characters where a backward walk sees one. Found by the exhaustive `CharacterIndex` test, which swept positions the engine cannot reach and failed until the sweep was narrowed. Provably unreachable from the engine: `NextPos` is only called at `pos < TextEnd`, and for the two to disagree at `pos` you need `pos >= TextEnd - 1` while `PrevPos(pos + 2)` also needs `pos <= TextEnd - 2`. So it is a latent trap rather than a live bug, and it is left alone deliberately - it is a semantic decision about what `beginning` and `length` mean when they split a surrogate pair, upstream indexes by codepoint so it has no opinion to port, and bundling a semantic change into a performance fix makes both harder to review and to revert. **Carried to S26's symbol accounting.** Recorded because the only thing that surfaced it was a test deliberately asking questions outside the domain, which is worth doing again.
- 2026-09-01 **Three ported tests asserted the opposite of what upstream asserts, and only a working engine could reveal it.** All three were written in Phase 1 against a suite that could not run them, and all three failed against a correct S25 engine. (1) `WordBoundaryTests` #6 dropped the trailing `""` from a `(?V1w)\b` split; `pattern_split` appends the segment after the last match "even if empty" (`_regex.c:22330`) and upstream's own assertion carries it (`test_regex.py:1566`). (2) and (3) `RegressionsKeepMarkerTests` #165 and #168 asserted on `m.Value` where upstream's `findall` yields **the one group's** text when the pattern has exactly one (`:22440`) - which inverted the point of the upstream assertion, that `\K` shrinks the match *without* shrinking the group: `regex.findall(r'(\w\w\K\w\w)', 'abcdefgh')` is `['abcd', 'efgh']` where the matches are `'cd'` and `'gh'`. Each fixed with the measurement quoted at the assertion. The general rule: **a skipped test is unverified, not merely unrun** - a Phase 1 port of an assertion is a hypothesis about upstream until something executes it, and the slice that un-skips it is where that hypothesis gets tested. Expect a handful per slice for the rest of the phase, and prove each against the real module before touching it.
- 2026-09-01 **`state->version_0` and `state->visible_captures` are both written and never read in this release, so there is nothing to port for either.** S25's slice file expected a V0/V1 difference in how a split treats a zero-width match, and S24's expected one in `pattern_subx`. Neither exists: `state_init` sets `version_0` at `:18482` and no other line of `_regex.c` reads it; `visible_captures` is set at `:18307` and likewise unread. Measured for the split case over four V0/V1 pattern pairs, every one identical. Both pinned by a gap test naming the mechanism, so the next slice that reads an upstream comment about "the new behaviour" does not go hunting for a code path that was removed. **The general rule: a field being set is not evidence that it is read**, and `grep` for the reads is cheaper than any amount of reasoning about the comment beside the write.
- 2026-09-01 **Upstream writes the same scan loop twice - once as `findall`, once as the scanner - and this port has one, because the two were shown to agree rather than assumed to.** Only `pattern_findall` (`:22415`) carries the `slice_start <= text_pos <= slice_end` guard; `scanner_search_or_match` (`:20874`) has none and relies on `do_match` failing. Folding them is exactly the kind of tidying that costs a future sync, so it needed evidence: `regex.findall` and `[m[0] for m in regex.finditer(...)]` are element for element identical on nine subject/pattern pairs, overlapped and not, including the reverse and zero-width cases where the guard is the only thing that ends the scan. Kept as `Iteration.Scan` with the guard, since the guarded version is the one whose termination is visible in the code. **The rule this instance illustrates: two upstream functions may be merged only when a measurement says they answer alike, and the measurement goes in the comment.**
- 2026-09-01 **A wall-clock ceiling is not a sound complexity assertion inside a parallel test suite, and a ratio between two input sizes is.** S25's linearity guard for the iteration surface first asserted "under 5 seconds" and failed the ratchet on its first run at 7.5s - not because the scan was slow but because the suite's other 5,700 tests were running alongside it; the same three scans took 2.1s alone. Rewritten to `[NotInParallel]` plus a ratio: 468ms at 480,000 code units and 970ms at 960,000, a ratio of 2.07, against 10.8 for a scan that builds a `MatchState` per match. A ratio does not move with machine speed, so the threshold of 5 has the same margin on a slow CI box as here. Two corollaries worth carrying. **The engine's own `MatchTimeout` cannot enforce a per-scan complexity bound**, because a state carries its own start time and a per-match-state scan resets the budget on every match - which is why this needed timing at all rather than the `RepeatTests` trick of leaning on the timeout. And **the failure this guards is invisible to correctness tests**: a per-match-state `Matches` passes every one of the 169 tests S25 un-skipped and takes 209 seconds where the shared-state scan takes 0.14.
- 2026-09-01 **An oracle row whose answer is a sequence has to be compared as a sequence, or the harness agrees with a scan that is wrong in the only way a scan can be wrong.** S25 added `finditer`, `finditer-overlapped` and `split` operations to the wave, recording the whole list of matches (or split pieces, `null` slots included) rather than one. A row comparing only the first match, or comparing a count, agrees with a scan that finds the right matches in the wrong order, stops one match early, or repeats a zero-width match at one position - and those are precisely the defects an iteration port introduces. Confirmed by mutation: emitting matches in reverse order diverges on 243 of 600 rows, and dropping the trailing empty split part on 103. **The general shape for the rest of the port: the recorded outcome must be the whole answer at the granularity the caller sees it**, which for `Match` was groups-and-captures, for `sub` was the `(text, count)` pair, and for a scan is the ordered list.
- 2026-09-01 **The `NextPos`/`PrevPos` asymmetry is kept, and what the *public* surface does when `beginning`/`length` cuts a surrogate pair is pinned instead.** The three options the S26 slice file offered were to make the two symmetric, to keep the asymmetry and document it, or to defer it to Phase 6. Kept, for two reasons. The disagreement is only reachable above `TextEnd`, which the engine never asks about, so a bound test in `PrevPos` would buy nothing observable and sits in the inner loop of every reverse step and every `CharBefore`; and a change with no test that can go red is not a change this repo can protect. What *was* missing is the semantics one layer up, and that turned out to have an oracle after all: Python cannot express an `endpos` inside a character, but it can hold a `str` whose last character is a lone surrogate, which is exactly the string a cut slice denotes. Measured against regex 2026.7.19: `regex.search('.', '\ud83d')` spans (0, 1), `regex.search('a.', 'a\ud83d')` spans (0, 2) and `regex.search('(?r).', 'a\ud83d')` spans (1, 2) - a lone surrogate is one character in both directions. This port answers the same for the equivalent cut, and `MatchSpineTests.A_length_that_cuts_a_surrogate_pair_leaves_a_lone_surrogate_that_matches_as_one_character` pins it in both directions with the pair-intact case beside it. **The general rule: when an internal asymmetry is unreachable, the thing worth spending a test on is the observable semantic above it, not the asymmetry.**
- 2026-09-01 **The oracle consumer ran every row with `InfiniteMatchTimeout`, so a row this port could not finish was a hung run rather than a red one.** Found the hard way while re-running S17's negative control: a mutant sat in `dotnet test` for six minutes on a 1,200-row wave the honest engine answers in 589ms, and the harness printed nothing at all - which looks exactly like a slow build. Now ten seconds per row, which turns a runaway into an ordinary divergence with its pattern and subject in the report. Generous by two orders of magnitude: every generator's subjects are eight characters or fewer, and a row upstream itself could not answer quickly would have stalled the recorder first. **The general rule for a differential harness: the reference side has a timeout by construction, because a human is watching it record; the port side needs one written down, or the harness's failure mode is silence.**
- 2026-09-01 **A negative control is reproducible only if its notes name the mutation exactly and the seed it ran at, and S17's named neither.** Re-running all 37 Phase 3 controls from the closing notes: 34 reproduced their recorded figure exactly at the recorded seed, a 35th (S19-A) reproduced only once that recorded figure disambiguated which of two sites its notes meant, and S17's two could not be reproduced as described at all. "Inverting `InSetSymDiff`'s toggle" has two readings, and the one that flips the *condition* is a **no-op for any even-membered set** - every `~~` atom the generator emits has exactly two members - so it diverged on 0 of 1,200 rows where the notes recorded 6. Inverting the accumulator's initial value fires: 10 at seed 7 and 19 at a fresh seed. Neither number can be checked against 6, because S17 recorded no seed. **Two rules from this.** A control's notes must quote the before/after as the file reads plus the seed - already the standing rule, and S17 predates it. And **prefer a mutation whose effect cannot cancel**: a condition flip inside an XOR fold, a sign flip on a value that is then squared, or an off-by-one on a bound the data never reaches are all mutations that measure the generator's luck rather than the wave's reach.
- 2026-09-01 **An analyzer set strong enough to be worth having will refuse some mutations, so a negative control has to be chosen to keep the code compiling.** S17's second control - "swapping the ASCII property table for the Unicode one" - was written as deleting the ASCII substitution from `Encodings.HasProperty`, which leaves `_asciiMax` unused and fails the build on S1144 as an error. The harness then reports no verdict line, which is indistinguishable from a control that fired until you read the build log. Rewritten as swapping which encoding gets the substitution (`CaseEncoding.Ascii` to `CaseEncoding.Unicode`), it keeps every symbol used and expresses the same fault: 138 divergences of 1,200 at seed 7 and 106 at a fresh seed, against the 133 recorded. **So the driver script must treat a missing report as a failure and print the build tail**, which is now what `.scratch/run-controls.py` does, and a control that deletes a code path should be re-expressed as one that redirects it.
- 2026-09-01 **`subprocess.run(timeout=...)` does not bound `dotnet test`, because the timeout kills the direct child and then blocks for ever draining a pipe its grandchildren still hold.** A 240-second bound sat unfired for six minutes: `dotnet test` spawns MSBuild nodes and a test host that inherit the captured `stdout` handle, and `run` calls `communicate()` again after the kill. Redirecting the child's output to a *file* instead of a pipe fixes it. Worth carrying because every future harness script in this repo drives `dotnet` from Python or PowerShell: **capture through a pipe only where you are prepared for the timeout not to work.**
- 2026-09-01 **The pinned oracle question was settled by source identity rather than by a CI run, which is the stronger answer.** S26 owed a re-check of S15's five rejections against the CI-built 2026.8.12 rather than the PyPI 2026.7.19. Neither route was available in the session - `gh` is deliberately outside the driver's allowlist so an unattended slice cannot post, and a local build fails for want of MSVC ("Microsoft Visual C++ 14.0 or greater is required") - but the delta between the two releases is one commit, `1760a20`, and `git diff 1760a20~1 1760a20 -- src/ regex/_regex_core.py` is **empty**: the whole change is a version string, a classifier and a CI matrix line. The C engine that raises `RuntimeError: invalid RE code` is byte-identical in both builds, so a run against 2026.7.19 *is* a run against 2026.8.12 for this question, and all five still reject. **The general rule: before booking a CI run to compare two versions, diff the two versions - a proof that the code cannot differ beats an observation that it did not.**
- 2026-09-01 **The prefilter contingency is closed with no prefilter ported: nothing in the suite is slow, and the two complexity guards are the reason.** S19 opened it in case a ported test had to be skipped for timeout without `locate_required_string`, and S26 widened it to cover a test that merely passes *slowly*. Measured from the final ratchet's own TRX: 5,716 results in 38.7s of test time, of which **10 take a second or more and every one of those is either a Unicode table sweep over the whole codepoint space or a deliberate complexity guard**. Both guards pass - 0.463s for the BMP subject and 0.588s for the astral one, over 240,000 code units each. So the prefilters stay Phase 7 work, where they belong as optimisation behind benchmarks, and `try_match`'s predicate dispatch with them. **And the guard was watched failing, which corrected the recorded control**: forcing `MatchState.OneUnitPerCharacter` false does *not* make it fail (the class runs in 1.2s; the two tests that fail are the astral-correctness ones), because that only moves a BMP subject off the restored arithmetic and onto the sampled position table, which is also linear. Re-expressed as the defect that actually existed - `StepBy` and `CountBetween` converting by walking - the guard fails in **20s 285ms** with `RegexMatchTimeoutException` against 0.493s unmutated, so the 20-second ceiling really is enforced by the engine's own timeout (a `ushort` counter bumped by 0x100 per dispatch iteration polls the deadline every 256 iterations). **The general rule: mutating a fast-path *selector* tests the fast path, not the algorithm - to find out whether a complexity guard is load bearing, mutate the thing whose complexity it asserts.**
- 2026-09-11 **A capability tag on the board is a hypothesis until the tests behind it have been run.** At the Phase 4 checkpoint every remaining `needs:` tag was probed by removing its `[Skip]` attributes and running the suite: 92 skipped tests already passed - the whole of `inline-flags`, `version-flags`, `comments`, `branch-reset` and `possessive`, `(*FAIL)`, and 17 of the 20 `named-lists` tests - because their skip prose described a parser that did not exist yet when Phase 1 wrote it. Upstream never had a `STRING_SET` opcode (`StringSet` lowers to `BRANCH`, `_regex_core.py:4069`) nor a possessive one (`PossessiveRepeat._compile` emits `ATOMIC` + the greedy repeat, `:3034`), so both families were finished by S13, S19 and S20 without anyone noticing. The four "unclaimed" families S26 flagged collapse to `(*PRUNE)`/`(*SKIP)`, 30 tests, which Phase 4 takes. Un-skipped as between-slice maintenance; three named-list tests retagged to the lookaround or fuzzy tag their pattern actually needs. Rule for every future phase close: probe every tag on the board this way before writing the handover - it is a ten-second run per tag.
- 2026-09-11 **Before believing a negative control, check that it fires; a control at zero measures nothing, and the way it gets to zero is by mutating a flag nobody reads.** S27's control C was meant to express "the lookbehind body is compiled forwards" and was first written as `subargs.Forward = forward` to `subargs.Forward = args.Forward` in `NodeCompiler.BuildLookaround`. It found **0 divergences in 1200 rows** across two seeds, which looked at first like a thin generator. It was not: `grep -n Forward src/FuzzyRegex/Engine/NodeCompiler.cs` shows `args.Forward` is read at four lines only (`:1424`, `:1432`, `:1488`, `:1495`), all inside the repeat builders, so the field was never the lookbehind direction mechanism at all. The direction lives in the *parser* - `LookAround.Compile` passes `Behind` to `Subpattern.Compile` (`Parsing/Nodes.cs:3488`), so a lookbehind body arrives at the compiler already reversed. Repointed there, the same control finds 25 and 29 of 600. **The general rule: a control that does not fire is a finding about the control first and the generator second - trace the field you mutated to its readers before widening anything.** This is the mirror of the 2026-09-01 entry above, which warns against mutations whose effect can cancel; this one warns against mutations that never reach the behaviour they name.
- 2026-09-11 **A lookaround generator has to emit multi-atom bodies and a branch that is given up mid-attempt, or two of its three controls sit at one divergence in six hundred.** S27's generator began with single-atom lookaround bodies only, and controls A (the backtrack arm dropping `text_pos`) and B (the lookaround never saving its captures) found 1-2 rows of 600. The diagnosis is mechanical rather than statistical. A single-atom body either matches or fails **without having moved**, so it never leaves `text_pos` displaced and the restore that the backtrack arm exists for has nothing to restore; and a lookaround reached once per match attempt is never *unwound*, so whatever it undoes is never observed being undone. Adding a `sequence` body kind (two or three atoms, the first drawn from the subject) took A to 12 and 7; adding an `alt-with-look` piece - `(?:LOOK atom|atom)`, a branch leading with a lookaround beside a fallback - took B to 7 and 6. **The general rule for any construct that saves and restores state: the generator must reach it on a path that is abandoned, not only on one that succeeds, and the construct's body must be able to consume before it fails.**
- 2026-09-11 **`pop_repeats` is nearly unobservable to a differential wave, because a repeat's own backtrack entries restore the same state along the same path.** S28 ported `push_repeats`/`pop_repeats` with `CONDITIONAL` and built a `conditionals` generator for it. The branch-selection controls fire hard - 29/42, 17/32 and 46/53 divergences of 600 at two seeds - but control C, which corrupts the saved repeat count, sits at **2 and 8**, and control E, which stops the guard lists being restored, at **0 and 1**. Two widenings failed to move C: raising `repeat-of-cond` from weight 12 to 24 with a forced repeating quantifier, and then changing the mutation so a zero count would also bite. The reason is structural. `GREEDY_REPEAT` saves its own count on the bstack and the conditional is unwound along that same bstack, so the conditional's copy is mostly restoring what the repeat machinery would have restored anyway; only an *outer* repeat, mid-iteration, whose count is non-zero when the `CONDITIONAL` is entered can show the difference. **The rule for S30, which reuses the identical mechanism for `GROUP_CALL`/`GROUP_RETURN`: a green conditionals-shaped wave is evidence that the branch selection is right and says almost nothing about the repeat stack - cover that half with a generator built for it, or not at all, but do not claim it.**
- 2026-09-11 **`tools/run-controls.py` caches its waves, so a widened generator silently re-measures the old one.** The cache is `.scratch/control-waves/<generator>-<count>-<seed>.jsonl` and it is keyed on none of the generator's content. S28 widened `conditionals`, re-ran control C and got figures byte-identical to the run before the widening - which reads exactly like "the widening had no effect" and is in fact "the widening was not measured". Deleting the two cached waves gave different numbers immediately. **Delete the cached wave for any generator you have just changed, before you believe a control's figure.** This is the same class of error as the skill's existing rule that a control run partway through a slice measures a generator that no longer exists; this one is the mechanism by which it happens without anyone editing anything.
- 2026-09-11 **`findall` and `finditer` are not the same loop upstream, and S25's measurement could not have told them apart.** S25 recorded them as "measurably the same function - verified 2026-09-01 ... on nine subject/pattern pairs including the overlapped, zero-width and reverse cases", and built `Iteration.Scan` on `pattern_findall`. Only `pattern_findall` carries a `slice_start <= text_pos <= slice_end` loop guard (`:22415`); `scanner_search_or_match` (`:20903`), `pattern_subx` (`:21859`) and `pattern_split` (`:22285`) rely on `do_match`'s own test (`:18128`), which compares `text_pos` against `slice_end` going forward and **never** against `slice_start`. The guard can only bite when something moves `slice_start` mid-scan, and the only thing that does is `(*SKIP)` - which S25 had no way to compile, so its nine pairs were all in the region where the two genuinely agree. Measured against regex 2026.7.19: `regex.findall(r'[A-Z]*(*SKIP)_', '__BB__B', overlapped=True)` gives 3 matches and `regex.finditer` on the same arguments gives 6, and the six are the correct ones - each equals `p.match('__BB__B', k)` for k = 0..5. `Iteration.Scan` and `Iteration.Next` lose the guard and `MatchState.IsInSlice` is deleted. **The general rule: "I measured these two code paths and they agree" is only as strong as the inputs that could have separated them - before recording two upstream functions as identical, find the input that reaches the line where their source differs, and if you cannot compile that input yet, record the difference rather than the agreement.**
- 2026-09-11 **S29 is parked, not done: `(*SKIP)` does not commit the search position, and the mechanism is not understood.** `regex.compile(r'(?:..(*SKIP)x|q)x').search('ab cd xx')` is `None` upstream and stable across repeats, while `.match('ab cd xx', 4)` is `(4, 8)` - upstream finds that match only when the search starts at 4, because a failed attempt in which a `(*SKIP)` fired commits the search past position 4. This port re-tries it and returns `(4, 8)` from `search`. Forward direction, plain `search`, no `(?r)` and no multi-match operation, so it is the core semantics of the verb. It was not fixed because a hand trace of upstream's `FAILURE` advance (`:15695`-`:15738`, whose `text_pos < slice_start` clamp *is* ported, at `Matcher.cs:5334`) predicts upstream *should* match at 4 and it does not - and guessing in `basic_match`'s search-advance path risks 5394 passing tests for a cell no ported test covers. The 32 ported verb tests all pass, so the defect is invisible to upstream's own suite and was found only by the new `verbs` generator. **`verbs` is therefore kept out of `run-oracle.ps1`'s default list**, because a generator that is red for a known parked reason would turn every later slice's oracle run red and hide that slice's own result; the parameter's doc comment says to put it back the moment this is fixed. **The general rule: when a slice's central construct passes every ported test and fails the oracle, the ported suite was never evidence for that construct - park it with the reproduction rather than shipping the tag.**
- 2026-09-11 **`tools/run-oracle.ps1 -SkipRecord` silently ignores `-Rows`.** `-Rows` is consumed only inside the `if (-not $SkipRecord)` block, so `-Rows candidate.jsonl -SkipRecord` re-compares whatever wave is already at `TestResults/oracle/wave.jsonl` and reports a confident GREEN for it. It did exactly that here, against S28's leftover `conditionals` wave, and the only tell was the `waves:` line naming a generator and seed nobody had asked for. **Read the `waves:` line, not just the verdict** - it is the one part of the report that says which rows were actually compared.
- 2026-09-11 **S29's `(*SKIP)` divergence is upstream's required-string prefilter, not the verb, and the port's answer is the correct one.** `regex.compile(r"(?:..(*SKIP)x|q)x").search("ab cd xx")` is `None` but `.search(s, 4)` is `(4, 8)`; removing the required literal (`[xy]` for the final `x`) or planting an early `x` in the subject gives `(4, 8)` from 0; `..(*SKIP)xx` on `cd xxx` matches at 1, below the skip point, because a fixed `req_offset` lets `locate_required_string` (`_regex.c:11082`) jump past the attempt in which the verb would have fired. Same mechanism as upstream #612. The port deliberately has no prefilter until Phase 7, so this is an intentional divergence to record, not a fix to make - and it is the first concrete case for the strict allowlist ROADMAP specifies for Phase 6. Owner to decide whether that mechanism is pulled forward into S29.
- 2026-09-11 **Correction to the entry above, after a blind review and the decisive probe: the mechanism is the fixed-offset required-string jump, and it is not a bug on either side.** Upstream compiles `(?:..(*SKIP)x|q)x` with `req_offset=3, req_chars="x"` and `locate_required_string` therefore starts the first attempt at `found_pos - 3`, from which `(*SKIP)` steps past position 4. Perl does the identical thing (`rx_origin now 3` in its debug trace) and agrees with upstream on every probe; PCRE2 documents the class. Neutralising upstream's prefilter from Python (`req_offset=-1, req_chars=None`) turns every divergent answer into the port's, and 450 verb rows with no literal at all agree outright. The port is therefore right for an engine without a prefilter, and Phase 7's faithful port of `locate_required_string` will bring it into line with upstream. Until then the `verbs` generator is recorded against the prefilter-free upstream, tagged, with Phase 7 obliged to remove the wrapper. No upstream report. The blind review reported the claim falsified; its counter-cases were each explained by the optimiser trace, which is the reminder that a reviewer's verdict is a hypothesis too.
- 2026-09-11 **Second correction, and the one that closes S29: the required-string prefilter explains the *parked reproduction* and explains **none** of the four oracle-wave rows. There are two mechanisms, and they were filed as one.** The entry above predicted that recording `verbs` against a prefilter-free upstream would make the wave green. Tested before it was believed: the prefilter-free wrapper changes **0 rows of 3600** (seeds 7, 20260913, 4242, 1, 20260911, each row recorded both ways and compared), and the four divergences survive it unchanged. Their real cause is **upstream's `search_start`** (`_regex.c:8385`), the fast scan for the next plausible start position, which `basic_match` takes whenever the start test has a `search_start_*` twin (`:11819`). The two halves disagree about the slice: `search_start_END_OF_LINE_rev` (`:8055`) bounds itself with `text_end`, while `try_match_END_OF_LINE` (`:7108`) - the predicate `basic_match` itself consults - bounds itself with `slice_end`. Nothing but a `(*SKIP)` moves the slice inside an attempt, so the two agree on every pattern there is; once one does, upstream's fast path walks past a start position its own slow path would accept, and this port, which has only the slow path, tries it. Minimised to `regex.finditer(r"(?r)(?:a*(*SKIP)b|[^a-f])$", "\nb", regex.M)` - upstream one match, this port two - and confirmed by emulating the scanner in front of each attempt, which reproduces upstream exactly on every case including one where it must *not* lose upstream's second match. Isolating ladder: needs `(*SKIP)` (a `(*PRUNE)` agrees), needs `(?r)`, needs a multi-match operation, and needs `$` with MULTILINE - `\Z` and `$` without it agree, because `END_OF_STRING_LINE`'s two halves both use `text_end`. Same class as the prefilter and the same treatment: not a bug on either side, deferred to Phase 7, pinned by gap tests marked `PHASE 7: INVERT THIS TEST`, and `verbs` stays off the default generator list until `search_start` lands. **PORTMAP's two rows calling these prefilters "semantically transparent ... answers the same" are corrected: that is true only of patterns that cannot move the slice mid-attempt.** **The general rule: a root cause that explains the case you minimised is not thereby the cause of the rows you did not minimise - re-run the wave against the fix before you write down that it fixes the wave.**
- 2026-09-11 **A negative control's raw divergence count is meaningless when the honest engine already diverges on that wave; record the baseline beside it.** S29's controls were recorded as totals (A: 0 / 1 / 1). Measuring the same cached waves through the *unmutated* consumer gives a baseline of 0 / 3 / 0 / 1 at seeds 7 / 20260913 / 4242 / 314159, so control A's true delta is 0 / **-2** / +1 / 0 - at one seed its mutation *removes* two divergences, because widening the slice back to the whole text is exactly what neutralises the `search_start` asymmetry it happens to sit next to. A control that runs negative is not a thin control, it is a broken one, and no total could have shown it. S29-B (+48/+37/+36/+43) and S29-C (+225/+238/+220/+234) are unaffected; S29-D is +2/+1/0/0 and blind at two of four seeds. **Measure the baseline whenever the engine is not known-green on the generator you are controlling with, and record controls as before-and-after pairs.**
- 2026-09-11 **`state->group_call_guard_list` is write-only upstream, so S30 ports none of it and the "Phase 4 allocates it" hole in `MatchState.Create` is closed by deletion rather than by code.** `grep -n group_call_guard_list upstream/src/_regex.c` (2026-09-11) returns exactly six sites: the declaration (`:513`), the allocation and `memset` in `state_init_2` (`:18317-18323`), the reset in `reset_guards` (`:3398-3400`), the per-attempt clear in `basic_match` (`:11797-11803`), and two `re_dealloc` calls in `state_fini` (`:18718-18722`). Not one is a read - no `is_guarded` or `guard` call takes a list from this array, and the only local that aliases one is the clear at `:11800`, which zeroes it. It guards nothing in upstream as shipped. It is now a row in PORTMAP's "deliberately not ported" table with the grep and the date, and a `sync-upstream` check: if `group_call_guard_list` ever appears on the right of a comparison it becomes real state and `GROUP_CALL` has to consult it. **The consequence for slice planning: this also deleted one of the two negative controls S30's slice file specified ("the guard list never cleared"), because a mutation to state nothing reads cannot diverge. Before writing a control, check that the thing it mutates is read** - the same rule the 2026-09-11 S27 entry reached from the other direction.
- 2026-09-11 **`current` and the capture list are separate upstream, and a group call is the first construct in this port that can make them disagree.** `Match.GroupAt` returned an empty capture list for any group with `Current < 0`, which was invisible for three phases because every earlier construct moved the two together - `unsave_capture` decrements `count` as it clears `current`. `GROUP_RETURN` restores the caller's `current` (`pop_groups`, `:2662`) and deliberately leaves the callee's captures on the list, so after `regex.match(r'(?&routine)(?(DEFINE)(?<routine>.))', 'a')` upstream reports `group('routine') is None` **and** `captures('routine') == ['a']`: `match_get_group_by_index` (`:18847`) consults `current`, `match_get_captures_by_index` (`:19137`) walks `count` and never looks at it. Five of S30's sixty tests failed on this, and the fix is one line in `Match.cs`, not in the engine. **The general rule: when the last few tests of a slice all fail the same way and the engine trace is correct, look at the surface that reads the state, not at the code that wrote it.**
- 2026-09-11 **A `(?&name)` call inside a lookbehind matches here and fails upstream, and upstream contradicts itself twice about it - recorded as upstream's defect, next to issue 614, for Phase 6's sweep.** Minimal: `regex.compile(r'(?(DEFINE)(?<a>a))(?<=(?&a))c').match('ac', pos=1)` is `None`; this port gives `(1, 2)`, which is the answer "`a` precedes `c`" asks for. Upstream matches such a lookbehind only when it is the entire pattern. The two inconsistencies that place the defect upstream: its own `search` finds `(2, 3)` on `r'(?(DEFINE)(?<ab>ab))(?<=(?&ab))c'` / `'abcd'` with `captures('ab') == ['ab']` while `match(pos=2)` on that same compiled pattern returns `None`; and with the tail made optional, `c?` and `c*` both return `(1, 2)` - consuming the very `c` that `c`, `c+`, `[c]` and `(?:c)` refuse. Two explanations ruled out by measurement: **not our parser**, since this port's bytecode is upstream's code for code modulo one SET_UNION member's character order that `record-compile-corpus.py` sorts, and upstream's own dump carries the reversed copy of the called group (`CALL_REF 0, GROUP 0 1 1, CHARACTER_REV b, CHARACTER_REV a, END, END`); and **not the required-string prefilter**, since switching upstream's off the way `record-oracle.py` does for `verbs` leaves the answer `None`. Pinned in `Gaps/Engine/GroupCallTests.cs`; the `recursion` generator does not draw the shape, so one known upstream defect cannot colour the whole wave. **Nothing drafted or filed upstream - that needs the owner's approval first.**
- 2026-09-11 **A recursion generator must check every shape in *both* directions, because `(?r)` turns a right-recursive pattern into a left-recursive one.** `\w(?R)?` is safe as written and raises `MemoryError` from upstream the moment `(?r)` is prefixed: reversed, the call leads and the recursion never advances. It was caught before it reached the wave by running every candidate shape through upstream under both directions over a dozen subjects. Anything added to `RECURSION_SHAPES` gets the same check. Related, and the reason it matters more here than elsewhere: neither engine guards left recursion at all - upstream raises `MemoryError`, this port throws from `ByteStack.Grow`'s 1GB `RE_MEMORY_LIMIT` - so the failure is bounded and faithful, but it is a failure, and a generator that emits it produces a wave of errors rather than a wave of answers.
- 2026-09-11 **Widening a generator is not always the way to make a thin control fire; sometimes the answer is rows.** S30's control B (deleting `GROUP_CALL`'s repeat-guard clearing) found 0 of 600 at one seed and 4 at another. Four shapes with a repeat *inside* a called group took it to 5 and 2 - firing at both seeds. Four more shapes made it **worse**, 1 and 0, by diluting the draw, and were reverted. The path is genuinely rare - about one row in three hundred - so the control's count was raised to 2400, where it fires at all three seeds (10 / 5 / 8). **The general rule: when a widening moves a control the wrong way, the construct is rare rather than under-represented, and more rows beat more shapes. Measure after each widening and revert the ones that do not pay** - and delete `.scratch/control-waves/` first, or you will measure the generator you had before.
- 2026-09-12 **The ratchet's "passing" and "baseline" counts differ by design, and the tool now says so.** 107 ported tests have two identical `[Arguments]` rows because upstream's tables do (for example `test_repeat_minmax` repeats `^(\w){3,4}?$`), so they run twice under one id and the baseline, a set of ids, holds 108 fewer entries than the run has passing results. Verified from the TRX: 5462 passed results, 5354 distinct ids, 107 ids with two results. Not a hidden regression - `Test-Ratchet` reds on any failed result regardless of id - but it cost two slices' closing notes and an owner flag, so `check-ratchet.ps1` prints the distinct-id count beside the result count. The duplicate rows stay: they are upstream's, and deleting them would be editing the port's test data to please a tool.
- 2026-09-12 **`Matcher.TryMatch`'s reduction to upstream's default arm is transparent for a FAILURE and NOT for a PARTIAL, so S31 restored the partial half only.** Every caller stops on a negative status, so upstream stops inside the branch or repeat where this port carried on, entered it, and let the opcode raise the partial one step later - by which time an enclosing group had re-closed. `regex.search(r'(\D*?)z', 'a', partial=True)` leaves group 1 unset upstream and had it as (0,1) here. The one-character test nodes are now consulted through `MatchOne` and only a `PARTIAL` is propagated; `FAILURE` still reads as `SUCCESS`, so the Phase 7 fast path S19 measured and reverted (DECISIONS 2026-08-31) is not reinstated. Confined to partial matching by construction - `TryMatchOne` answers `PARTIAL` only when `partial_side` is set - and verified so: 51,000 non-partial rows and 1,980 non-partial `finditer` rows replayed against HEAD and the change with 0 differences.
- 2026-09-12 **`finditer` takes `partial` and `findall` does not, so `FuzzyRegex.Matches` gained the argument and `Count` did not.** The S31 slice file asserted that neither takes it and asked for the claim to be confirmed against `_main.py`; the confirmation failed. `_main.py:351` and `pattern_scanner`'s `kwlist` (`_regex.c:21089`) both carry it, `regex.finditer('abc', 'abc xab', partial=True)` yields `[(0,3) complete, (5,7) partial]`, and `regex.findall('abc', 'xab', partial=True)` raises `ValueError: unused keyword argument 'partial'` (all measured 2026-09-12, regex 2026.7.19). `scanner_search_or_match` builds a match for a `PARTIAL` status exactly as for `SUCCESS` (`:20898`) and ends the walk on the next turn (`:20886`), so the partial is yielded and is always last. The static convenience overloads still carry no `partial`, for the same reason they carry no `pos`/`endpos`.
- 2026-09-12 **Three partial-matching divergence families are pinned and parked rather than fixed, and the third one carries the measurement that says why.** (1) Upstream's unported `search_start` prefilter answers `RE_ERROR_PARTIAL` of its own (`:8402`, `:8411`, `:8662`-`:8960`): `regex.compile(r'(?r)\b$').search('', partial=True)` is `(0,0)` partial upstream and `None` here, while upstream's own `match` and `fullmatch` answer `None` - search-only is what identifies the cause. Same mechanism as S29's `verbs` rows; ~1 row in 2,000, so `partial` stays in the default generator list on the measured rate. (2) Half of upstream's partial arms are bounded by `slice_*` and half by `text_*` - `try_match_STRING` (`:7396`) against the `STRING` opcode's own arm - so they only part company once the slice is narrower than the subject; the new `partial-sliced` generator reaches that axis and finds 8 rows in 2,000 at seed 31, every one `(?r)`, and is deliberately off the default list. (3) A bounded lazy repeat loses its partial (`regex.match('ba??x', 'baa', partial=True)` is `(0,3)` upstream, `None` here). **S31 tried the obvious repair and reverted it:** asking the partial guard once more before the loop's limit break, where upstream's specialised arms ask before their own limit check (`:16545-16549`), fixes that family and breaks a larger one, because those arms also cap the limit per tail op (`min(limit, slice_end - 1)`, `:16543`). Measured by the second blind pass over 20,160 targeted rows: **125 rows fixed, 219 introduced**. All three need Phase 7 work the S31 slice file holds out of scope; all three are pinned in `Gaps/Engine/PartialMatchingTests.cs` and marked to invert.
- 2026-09-12 **The oracle row shape gained `pos`/`endpos`, recorded in UTF-16 with the untranslated pair beside them, and follows upstream's negative-index convention rather than clamping.** Upstream's `pos`/`endpos` are codepoint indices and this API's `beginning`/`length` are UTF-16 code units, so handing the consumer the raw pair would narrow a different slice on an astral subject - a divergence about encoding invented by the recorder. `regex` also wraps a negative index like a slice (`search('abc', 0, -1)` searches `[0, 2)`) rather than clamping it, and an `endpos` below `pos` must become an empty slice and not this API's `-1` "no limit" sentinel. All four cases were raised by the S31 second blind pass and are verified by an 8-row edge file covering reversed, negative, one-ended, out-of-range and astral slices.
- 2026-09-12 **`same_values` and `equivalent_nodes` are the required-string prefilter's, not POSIX's, and PORTMAP said otherwise for three phases.** They sit immediately before `save_best_match` in `_regex.c` and were bucketed with it by the 2026-09-01 symbol audit and then by S32's own slice file. Their one caller in the whole file is `:11771`, the `do_search_start` suppression in `basic_match`: a pattern whose required string *is* the start pair's test node needs no start-position scan. So they are Phase 7 with the rest of the prefilter, and S32 ported neither. The lesson generalises: **adjacency in `_regex.c` is not evidence of a family - grep the callers before believing a bucket**, because the same audit also promised the `RE_BestList` four to Phase 4 when they are `do_best_fuzzy_match`'s and stay Phase 5.
- 2026-09-12 **A generator that can emit a catastrophic-backtracking pattern is measuring the build configuration, not the port, and the fix belongs in the generator.** S32's first `posix` generator hung a quantifier on any piece, including pieces that already held one; seed 31 row 1407 was three quantifiers deep over a five-character subject and red the wave on the oracle's 10s row timeout. It was never a divergence - both engines answer four matches, the port in 1.1s and upstream in 0.69s optimised, and the port in 17.9s in the Debug build the consumer runs. POSIX is what exposed it, because it explores every remaining path instead of stopping at the first match. The rule now in `_posix_piece`: **a quantifier goes on a piece only if the piece holds none already.** Raising the row timeout would have been the wrong fix - it hides an exponential pattern rather than not generating one.
- 2026-09-12 **Reverse + a general repeat + a narrowed slice makes `fullmatch` diverge, and S32 pinned it without diagnosing it.** `regex.compile(r'(?r)(ab)+').fullmatch('xabz', 1, 3)` is `None` upstream and matches `(1, 3)` here. All three conditions are needed: `match` and `search` agree, a forward `(ab)+` agrees, `(?r)a+` (a `*_ONE` repeat) agrees, and the whole-subject slice agrees. Same slice-versus-text bounds family as S31's partial rows. Raised by the S32 blind review as an out-of-scope aside, reproduced independently before being acted on, and pinned in `Gaps/Engine/ReverseMatchingTests.cs` with all five measurements quoted. **Deliberately not fixed and deliberately not called upstream's bug**: upstream refusing to fullmatch `'ab'` against the slice that is exactly `'ab'` looks wrong, and that claim needs its own research and second opinion rather than a sentence in a slice that was about something else.
- 2026-09-12 **Owner rule: no known bug ships in this port.** Every bug identified with overwhelming evidence gets fixed before 1.0, whether ours or inherited from upstream; faithful reproduction of an upstream defect is not an exemption. Consequences: a divergence needs a verdict on which side is right (documented definition, a second engine's actual run, a blind review), never just "pinned and parked"; port wrong = fix now, test-first; upstream wrong = pin the port's answer as a permanent test, never inverted when Phase 7 ports the optimisation; Phase 7 ports upstream's prefilters without importing their answers, and a pinned port-is-right test changing is a regression; inherited bugs the port reproduces are Phase 6's fix list and reach 1.0 fixed. Design spec amendment 16.
- 2026-09-12 **The S29 and S31 parked divergences, judged against PCRE2 run directly** (`docs/plan/2026-09-12-divergence-research.md`, `tools/probes/pcre2-partial-and-skip.py`): the port is right on every `(*SKIP)` case (PCRE2 10.47 agrees with and without `NO_START_OPTIMIZE`; Perl's and upstream's answers are their optimisers', and upstream's `(1,5)` on `..(*SKIP)xx`/`cd xxx` retries below the committed position, a bug); right on `ba??x`/`baa` partial (PCRE2 None soft and hard - upstream's partial is a bug); right on `(?r)\b$`/empty search (upstream's own forward path and `match` agree); **wrong on the reversed empty-slice partial**, which S33 fixes. The S31 slice file's "the four rows must now agree" and every inverted in Phase 7 comment were the wrong call and are withdrawn. Two document-derived predictions from a research subagent (PCRE2 would say partial for `ba??x`/`baa` and for an empty subject) were refuted by running PCRE2: **run the binary when one is reachable; a derivation from its manual is a hypothesis.**
- 2026-09-12 **The reversed empty-slice partial was `try_match`'s six `try_match_STRING*` arms, and the reason only the reversed half diverged is that `text_end` and `slice_end` are the same number.** `do_match` sets both to `endpos` (`:18435`), so a forward arm bounded by `slice_end` (`try_match_STRING`, `:7396`) and the `STRING` opcode's own arm bounded by `text_end` (`:14737`) cannot disagree at the top level; `slice_start` is `pos` and `text_start` is always 0, so the reversed pair part company the moment `pos > 0`. Ported as `Matcher.IsStringTestPartial`, reduced to the partial answer only, on S31's terms. **It is an opcode-by-opcode answer and not a rule about `slice_start`:** `try_match_CHARACTER_REV` (`:7137`) keeps the `text_start` bound, so `(?r)a` on `'abc'[1:1]` is `None` in both engines while `(?r)a(bc)*` is a partial in both, and a second gap test pins six such rows so the fix cannot be "simplified" into one bound.
- 2026-09-12 **The reversed narrowed-slice `fullmatch` is upstream's bug, and the mechanism is that upstream has three `match_all` checks that do not agree.** `try_match`'s `RE_OP_SUCCESS` arm (`:7832`) bounds it by `text_start`; `basic_match`'s own `SUCCESS` opcode (`:15167`) and the search loop after a match (`:11880`) both bound it by `slice_start`. Only a GENERAL repeat consults `try_match` for its tail (`:13237`, `:13617`), which is why the symptom needs `(?r)` + `fullmatch` + `pos > 0` + a repeat whose body is not a single character, and why `(?r)a+` (a `*_ONE` repeat) is fine. **Two symptoms, one defect:** the match is lost outright (`(?r)(ab)+` fullmatching `'xabz'[1:3]`), or a complete zero-width match is reported as a partial (`(?r)([\p{L}\p{N}])??` under `F|I` fullmatching `'AAB'[2:2]`, where upstream's own `match` and `search` say complete). The second was the `partial-sliced` wave's row 756 at seed 31 and looked like a separate `\K` family. Port right, tests permanent, reported in `docs/plan/upstream-reports/LEDGER.md`.
- 2026-09-12 **`search_start` cannot be switched off from Python and `locate_required_string` can, which decides how a judged divergence is recorded.** `do_search_start` is set unconditionally at `:25732` and is not one of `_regex.compile`'s arguments; the only lever from outside is `req_string` happening to equal the start test node (`:11770`), which reaches a literal-prefixed pattern and nothing else. `req_offset`/`req_chars` ARE arguments, so `PREFILTER_FREE_GENERATORS` works for the required-string prefilter only. S33 added `partial-sliced` to it after finding that on a partial row that prefilter **suppresses** a partial rather than merely moving an attempt: `regex.compile(r'(?rimf)(x)[\p{L}\p{N}]{2,}?').match('a\na..A', 6, 6, partial=True)` is `None` plain and `((6,6), True)` prefilter-free, and the same pattern without the leading `(x)` - nothing for the prefilter to find - is `((6,6), True)` either way. So plain upstream answers according to whether the pattern happens to carry a required literal, which is a property of its optimiser and not of the language.
- 2026-09-12 **An oracle expected-divergence list needs a discriminator recorded per row, and the control that proves it must be written first.** S33's `ExpectedDivergences` classifies a judged divergence as `expected` so a generator can go back on the default list; the first version's `search-start-partial` predicate was "a search, asked with partial, where upstream answered a partial and this port answered none", which **silently ate a mutation that reverted S33's own fix** - control S33-A reported 0 divergences over 2,000 rows at three seeds. The repair is `searchOnlyPartial`, recorded by `tools/record-oracle.py`: `search_start` is consulted on a search and nowhere else, so upstream's own `match` denying the partial its `search` reported at the same position is the family's signature and the matcher's own partial is not. Verified on six prefilter rows (all deny) and the real-bug row (agrees). **Rule: an entry without a recorded discriminator is a silencer, and the way to find out which you have is to write the negative control before the entry.**
- 2026-09-12 **Run every oracle generator at three seeds before believing a wave; one seed is how four families stayed hidden.** S33's blind review ran the generators at seeds no earlier slice had used and found that a default wave is not reliably green and was not before S33 either: forward `(*SKIP)` in a bounded repeat (`regex.finditer(r'(?:[^\d](*SKIP)){2,3}', '\r\naabb ', regex.M, overlapped=True)` - the port matches more than upstream), a reversed group call recording a capture whose end precedes its start (`spans('g') == [(3, 2), (1, 3)]`), and the bounded-lazy-repeat partial family, which the `run-oracle.ps1` note had said no generator reaches. None is caused by S33 - each reproduces on `fb4e705` - and `verbs` therefore stayed off the default list, because a generator whose rows red every later slice's run for a reason that is not that slice's is the thing S29 held it out to avoid. A 2000-row generator run costs about fifteen seconds.
- 2026-09-12 **Phase 6 opens with the upstream sync and the bug sweep, and Phase 7 is gated on both** (owner decision; ROADMAP, spec amendment 17). Sync to releases, never head - the oracle must be a PyPI wheel - but diff release..head each time and port any unreleased engine fix with its own issue as ground truth. Measured the same day: pin 2026.8.12, upstream at 2026.9.10 = head, 21 commits, five releases, the four 611-614 fixes plus Python-API error propagation. Issue 614's fix means upstream now agrees with this port on S30's group-call-in-lookbehind row, which the strict divergence list must notice at the sync.
- 2026-09-12 **Two of the three divergences S33 left for S34 are bugs UPSTREAM HAS ALREADY FIXED, and the way to find that out was to fetch upstream's history rather than read the pinned submodule.** `git -C upstream fetch` from this session showed five releases past the 2026.8.12 pin. The reversed group call that records a capture whose end precedes its start is **issue 614** - `build_GROUP()` not propagating the match direction - fixed 2026-08-30 by commit `9398a6d`, one line, `subargs.forward = forward;`, released in 2026.8.30; upstream's own inline copy of the called body already gives this port's answer, `(2, 5)` where the call gives the impossible `(2, 1)` rendered as `''`. The forward `(*SKIP)` overlapped scan is the same class as **issue 613**, fixed the same day by `b77694a`, whose second half clamps `GREEDY_REPEAT_ONE`'s backtrack limit down to the current position because a stale slice had made the equality-only stop unreachable. **So the sync slice, not this one, is where either goes away** - and both are classified in `ExpectedDivergences` against the pinned release so the sync has to notice. Nothing about 613 covering S34's exact row is proven: installing the 2026.9.10 wheel to test it was not permitted in this session, and the project builds no C locally, so the draft upstream report for it is marked HOLD until the sync can check.
- 2026-09-12 **This port still carries upstream's pre-613 `GREEDY_REPEAT_ONE` backtrack code, and that is a known inherited defect with a name, not a suspicion.** `Matcher.cs`'s GreedyRepeatOne arm clamps the retreat limit up to `SliceStart` and then stops only on `pos == limit`, which is upstream's code at the pin, verbatim. Upstream added a second clamp because a `(*SKIP)`-moved slice can raise the limit above `pos` and let a repeat unmatch below its minimum. No wave row reaches it here - every overlapped `(*SKIP)` answer this port gives matches upstream's own per-position `match` - so it is not fixed in S34: porting an upstream fix test-first is the sync slice's job, and inventing the test for it here would guess at the trigger. Recorded so the sync ports it deliberately rather than diffing past it.
- 2026-09-12 **The oracle's expected-divergence list gained a per-row discriminator for scans, `anchoredScan`, and it is upstream's own answer rather than a rule.** For a `finditer` row whose pattern contains `(*SKIP)` - the only construct that moves the slice mid-attempt - the recorder also walks the same scan one match at a time from a fresh state and records every match in full, groups and captures included. A divergence is then classified only when upstream's stateful scanner disagrees with that walk AND this port agrees with it, rendering for rendering. This is the analogue of S33's `searchOnlyPartial` and it is what let `verbs` join the default generator list: the alternative predicate, "a `(*SKIP)` scan whose two answers differ", also classifies a real defect in this port's own scan. The walk records whole matches and not just spans because a carried slice shows in a CAPTURE as readily as in a span - at seed 314159 upstream reports group 1 as (0, 4) for a match spanning (1, 4), a capture outside its own match in a pattern with no lookaround.
- 2026-09-12 **An `ExpectedDivergences` example is frozen recorder output, so the staleness alarm cannot see UPSTREAM's answer change - only this port's. The sync must re-record every example.** `Every_expected_divergence_still_diverges` runs each example through the live engine and compares against the recorded upstream answer, which is what catches a port-side regression. It is blind to upstream fixing its side, because the recorded half never moves. Two entries are already waiting for exactly that. Written into the class remarks as a procedure, and the Phase 6 sync is its first test.
- 2026-09-12 **A wave counts at three seeds, and the default count of 300 rows per generator is not enough either.** `run-oracle.ps1` now takes `-Seeds` (comma-separated, because `pwsh -File` hands every argument over as a string and a real `[int[]]` binds the second seed to the next parameter) and defaults to 7, 4242 and the run's date, running each in turn and keeping a red seed's wave and report under its own name. The default list, all twenty generators including `verbs`, is green at those three seeds. At `-Count 2000` the same three seeds turn up one more row, and it is **a crash both engines share**: `regex.compile('(?r)^İﬁ', regex.I | regex.F)` raises `IndexError: tuple index out of range` from `String.get_firstset` (`_regex_core.py:4036`) on an empty `String` node, and this port raises `IndexOutOfRangeException` from the ported `String.GetFirstset` (`Nodes.cs:2099`) for the same reason. Not fixed in S34 - it is a new inherited defect with no upstream answer to port, so it is drafted upstream as report 6 and handed to Phase 6's issue sweep. **Read this as the rule generalising: a green wave is green at the count and seeds it ran, and nothing more.**
- 2026-09-12 **No upstream report is filed until absolutely everything else in the plan is done; the drafts became `docs/plan/upstream-reports/LEDGER.md`.** Owner decision: the behaviour had not yet been checked thoroughly enough to convince them. The ledger keeps each defect re-verifiable (reproduction, mechanism, second engine, port's answer) and is re-checked at every upstream sync; filing is Phase 8's last step, text approved by the owner. Re-verified the same day against 2026.9.10 in `.venvs/regex-2026.9.10`: every entry reproduces unchanged except the reversed group-call span (614's fix, now the port's answer); the `(?<=(?&a))c` row S30 pinned is **not** fixed by 614.
- 2026-09-12 **The eighteen Phase 4 commits carrying a `Co-Authored-By: Claude` trailer were rewritten (`git filter-branch --msg-filter`, trees byte-identical, verified) and `main` force-pushed by the owner.** The trailer came from a harness attribution reminder that the session followed over the owner's standing instruction; the `port-slice` skill now says to ignore such reminders, and the owner's CLAUDE.md already forbade it. Never again.
- 2026-09-12 **Rider's "anonymous function can be made static" cannot be enforced by Roslyn's IDE0320 in `dotnet build` on SDK 10.0.400, measured.** A minimal project with the documented option (`csharp_prefer_static_anonymous_function`, default true) and `dotnet_diagnostic.IDE0320.severity = warning`, plus every variant (`true:warning`, `EnableCodeStyleSeverity`, `AnalysisLevelStyle=latest-all`/`preview`), reports nothing on the textbook `Use(x => x + 1)`, while IDE0055 and IDE0022 fire in the same build and `ReportAnalyzer` shows the IDE0320 analyzer executing in under a millisecond; `dotnet format style --diagnostics IDE0320` finds nothing either. Roslyn 5.9.0-1.26379 CodeStyle. So the build gate for that rule is ReSharper's own engine, `jb inspectcode`, in CI and in `tools/check-inspections.ps1`, with the IDE0320 lines kept in `.editorconfig` for the day Roslyn reports it. CA1822 does enforce in the build and deliberately exempts test methods; TUnit0048 forbids static `[Test]` methods (a build error, verified), so the Rider "member can be made static" findings on test methods are disapplied for `tests/`.
- 2026-09-12 **401 non-capturing lambdas across 88 files made `static`, from ReSharper's inspection report, and the gate is `tools/check-inspections.ps1` in its own CI job.** The sites came from `jb inspectcode` (Roslyn IDE0320 running inside ReSharper's host, which does report it), applied by offset with a matcher that refused any range not starting on a lambda or delegate - one of 401 needed a hand fix, a discard-parameter lambda whose range began at `=>`. Ratchet GREEN before and after, 5578 passing either way. The ~465 WARNING-level ReSharper findings (mostly `RedundantExplicitArrayCreation`, 375) were not asked for and are untouched; `check-inspections.ps1 -Severity WARNING` lists them.
- 2026-09-12 **S35 added before the phase close** for the two port bugs still unscheduled: the case-H `$`-after-`(*SKIP)` reading a moved slice bound (independent verifier's reversal), and the `(?r)^İﬁ` I|F crash both engines share (S34's 2000-row wave; still crashes on 2026.9.10). Close is S36. `regex` 2026.9.10 lives in `.venvs/regex-2026.9.10` (gitignored) because slice sessions cannot `pip install` - 26 allowlist denials in S34's log - but can run a Python that is already there.
- 2026-09-12 **S29's `search_start_END_OF_LINE_rev` verdict is REVERSED: the port was wrong, and `TryMatchEndOfLine` now reads `TextEnd` rather than `SliceEnd`.** Upstream's fast and slow paths do disagree about the bound, exactly as S29 measured - but `$` under MULTILINE is true at the end of the text and before a newline and nowhere else, so on `"\nb"` at position 1, where the next character is `b`, it is false whichever bound the code reads. It is the slow path that is wrong, on both sides, and this port had only the slow path. **A verb moves where the next attempt starts and nothing else** (PCRE2 pcre2pattern, "Verbs that act after backtracking"), so no assertion may read a bound a `(*SKIP)` has moved; every other zero-width assertion here already read the text bound, and the blind review re-checked all of them. The whole `search-start-skip-slice` family stopped diverging and its `ExpectedDivergences` entry is deleted - **the first time the staleness alarm has removed an entry rather than defended one, which is the argument for keeping the list strict.** Two rows of the family survived as a different mechanism, `overlapped-skip-stale-slice-reversed`.
- 2026-09-12 **`Sequence._fix_full_casefold` slices the unfolded run with folded offsets, and this port maps the chunk START back to a character boundary while leaving the END drifted.** Upstream's arithmetic works out for one expansion and fails for two: `ﬁaﬁ` stops matching `fiafi`, `ﬀaﬃ` stops matching `ffaffi`, and `(?r)^İﬁ` builds an empty `String` and crashes in `get_firstset`. Leaving the end alone is deliberate and measured - it drifts the same way, so it only ever takes in trailing characters that did not need the full fold, and doing so **keeps the emitted bytecode bit-identical to upstream's on every pattern upstream gets right**, where a both-ends mapping splits compile-parity corpus rows #323 and #333 differently for no behavioural gain. It costs a `pos >= characters.Count` guard, which the first attempt lacked: the blind review found 1,764 crashing runs of four characters that both minimised tests passed, and the sweep test over all 22,620 short runs is the guard against a third route.
- 2026-09-12 **The differential oracle cannot detect an inherited bug, demonstrated rather than asserted.** S35 wrote the negative control for the case-folding fix, reverted the fix, and ran `case-folding` at 600 rows on three seeds: **0 divergences**, because reverting it makes this port agree with upstream, which has the same bug. The control was deleted rather than kept, since one that can never fire is noise in every future run. The generator rows S35's slice file asked for were written and reverted for the mirror reason: after the fix they find 34 divergences of 600 at seed 7, every one of them upstream's bug, which would red the default wave permanently and need a classification entry whose only job is to hide it. **A gap test in the ordinary suite is the stronger and cheaper alarm for a bug both engines share**; the oracle's job is finding ours. ROADMAP's Phase 6 entry already said this in the abstract; these are the numbers.
- 2026-09-12 **Ledger entry 7: `İ` (U+0130) never reaches the full fold on either side, and it is left open on purpose.** `_fix_full_casefold` builds its expansion inventory with `fold_case` alone while the text it looks in is `fold_case(...).lower()`, and U+0130 is the one character the two disagree about, so no chunk is ever marked for it and `İ` does not match `i̇`. Lowering the inventory is one word and half-works: the matcher's `STRING_FLD` folds with the same `fold_case`, so U+0130 would have to expand there too - a change to the folding tables and to every construct that reads them. **This is the only known bug S35 identified and did not fix**; under the no-known-bugs rule it needs a slice before 1.0, and whether Phase 6's sweep takes it is an owner decision.
- 2026-09-12 **The three `verbs` rows S35 left are all upstream's, and the port is right on all three - including the one the S36 slice file called a port bug.** Row 1567's damning evidence was that this port's own `search(0, 14)` finds the match its overlapped scan does not; but `search(0, 14)` truncates the subject, which is what makes `$` true there, and inside the scan the character at 14 is an `A`. Minimised to `regex.finditer(r'(?r)(?:.{2}(*SKIP)A|x)$', 'bxA', regex.M, overlapped=True)` = `[(0, 3), (1, 2)]` against this port's `[(0, 3)]`, with two controls - delete the verb, or make it `(*PRUNE)` - each leaving upstream with `[(0, 3)]` alone. So it is S35's defect seen through a scan, and the strongest evidence yet that S35's reversal of S29 was right. **Row 1439 does NOT depend on call order**, which is what the slice file suspected from the wave and the isolated run disagreeing: `verbs` is recorded with `locate_required_string` neutralised and the isolated run was not. Recompiled prefilter-free it is deterministic. The recorder therefore needs no per-row isolation, and that non-change is recorded as loudly as a change would have been, because "upstream's answer depends on call order" would have been a standing hazard for every later wave.
- 2026-09-12 **`reverse-group-call-direction` is renamed `group-call-direction` and widened, because issue 614 is about the DIRECTION and not about `(?r)`.** A called group runs the wrong way round whenever the lookaround containing the call runs the other way from the pattern, so a LOOKBEHIND in an ordinary forward pattern has it too: `regex.compile(r'(?P<g1>A*)(?<=(?&g1))').finditer('A')` records g1's second capture at `(2, 1)` on a one-character subject - a start past the end, an end before its own start - where 2026.9.10 and this port both answer `(0, 1)`. Found by the composed `interactions` wave at seed 7, which is the first generator to put a call inside a lookaround at all. **The same row broke the recorder**: `_to_index_length` indexed a two-entry codepoint table with 2 and raised `IndexError`, so one upstream bug failed a whole 2000-row wave. `_utf16_index` now EXTENDS an out-of-subject index rather than clamping it - one unit per codepoint past the end, a negative index kept - so an impossible span stays impossible on this side and is reported rather than quietly made plausible; the recorder's `--self-check` gained two cases, watched failing against a clamping version.
- 2026-09-12 **A row count is a seed by another name, and the `interactions` widening is bounded at 2000 rows rather than claimed green at 6000.** The composed generator is green at the default 300 and at 2000 on three seeds, and RED at 6000 on five - 13 rows, every one of them in a family already judged: 9 are S30's group call inside an opposite-direction lookaround, 3 are `search_start`'s own partial arms in a shape `search-start-partial`'s predicate does not reach, and 1 is a new `bounded-lazy-repeat-partial` row. Classifying them is a slice's work and is handed on rather than rushed. **Two shortcuts were tried, measured and reverted**: recording `interactions` prefilter-free removed one diverging row and introduced another (those three rows are `search_start`, which Python cannot reach, not `locate_required_string`, which it can); and retargeting five thin controls at the composed generator made all five fire at every seed until the unmutated baseline was measured, at which point every figure was at or below it.
- 2026-09-12 **Phase 4 closes at ten slices and eleven sessions, 1.1 against Phase 3's 1.0, with a median of 50.9M tokens.** The three slices beyond the seven authored were not scope creep and not features: each came from verification getting stricter (act on the divergence research, make three seeds the floor, fix what the wider wave then exposed) and each found a real defect. `run-controls.py` gained `--seeds N` because a control accumulates seeds and a mutated engine is often slow as well as wrong - S28-C's 2400-row wave takes ten minutes under its own mutation against 44 seconds clean. Of the 24 Phase 4 controls re-run at their recorded seed and at a fresh 99991, 20 are healthy and 4 are thin or dead (S28-E 0/0, S29-A 0/1, S29-D 2/0, S35-A 0/0) - and all four were already recorded as weak by the slice that wrote them, so the re-run corroborated the notes rather than surprising anyone.
- 2026-09-12 **`ENHANCEMATCH`'s dead `same_match` check is not ported as live code** (S41). Upstream computes `same_match` and overrides it to `FALSE` on the next line (`_regex.c:17942-17943`), so its `same_span_of_group` loop never runs. Bisected against PyPI source releases: 2014.12.24 and 2015.09.28 had the check live (`if (same) break;`) in one combined best/enhanced loop; 2015.11.5 (Hg issue 165, "Performance / hung search") split the loop into `do_simple`/`do_enhanced`/`do_best` and introduced `same_match` already overridden. The check is only an earlier exit: honouring it can never find a match the current code misses and can lose one, and the override costs exactly one failing run per enhanced match on an already-narrowed slice. Verdict: deliberate, not a bug. Port the effective behaviour, keep the dead lines as a quoted comment, and put `same_span_of_group` in PORTMAP's deliberately-not-ported table. S41 measures it (honoured check behind a temporary switch, wave both ways, runs counted).
- 2026-09-12 **`BESTMATCH` and `ENHANCEMATCH` rank by cost in this port, ties by fewer errors, then earliest** (owner decision; S41, S42). Upstream ranks by error count only (`total_errors`, never `total_cost`), which is open issue 470: `(?b)(voices){1i+1d+2s<=2}` on `voixes voicees` returns the cost-2 substitution over the cost-1 insertion because both are one error - verified on 2026.9.10, `tools/probes/upstream-bestmatch-cost-ranking.py`. Evidence it is a regression: releases up to 2015.09.28 ranked by cost (`state->max_cost = state->total_cost - 1`) and the 2015.11.5 rework replaced `max_cost` with `max_errors` throughout; TRE/agrep, the only other engine with per-type costs, defines best as lowest cost; upstream's docs define costs but never define "best"; its one weighted `(?b)` test answers the same under both rules. With unit costs the rules agree, so no ported test changes; the divergence is confined to cost equations under `(?b)`/`(?e)`, pinned by gap tests and a strict `ExpectedDivergences` predicate, and ledgered (nothing filed). Procedure in both slices: port upstream's rule first and get the tag green, then switch through one shared helper.
- 2026-09-12 **Phase 5 authored as S37-S43** and approved by the owner the same day; the split and its reasons are in ROADMAP's Phase 5 note.
- 2026-09-12 **`interactions` is green at 6000 rows on four seeds, and S36's thirteen rows were three families, not the three it named.** S36's table said 9 group-call rows, 3 `(*SKIP)`-plus-partial and 1 `bounded-lazy-repeat-partial`. At the three default seeds the rows are 4 group-call, 3 partial and **one reversed overlapped `(*SKIP)` extra match** - no bounded-lazy row appeared at all - which is what a different seed set does to a per-family count and why the families rather than the counts are what was carried forward. Three changes closed them: a new entry `group-call-loses-the-match`; `search-start-partial` widened to the arm where this port answers its OWN partial elsewhere; and `CarriesACaptureOutsideItself` narrowed from "any lookaround" to "any POSITIVE lookaround". None needed engine work, which is what the slice predicted.
- 2026-09-12 **A negative lookaround cannot put a capture outside a match, so only the positive forms and `\K` are excluded from the capture tell.** `regex.search(r'(a)(?!(?:(b))x)b', 'ab')` is (0, 2) with group 1 `['a']` and group 2 EMPTY, and this port agrees; a negative lookaround only succeeds when its body fails, so nothing it matched survives. The old clause excluded `(?!` and `(?<!` as well and kept one real row of `overlapped-skip-extra-match-reversed` out of its own entry for no reason. `tools/probes/upstream-negative-lookaround-captures.py`.
- 2026-09-12 **Upstream loses a match when a group call sits inside a lookaround running the other way, and the judgement needs no second engine: deleting the piece that holds the call, which in every judged row can match zero-width, gives upstream the match it refused.** Five rows in four outcome shapes, all unchanged on 2026.9.10, so issue 614's fix does not cover it (ledger entry 8). **The minimal form is NOT established and the entry says so**: every shrink that kept upstream self-contradictory landed on a pattern this port answers the same way upstream does, so "a call through an opposite-direction lookaround" is necessary and not sufficient, and the rows are listed whole.
- 2026-09-12 **`search-start-partial`'s second symptom is keyed on ROWS, not on a predicate, and the blind review is why.** The first draft asked only that UPSTREAM's partial cover the whole searched region - genuinely `search_start`'s fingerprint - and put no condition on this port's answer beyond "a partial, rendered differently". One added line in `Matcher.cs`'s partial block (`state.MatchPos = state.Reverse ? state.MatchPos + 1 : state.MatchPos - 1;`) then had the entry's own minimised row classified EXPECTED with the port answering (3, 1) instead of (4, 0). Row-keyed it is `expected 0 diverge 4` mutated and `expected 4 diverge 0` clean. **The general rule this is the second instance of** (after `bounded-lazy-repeat-partial`): when a family's discriminator is a fact about UPSTREAM alone, a predicate cannot be strict, because strictness in this list comes from pinning what THIS PORT says.
- 2026-09-12 **`CarriesACaptureOutsideItself` reads the pattern with every space removed, because `(?x)(?<  =(a))` is a positive lookbehind.** Upstream reads the character after `(?<` with `source.get()` rather than `get(True)` (`_regex_core.py:863`), so VERBOSE whitespace is skipped there and the spaced spelling captures outside the match exactly as `(?<=` does. The blind review built the two rows, applied control S37-C and watched the spaced one classified while its twin was reported. Stripping is conservative in the safe direction: it can only add an exclusion, and an exclusion only reddens the run.
- 2026-09-12 **A fourth seed at 6000 rows found a row three seeds did not, and the predicate's deliberate gap is what caught it.** `UpstreamFoundStrictlyLess` was written with no `split` arm, on the rule that a shape no row has shown should red the run rather than fall into an untested predicate; seed 99991 then drew a `split` row of exactly that family. This is evidence for the VERIFICATION rule 7a floor rather than a new rule: three seeds stays the floor, and a slice widening a generator should say which seeds it used.
- 2026-09-13 **`match_fuzzy_changes` reports only the first `Total` entries of the change list, not the whole list.** Upstream's loop is `for (i = 0; i < count; i++)` where `count` is the sum of the three fuzzy COUNTS (`_regex.c:20522`), not the length of the array `pattern_new_match` copied (`:20761-20770`). A nested fuzzy section leaves the two out of step - `END_FUZZY` recomputes the counts and does not always unwind the change list to match - so the counts and the changes can genuinely disagree about which KIND of error happened, and upstream reports them disagreeing. Reading the whole list instead put an extra change on four rows across the three default seeds of S38's `fuzzy` wave. Upstream has no bound check there at all, so its `count` can exceed its own array; the port clamps. Pinned by `FuzzyMatchingTests.Only_as_many_changes_are_reported_as_the_counts_say_even_when_more_were_recorded`.
- 2026-09-13 **`start_match`'s fuzzy-counts clear (`_regex.c:11790-11792`) is load-bearing, and only a backtracking verb makes it visible.** Ordinarily the `FUZZY` backtrack arm has already restored the counts by the time `FAILURE` jumps back to `start_match`, so the clear looks redundant - which is why S38's first draft left it out. `(*PRUNE)` and `(*SKIP)` cut the backtracking and drop the fuzzy frames instead of unwinding them, and then the next start position begins with the abandoned attempt's errors: `regex.search(r'(?:[ab][bc](*PRUNE)[wx]){e<=2}', 'qab')` is `(1, 3) (0, 0, 1)` upstream and was `(2, 0, 1)` here. Found by S38's blind review; the `fuzzy` generator emits no verbs and could not have.
- 2026-09-13 **`state->fuzzy_guards` is never to be ported, on the same evidence as `group_call_guard_list`.** `grep -n fuzzy_guards upstream/src/_regex.c` (2026-09-13) gives a declaration, a null, two `reset_guard_list` calls in `reset_guards`, an allocation, a `memset` and the frees - twelve lines, no read. So a fuzzy section's body and tail guards guard nothing. The holes S16 left in `MatchState.Create` and `ResetGuards` said "waits for Phase 5"; they now say "never", with the grep and the date. `RE_FuzzyData.limit` is the same shape (written at `:10203` and `:10206`, read nowhere) and is left out of `Engine.FuzzyData`.
- 2026-09-13 **A fuzzy section of two adjacent literals is a fuzzy STRING, so `(?:foo){e<=1}` is not in the one-character subset.** `Sequence.pack_characters` (`_regex_core.py:3526`) packs a run of two or more positive, non-zero-width `Character` items into one `STRING` node; anything else - a class, a property, a dot, an assertion - flushes the run. S38's first draft of both the gap tests and the oracle generator used word-like literals throughout and every row hit the STRING seam. Both were rewritten to keep literals apart, and the rule is written at the top of `FuzzyMatchingTests` and beside `FUZZY_ATOMS` in `tools/record-oracle.py`.
- 2026-09-13 **`any_error_permitted` cannot be shown to matter in the simple fuzzy path, and that is a fact about the predicate rather than about the generator.** Its cost test is strictly weaker than `this_error_permitted`'s (`cost + this_cost <= MAX_COST`, with every per-error cost at least 1) and its `error_count < max_errors` test is dead where `do_simple_fuzzy_match` sets `max_errors` to `PY_SSIZE_T_MAX`. Two mutations of it - relaxing the cost gate by one and tightening it to `<` - found nothing at either seed; only "the predicate is consulted at all" is observable, which is what control S38-A now measures (282 and 265 of 600). Expect the `max_errors` term to come alive in S41 and S42.
- 2026-09-13 **A `stackalloc` inside a `switch` case inside the matcher's loops is unbounded stack growth, and CA2014 does not see it.** Stackalloc memory lives until the method returns, so one per backtrack step accumulates. S38's first draft had three inside the `END_FUZZY` cases; the analyzer passed them and fired only on a control mutation that put one directly in a case. They are now declared once beside `folded`/`gfolded` at the top of `BasicMatch`, which is the convention that was already there. The rule for the engine is: no `stackalloc` below the `advance:`/`backtrack:` labels, whatever the analyzer says.
- 2026-09-13 **A control that fires at no seed is a finding, and the finding has to be reproduced by hand before it is believed.** S38's control E (the nested section inheriting the outer counts) is silent at both seeds because the generator's sections are too short for the outer one to have spent an error before the inner is entered - not because the fault is unobservable. `tools/probes/fuzzy-nested-rows.py` builds 24 rows that do reach it: 24 agree on the honest engine, 3 agree and 21 diverge with the mutation. The shapes are pinned as a test, and the control stays in `controls.json` with its zero recorded, because a zero with an explanation is evidence and a zero without one is a gap.
- 2026-09-13 **The fuzzy branches of both `*_REPEAT_ONE` backtrack arms are unreachable, so S39 ports neither.** `sequence_matches_one` (`_regex.c:24056`) refuses a `REPEAT_ONE` whose body node carries `RE_STATUS_FUZZY`, and every op `node_matches_one_character` (`:3433`) accepts comes from a parser class that sets `FUZZY_OP` inside a section (`Any`, `Character`, `Property`, `Range`, `SetBase`, `SetUnion`, `ZeroWidthBase`), so no `REPEAT_ONE` exists inside one; outside one, the node after the repeat is `FUZZY` or `FUZZY_EXT`, which `Fuzzy._compile` emits with `REVERSE_OP` only, and `can_test_past` (`:23697`) walks past neither. Measured over all 1,534 compile-parity patterns: 639 `REPEAT_ONE` nodes, none with a fuzzy test node. The greedy branch would have been a copy of its own default arm anyway. `RepeatTests.No_repeat_one_node_in_the_corpus_has_a_fuzzy_test_node` is the guard, not the claim: it reds if a sync changes any of the three facts. Found because the two gap tests written for those loops passed before S39 wrote a line.
- 2026-09-13 **`string_pos` is an index into a node's values for a `STRING*` and a subject position for a `REF_GROUP*`, and this port has to say which.** Upstream's `fuzzy_match_string` moves it with `data->new_string_pos += step` for both, because a C position is a codepoint count either way; here one moves by `+ step` and the other by `Matcher.Step`, so `FuzzyData.StringPosIsText` exists with no upstream counterpart. The S21 comment at `REF_GROUP`'s own walk predicted exactly this and kept `NextPos` for it.
- 2026-09-13 **S35's `_fix_full_casefold` divergence is reachable by an oracle wave, and the `fuzzy` generator deliberately stays off it.** Two expanding folds in one literal run make upstream drop the `FULLIGNORECASE` flag from the second chunk - `(?fi)ßaß` stops matching `'ssass'` there and matches here - and S39's widened generator is the first instrument to draw the shape: 22 divergences over three 2000-row seeds, every one this family. It is a COMPILE-time difference both sides' own tests already pin, and no predicate over the two answers is narrow enough for `ExpectedDivergences` because upstream compiles a genuinely different pattern. So the generator draws at most one expanding fold per section and **Phase 6 decides whether the manifest gets an entry**; hiding it behind a broad predicate would have blinded the wave to the matcher arms S39 ports.
- 2026-09-13 **An unbounded `{e}` budget is only safe over one-character atoms.** Next to a string, a fold, a repeat or a backreference it makes upstream raise `MemoryError`, which kills the recorder and so the whole wave - `regex.sub(r'(abx)(?:\W[a-f](?:[ab]+\1){e}){s<=2}', '<>', 'abx.dabx')` and `regex.split(r'(?fi)(?:straße\d\A){e}', 'sTrasßE0')` on 2026.7.19, the same family as upstream issues 551 and 554. `FUZZY_BOUNDED_CONSTRAINTS` is what a section with a multi-character item draws from; the generator stays off the shape rather than the recorder learning to survive it.
- 2026-09-13 **`tools/run-controls.py` caches waves by generator, count and seed, and the key does not know the generator changed.** S39's first control run read zero for all six controls at seed 7 and non-zero at the new seed, because `.scratch/control-waves/fuzzy-600-7.jsonl` was still S38's wave - a generator with no strings, folds or backreferences in it. **Delete the cached wave for any generator you have widened**, or the controls measure a generator that no longer exists. This is the same trap the port-slice skill records for figures recorded mid-slice, arriving by a different door.
- 2026-09-13 **A `needs:` tag names the capability a test waits on, not the feature it is about.** With S40's `{...:test}` constraint ported, 31 test cases tagged `fuzzy-matching`, `fuzzy-budget`, `fuzzy-counts` or `fuzzy-changes` were still red, and **all 31 failed at the engine's own `ENHANCEMATCH` or `BESTMATCH` seam rather than on an assertion** - their patterns carry `(?e)`, `(?b)`, `(?be)` or `FuzzyRegexOptions.BestMatch`. They are retagged to the two capabilities that actually gate them, and eight upstream methods mixing plain and ranking rows in one `[Arguments]` fan-out were split so a plain row is not skipped to keep a `(?e)` sibling company. The slice rule against retagging a red test is against retagging *instead of* analysing it; the analysis here is the seam's own exception message, which is mechanical. Design spec amendment 18, and the ROADMAP's Phase 5 paragraph.
- 2026-09-13 **`fuzzy_ext_match` and `fuzzy_ext_match_group_fld` are asymmetric in upstream, and both asymmetries are ported as written.** `fuzzy_ext_match` (`_regex.c:9938`) lists `SET_*` and `SET_*_IGN` but no `SET_*_REV` or `SET_*_IGN_REV`; `fuzzy_ext_match_group_fld` (`:10033`) lists no `SET_*_IGN` either. An opcode with no arm falls off the end of the switch to `return TRUE`, so a reversed set test - and, inside a case folding, any set test at all - constrains nothing where the same test forward would bite. Measured on regex 2026.7.19: `(?<=(?:[ab][cd]){e<=1:[0-9x]})$` against `'azc'` substitutes where `{e<=1:x}` has to delete instead. `.` is a no-op everywhere for the same reason, ANY having no arm at all. `Gaps/Engine/FuzzyTestConstraintTests.cs` pins each one; whether upstream should be told is Phase 6's, not this slice's.
- 2026-09-13 **A fuzzy test accepts only a character set, which makes three ports of upstream code unreachable and one V1-only.** `{e<=1:(?-i:x)}` and `{e<=1:(?i)x}` are both `error: expected character set`, so a test compiled under `(?fi)` is always an `_IGN` opcode and `fuzzy_ext_match_group_fld`'s non-`_IGN` arms cannot run; they are ported because upstream writes them. `SET_DIFF`, `SET_INTER` and `SET_SYM_DIFF` need the set-operator syntax, which needs `(?V1)`, so the oracle generator - which emits V0 patterns - cannot reach them and `FuzzyTestConstraintTests.The_other_three_set_opcodes_reach_the_same_arm` covers them directly instead.
- 2026-09-13 **A control can be blind because no *test* in the pool discriminates, not because the code path is cold.** S40's control E replaces `folded_char_at`'s `folded_pos` with a literal `0` and read 0 of 2000 at both seeds three times running. The path was live all along - inverting the same arm's verdict diverged 1 and 10 rows - but the foldings the generator produces are `ß`->"ss" and `ﬀ`->"ff", two identical characters, and every one of the 15 tests in the pool answered the same for both halves of every folding. `FUZZY_SPLIT_TESTS` exists for that: `[a-s]` splits "st", `[a-f]` splits "fi". **When a control reads zero, ask whether the instrument can tell the two answers apart before concluding the shape is unreachable** - that is a third failure mode alongside "the generator never draws the shape" (S38's control E) and "the cached wave is the old generator" (S39's).
- 2026-09-13 **A row upstream never finishes is a recorded outcome, not a dead wave.** `tools/record-oracle.py` gives every upstream call `timeout=10` and records a miss as a kind of its own, `{"kind": "timeout", "seconds": N}`; the consumer reads it as `TimeoutOutcome`, skips the row without asking this port - there is no ground truth to compare against, and asking would spend `OracleComparer.RowTimeout` per row for nothing - and tallies it under `OracleVerdict.Timeout` so the report's summary line shows it. **A kind of its own rather than an `error`**, because upstream did not reject the pattern: filing it as a rejection would score a port that answers as diverging and a port that also hangs as agreeing, which is both halves of the comparison backwards. Ten seconds to match `RowTimeout` - neither engine gets longer than the other to answer the same question - and the recorded value is that CONSTANT, never an elapsed time, which is what keeps `--verify-determinism` sound. It fires for real: 6000 rows a generator at seed 4242 records `timeout 1`.
- 2026-09-13 **The slice goes back to what the caller asked for at the start of every match, and that is a deliberate departure from upstream's code.** `(*SKIP)` moves `slice_start`/`slice_end` mid-attempt (`_regex.c:14553`) and upstream restores them nowhere, so one scanner state carried a moved slice into the next match - upstream's own bug, ledger entry 5, whose proposed fix is exactly this reset. This port could not simply inherit it: with no `search_start` prefilter until Phase 7 it reaches the carried slice on shapes upstream's optimiser skips past, and there it LOSES MATCHES rather than merely moving them (`finditer(r'\b(?:[^a](*SKIP))*', 'b\n\rS', overlapped=True)` was three matches here and four upstream, and this port's own `Match` at the missing position found it). `Matcher.DoMatch` resets from `MatchState.InitialSliceStart`/`InitialSliceEnd`, and **`FuzzyRegex.NewMatch` records those too** - handing `Match.NextMatch` the moved slice reintroduced the defect on that one path, which S40a's blind review caught with a scan and a walk disagreeing.
- 2026-09-13 **A fuzzy match can report changes that contradict its own counts, and that is upstream's, not ours.** `basic_match`'s `start_match` clears `fuzzy_counts` and leaves `fuzzy_changes` (`:11790`), and `match_fuzzy_changes` reports the first `sum(counts)` entries (`:20522`), so a change recorded by an abandoned attempt DISPLACES the winning attempt's. Upstream's own `search(r'(?:[ab][bc](*PRUNE)[wx]){e<=2}', 'qab')` is `counts=(0,0,1)` with a SUBSTITUTION at 0, unchanged on 2026.9.10, and S38 had already pinned both rows. **The one-line fix - clearing the list beside the counts - was made, measured and reverted**: it turns those pinned rows red and makes this port diverge where it currently agrees. Ledger entry 11; an inherited bug, so by the owner's 2026-09-12 rule it is fixed here before 1.0, in Phase 6's sweep beside entry 7. The lookbehind rows S40's review read as a port bug differ only because `$` lets upstream's prefilter make one attempt where this port makes four.
- 2026-09-13 **Restoring the slice before `DoMatch`'s partial retry is part-right and is NOT done.** The two `DoMatch2` calls are two attempts at one match, so a `(*SKIP)` in the non-partial pass leaves `slice_start` moved for the partial pass and the search stops being leftmost - a defect on this port's own answers (`\b\D(*SKIP)z` over `' A'`: search (2,0), this port's own `MatchAtStart` at 1 gives (1,1), and so does upstream). Adding the restore fixes that row, **introduces** a reversed one - restoring `slice_end` moves what every end-of-subject assertion means - and turns S37's permanent `A_skip_alternation_partial_starts_where_this_port_ran_out_of_text` red, which turns out to be the same defect pinning (4,0) where this port's own matcher answers (2,2) earlier. All three want judging together, in a slice of their own. Written out at the code so it is not rediscovered.
- 2026-09-13 **The 6000-row gate at three seeds finds fifteen diverging rows, not four.** S40 recorded four because only seed 7 ever completed; 4242 gives five and 20260913 gives seven, eleven of them never triaged, and **one is a crash** - row 93133 throws `ArgumentException: capture index out of range` out of `Substitution.ExpandField` where upstream answers `sub 0`. Two independent blind reviews confirmed none of the fifteen is caused by S40a's diff (the same `wave.jsonl` consumed against a reverted engine gives a byte-identical report). So "the default wave green at 6000 rows" is not a slice-sized exit gate, and S40a's closing section proposes splitting it four ways with the crash first. This is the Phase 4 pattern again: verification got stricter and found real defects.
- 2026-09-13 **Row 93133 is not a crash, and the entry above it is wrong about that - this supersedes it.** `Substitution.ExpandField`'s `ArgumentException: capture index out of range` is exactly what upstream raises for the identical template the moment it has a match to expand it against: `{1[2]}` names a third capture of a group that made one, and deleting the lookahead gives upstream a match and then `IndexError: list index out of range`. The port's format-field handling was measured against upstream's on seven index forms - `{0}`, `{0[0]}`, `{0[-1]}`, `{0[-2]}`, `{1[0]}`, `{1[2]}`, `{2[-1]}` - and agrees on every one, negative indices included. So the whole divergence is upstream LOSING THE MATCH, which is `group-call-loses-the-match`, and `UpstreamFoundStrictlyLess` now has an arm for a substitution row whose port-side answer is a rejection rather than a replacement, narrowed to `ArgumentException` with its hole named. Re-runnable: the last two sections of `tools/probes/upstream-group-call-loses-matches.py`, which print the row and then upstream's answer to each of the seven index forms beside this port's recorded one.
- 2026-09-13 **The operation was never what discriminated `overlapped-skip-extra-match-reversed`, so it now covers a plain reversed `finditer` too.** The prefix comparison and the per-extra-match refutation are what keep it narrow, and neither was loosened. The row that forced it is this port's own improvement: with the per-match slice reset in, a plain reversed scan stops carrying the moved slice, so seed 20260913 row 116766 - `(?r)[[:digit:]]*(*SKIP)a$` over `'\r\nAAa\r\naaa'` - now ends at upstream's first match where it used to reproduce all three, and upstream's own `(?m)$` holds at 1, 6 and 10 alone while its second and third matches need it where the subject has an `a`. **Two rows of the same family are deliberately left red**: a `sub` row carries no match positions for a tell to read, and an overlapped row with no `$` and no groups has no tell at all - its refutation is upstream's own search over a truncated subject, which is the reversed `anchoredScan` the recorder refuses. `tools/probes/upstream-reversed-skip-scan-shapes.py` runs all three.
- 2026-09-13 **Upstream leaks a partial through a group call, on both widths and in 2026.9.10, and the odd answer on this side is the ASCII one - not the astral one S40a first named.** A call inside an opposite-direction lookaround with an OPTIONAL tail reports `partial=True` upstream where the same lookaround written out reports a complete match; that is ledger entry 8's signature and it survives issue 614's fix, so the sync will not remove it. Across twelve cases this port answers "not partial" for every optional tail and "partial" for every required one, at both widths - except the two ASCII CALL rows, `(?P<g1>A)(?:(?<=(?P>g1))\w)?` over `'A'` and its reversed twin `(?r)(?P<g1>\w+)(?:(?!(?P>g1))\s)?` over `'a'`, where it reports upstream's partial. So the question for S40c is why those two leak, not why the astral rows do not, and the answer decides whether seven wave rows become one `ExpectedDivergences` entry or none. **Five of the seven are "upstream partial, this port a complete match of the same span" and two are not**: row 98191's subject holds no astral character and this port answers no match at all, and row 74396 is a substitution where neither engine reports a partial and the replacement counts differ - so the astral subject is what the generators draw, not the precondition. `tools/probes/upstream-call-partial-leak.py`, `--newer` for 2026.9.10.
- 2026-09-13 **A green commit that leaves the slice file pending is a CHECKPOINT, not a failure.** `Get-SliceFailureReason` returns the literal `checkpoint` when the session committed, left the tree clean and the ratchet green but did not move its slice file; the driver keeps the commit, logs `checkpoint`, resets the failure count and starts a fresh session on the same slice, stopping after three. Before this the driver rolled such commits back and auto-retried from scratch: S29 (2026-09-11) and S40a (2026-09-13) both ended a session that way on purpose, both were rolled back, and each needed a recovery session that fished the commit out of the reflog (S40a's cost 40M tokens before it could start its own work). The ratchet check now runs before the pending check, so a red pending tree is still a failure. Pester tests cover both orderings.
- 2026-09-13 **The slice IS restored before `DoMatch`'s partial retry, at both ends - this supersedes the "part-right and NOT done" entry above.** That entry's blocking premise was measured and is backwards. It read the reversed row as a regression restoring `slice_end` introduces; the row is **upstream's own instance of the same defect**, in the direction its `search_start` prefilter does not mask. A reversed search is anchored by its end and tries the highest `endpos` first, so the answer it owes is the first anchor that matches: upstream's search gives the zero-width partial at (0, 0) - the *last* anchor it would try - while its own `match(0, 1, partial=True)` gives (0, 1), and so does the same pattern with `(*PRUNE)`, which prunes backtracking identically and moves no bound. Three such rows are the `partial-retry-reversed-slice` entry (seeds 7 and 20260913, none at 4242). S37's pinned row was the same defect too and its answer moved from (4, 0) to (2, 2), which is upstream's own `match(pos=2)`, **narrowing `search-start-partial` to the prefilter alone**: a row whose only difference is where this port started is now a defect to fix, not a row to classify. What decided every part of it was self-refutation - a search that reports a position its own anchored matcher beats is wrong whatever upstream says - which is how the reversed half could be judged against an upstream that disagrees with it. Also a units trap worth naming: S40a's "(2, 2)" for the S37 row is index and length, and reads (2, 4) as a span; it was nearly filed as a correction.
- 2026-09-13 **The gate's three seeds cannot always separate a half-fix from a fix, and a fourth seed is the cheap way to find out.** S40b's control B - restoring the forward slice end only, leaving `slice_end` alone - scored **identically to the committed code** at seeds 7, 4242 and 20260913 (1+2+2 diverging on `partial,verbs` at 6000 rows). Only the classification moved, not the count, because **upstream shares the reversed defect and a port that shares it too simply AGREES**. Two things did separate them: this port's own self-consistency (the permanent test and the staleness alarm both red under B), and **seed 31**, where B leaves 4 diverging rows where the fix leaves 3 and the row it misses is reversed - restoring `slice_end` makes this port agree with upstream there. So when a control's number does not move, the instrument may be the thing that cannot see the difference, not the change; try a seed the gate does not use before concluding the two designs are equivalent. (Seed 31 also carries three unjudged divergences of its own, present with and without S40b's fix, recorded in S40b's closing notes for S40d.)
- 2026-09-13 **An oracle row index is meaningless without the command that drew it.** The index counts across every generator in a run, so "row 101560" identifies a row only under `tools/run-oracle.ps1 -Count 6000` with the full default generator list - 126,000 rows a seed. S40b's blind review looked for that slice's three rows in a 12,000-row `-Generator partial,verbs` wave, found them at 5560, 397 and 3556, and reported the recorded provenance as wrong. Nothing depends on the index, because every row-keyed `ExpectedDivergences` entry matches on the row's own question; it is a pointer for a reader, and it has to name the command to be one.
- 2026-09-13 **There is no group-call partial leak, and the entry above is superseded: upstream's extra partial is `min_width` showing through the two-pass structure, and the odd answers were this port's astral ones.** `do_match` answers a partial request with a NON-PARTIAL pass first and falls back only if it FAILS, and `do_exact_match`'s width early-out is guarded by `partial_side == RE_PARTIAL_NONE` (`_regex.c:18069`), so it fires on the first pass alone. `min_width` counts a group CALL at the callee's width even inside a zero-width LOOKAROUND, so `(?P<g1>A)(?:(?<=(?P>g1))\w)?` has `min_width` 2 against the inlined form's 1; on a one-character subject the non-partial pass is skipped on arithmetic and the partial pass answers instead. **It therefore depends on how many characters are AVAILABLE, not on the call**: more text removes the partial, and a wider callee moves the threshold by exactly the callee's width. Predicted before it was believed and confirmed nine for nine on 2026.7.19 and 2026.9.10 (`tools/probes/upstream-min-width-partial-retry.py`). **This port's defect was counting `available` in UTF-16 code units** - one astral character read as two, the early-out did not fire, the non-partial pass SUCCEEDED, and the retry never happened. The comment that had stood at that line, and its twin in `Substitution.cs`, argued the mismatch was safe because over-counting can only fail to fire and the engine then "does the work and fails in the dispatch loop instead - the same answer"; **a pass that succeeds is not a pass that fails**, and what it suppressed was the fallback. Five of S40c's seven wave rows now agree; row 98191 turned out to be `bounded-lazy-repeat-partial` and row 74396 `group-call-loses-the-match`, and one example row left the latter because the fix made it agree - its divergence had been the width early-out all along, which padding the subject proves.
- 2026-09-13 **A matrix that holds the deciding variable fixed cannot answer the question, however many cases it has.** S40a's twelve-case call/partial matrix varied the pattern's shape and never varied the subject's length, which is the only thing the early-out reads - so two sessions read it, drew opposite conclusions, and neither was right. What answered it in minutes was an env-gated opcode trace in `Matcher`'s two dispatch loops printing `partial_side`: the ASCII case ran one pass with it SET and the astral case one pass with it CLEAR, which says the two took different roads before matching began. **When a matrix is consistent and still does not explain, suspect the axis it does not have.** A related trap from the same slice: the first draft of the upstream prediction probe scored eight of nine, and the failing row was a badly chosen experiment rather than a refutation - its optional tail could actually match, so no partial could exist whatever the early-out did. Rebuilt with a `\w*` prefix so the tail stays past the end of the subject while the width varies, it scored nine of nine; the note is kept in the probe.
- 2026-09-13 **The recorder's reversed `anchoredScan` refusal was about the PATTERN, not about reversal, and it now reads the pattern.** `_anchored_scan` records upstream's own answer to an overlapped `(*SKIP)` scan asked one match at a time; it refused every reversed row because stepping one moves `endpos`, which truncates the subject and changes what `$`, `\Z`, `\b`, `\B`, `\m` and `\M` mean. A reversed pattern holding none of those can be walked, and the step is upstream's own - `state->text_pos = state->match_pos - 1` (`:20903`), where a reversed attempt's `match_pos` is the match's END. Measured on seven shapes including a zero-width one, all SAME, and on four end-reading shapes, all DIFFER: `python tools/probes/upstream-reversed-walk-step.py`. `_reads_the_end_of_the_subject` is a crude whitespace-stripped substring test over `$ \Z \z \b \B \m \M \K \G \X (?= (?!` and every way it is crude refuses MORE than it must, which can only cost a classification. It classified seed 4242 row 117071 and nothing else across 378,000 rows.
- 2026-09-13 **A diverging `sub` row carries no positions, so the recorder now records where upstream replaced.** `subMatches`, on a `sub`/`subf` row whose pattern holds a `(*SKIP)`, asked as a separate `finditer` because `subn` walks the same scanner - which makes the `$` tell readable on a substitution and classified seed 20260913 row 116388. The consumer refuses to read the list unless its length equals the recorded replacement count, and that guard immediately earned its place: the first draft truncated by the row's raw `count`, and **upstream's count convention is not a slice** - 0 means no limit and a NEGATIVE count replaces nothing, so a row upstream never touched was recorded with every match but the last. Found by the blind review, 8 bad rows in a 6000-row seed-7 `verbs` wave, 0 after the fix. The arm compares counts and not text, because a sub renders as one string and this port's own replacement spans are not in the row; the hole is stated beside the code.
- 2026-09-13 **The reversed carried slice has one more symptom - upstream's scanner LOSING a match, ledger entry 5's fourth - and a seed outside the gate's three found it in under a minute.** Seed 20260914, `verbs` row 3679: `(?r)\p{Lu}*(*SKIP)B(?P<g1>(?:\D{2,4}(*SKIP)a|.))` over `'B_\ra'`, upstream reporting one match where this port reports two. Same mechanism as ledger entry 5 with the moved `slice_end` too far LEFT rather than too far right, so the next attempt runs in a view that cannot hold the match. Upstream's own stepwise door finds both - `search(S, 0, 4)` is (0, 4) and `search(S, 0, 3)` is (0, 2) - and both verbs made `(*PRUNE)`, or deleted, give upstream both. New entry `overlapped-skip-missing-match-reversed`, keyed on the walk alone; the three reversed entries are now disjoint by count (equal, upstream longer, upstream shorter). **This is the second time a seed the gate does not use has found a real family** (S33 was the first), and it cost about forty seconds.
- 2026-09-13 **A test that passes with its `(*SKIP)` deleted is not automatically a weak test.** S40d's third blind pass raised exactly that about the new pinned test, and it is the point rather than a weakness: this port's correct answer IS the verb-free answer, because a moved slice no longer carries into the next match. What says the test pins something is the mutation it fails under - delete `state.SliceEnd = state.InitialSliceEnd;` from `Matcher`'s per-match reset and it goes red - not whether the verb changes the expected value. Recorded in the test's own comment so it is not re-raised.
- 2026-09-13 **Upstream's `better` is the improvement loop's TERMINATION as well as its ranking, so a port that ranks differently must split the two tests.** `do_enhanced_fuzzy_match`'s `better = total_errors < fewest_errors` (`_regex.c:17930`) also drives `else break` (`:17972`), and `max_errors` holds every run after the first to fewer errors than the last - so the test is all but always true and exists to stop the loop, not to choose between candidates. S41's first draft replaced the whole of it with this port's cost comparison, which does not re-rank the chain, it CUTS it at the first run that costs more: `fullmatch("(?e)(?:x|xyq){1i+9s+9d<=20}", "yzxyz")` kept a run worse than upstream's answer on the cost this port ranks by AND on the error count upstream ranks by. Found by the blind review, reproduced through `run-oracle.ps1 -Rows` before anything was touched; 54 diverging rows of 2500 against 31 after the fix (`tools/probes/enhancematch-cost-rows.py`, seed 777). **S42 has the same shape to port at `:17647` and must split it the same way.**
- 2026-09-13 **`ENHANCEMATCH`'s dead `same_match` check was measured, and the measurement is stronger than the 2026-09-12 argument for skipping it.** Honouring it saves about 5% of the `BasicMatch` runs (1.42 per enhanced match down to 1.35, three 2000-row `fuzzy` waves) - inside the "at most one extra run" the slice predicted. It is NOT "identical or better" as the slice also predicted: it changes the answer on 18, 15 and 14 rows of 2000 and loses perfect matches, e.g. `(?e)(?:\d+b+?){2i+1d+1s<=2}` over `1bb` goes from an exact match at (0, 2) to a one-substitution match at (0, 3). The override's answer is upstream's on every changed row, because the wave is green with it at three seeds. A lower bound: the experiment guarded the check with "a previous best exists", which upstream's dead code does not.
- 2026-09-13 **`best_fuzzy_counts` stays unported in `SaveBestMatch`/`RestoreBestMatch` even though S41 ported the helpers, because upstream has no `best_fuzzy_changes` beside it.** POSIX saves a match and then fails on purpose to look for a longer one; the backtracking unrecords the fuzzy changes, so restoring the counts alone leaves a count of 1 over an empty changes list and `match_fuzzy_changes` (`:20504`) reads past the end - `regex.match(r'(?p)(?:cat){e<=1}', 'caz').fuzzy_changes` segfaults on 2026.7.19 where `.fuzzy_counts` answers `(1, 0, 0)`. This narrows ledger entry 9, whose "`match` is not affected, only `search`" was about the matching call and not about reading the changes. In a memory-safe language the same asymmetry is a match reporting one substitution and no substitution position, so S42 takes the counts and the changes together or neither.
- 2026-09-13 **An `ExpectedDivergences` entry that covers only part of its own family is better than one widened to cover all of it.** `enhancematch-ranks-by-cost` classifies a cost-ranking divergence only where the two answers are otherwise the same match. A cheaper match can also sit at a different span, and then the `sub`, `split` and `finditer` counts differ too - 31 such rows against 12 classified on the 2500-row probe wave, none of them dearer than upstream. Widening the predicate to take them would also swallow "this port answered a different span", which is what a real engine defect looks like and is the one thing the list must never hide. The rows stay reported, and the entry says so.
- 2026-09-13 **`BESTMATCH` ships with upstream's error-count ranking, because the owner's cost rule cannot be reached from `do_best_fuzzy_match`'s second pass.** Issue 470's own example is the proof: `(?b)(voices){1i+1d+2s<=2}` over `voixes voicees` should answer the cost-1 insertion `voicees` and never can, because the FIRST pass holds the next run to fewer ERRORS than the one it has (`_regex.c:17675`), both candidates are one error, and the search stops at `voixes`. Reaching it needs a cost BOUND inside `basic_match` - `state->max_cost = state->total_cost - 1`, which releases up to 2015.09.28 had and the 2015.11.5 issue-165 **hang fix** replaced with `max_errors` throughout - so it is a change to the shared fuzzy predicates on every mode's hot path, with a hang risk named in its own changelog, and not a line in `DoBestFuzzyMatch`. Layering cost into the equal-count tie-break instead was measured and rejected: issue 470 unchanged, and the `fuzzy` wave goes from 0 to **3, 2 and 3** divergences of 2000 at seeds 7, 4242 and 20260913, every one a cheaper match at a DIFFERENT SPAN - in an overlapped scan a LOST match, 4 where upstream has 5 - which is precisely the family the entry above refuses to classify. The whole change or none of it. `Gaps.Engine.FuzzyBestMatchTests.Bestmatch_still_agrees_with_upstream_on_the_issue_470_example` is what turns red when it lands.
- 2026-09-13 **Upstream's four `*_best_list` functions and three `*_best_changes_list` functions are not ported, because `List<T>` already is them.** A collection expression is `init`, going out of scope is `fini`, `Clear` is `clear` and `Add` is `add_to`; only `RE_BestEntry` and `add_best_fuzzy_changes` have any content, as `Engine.BestEntry` and `Matcher.AddBestFuzzyChanges`. The S42 slice file asked for all four kept as named methods "so PORTMAP's row 428 maps"; four one-line wrappers buy nothing a PORTMAP sentence does not, and `init_fuzzy_changes_list` set that convention in row 417 already.
- 2026-09-13 **A generator's two round-robins pair fully only if their lengths are coprime, and the `fuzzy` generator's are coprime by luck.** The operation is `ALL_OPERATIONS[i % 8]` and the subject alphabet is `FUZZY_SUBJECT_ALPHABETS[i % 3]`. Adding a fourth alphabet band makes `gcd(4, 8) = 4` and LOCKS each operation to exactly one alphabet: `search`, `match`, `subf` and `finditer` then draw no astral subject at all, at any row count (measured seed 7, 2000 rows, 0 of 250 each). Found by S42's blind review. The fix is to index by `i // len(ALL_OPERATIONS)`, which makes the pairing exhaustive rather than accidental; it is written into the comment and NOT applied, because it reshuffles every fuzzy row and doing so uncovered a real `ENHANCEMATCH` defect that S42 was not the slice to fix.
- 2026-09-13 **The checkpoint rule's first live firing crashed the driver**, because `Write-SliceLogEntry`'s `ValidateSet` did not list the new outcome: "The argument "checkpoint" does not belong to the set completed,failed,parked,rate-limited" (`.scratch/driver-s42.err`). The commit survived - the crash came after the verdict and before any rollback - so the outcome was the intended one by accident. Fixed with a Pester test on the writer; S42's first sitting is written into `slice-log.jsonl` by hand from its session log (80,631,260 tokens, the driver's own four-field sum), so the phase close's cost figures are complete.
- 2026-09-13 **`BESTMATCH` ranks by cost through a SECOND WALK over the slice, not through a tie-break, because the owner's rule is lexicographic and one scalar budget cannot express a lexicographic order.** A budget that holds the next run to a strictly lower COST cannot let an equal-cost run through to be judged on its error count; one that holds it to strictly fewer ERRORS prunes the cheaper-but-equal-count match that is all of issue 470. So `Matcher.DoBestFuzzyMatch` walks twice: walk 0 bounded by `MatchState.MaxCost` answers "what is the cheapest match in this slice?", then upstream's own walk (`:17612-17676`) runs unchanged with that cost pinned, which makes its fewest-errors-then-earliest rule the owner's tie-break and leaves the second pass untouched. `MaxCost` and the conjuncts it adds to `any_error_permitted` and `this_error_permitted` are release 2015.09.28's own lines (`:9923`, `:9936`, `:16352`), removed by the 2015.11.5 issue-165 hang fix. S42's first sitting recorded that layering cost into the second pass's tie-break "buys nothing"; that was right, and the reason is this, which it did not have.
- 2026-09-13 **The cost walk is taken only for a pattern with ONE fuzzy section and a WEIGHTED cost equation, and both conditions are load-bearing rather than defensive.** Nesting: an error made inside an inner section is priced at the inner rates while it is being made - which is what the predicates check the bound against - and at the outer rates once `END_FUZZY` merges the counts, which is what `state->total_cost` then reports. The two numbers differ, the budget never bites, and because `start_pos` does not advance the walk HANGS: `(?b)(?:(?:b\W){e:0}){1i+2d+1s<=4}` over `'B-\U0001D518'` re-found the same cost-4, two-error match under a cost-3 budget until it was killed. Weighting: where the three kinds cost the same, a match's cost is a fixed multiple of its error count, so the walk provably cannot change the answer - and it is not free, at 1964 runs of `BasicMatch` against upstream's 1600 over the 665 `(?b)` rows of the committed seed-7 fuzzy wave, +22.8% for nothing. `PatternObject.HasWeightedFuzzyCosts` is set in `NodeCompiler.BuildFuzzy` from the bytecode's three cost words. **A term the equation omits costs NOTHING, not one**, so `2d+1s<4` is weighted - which is why `test_fuzzy#44` diverges and why a `1d+1s` control would have been no control at all.
- 2026-09-13 **One ported test asserts this port's answer rather than upstream's: `test_fuzzy#44`, and S42's first sitting was wrong to record that none would.** `(?b)(foobar){i<=1,d<=2,s<=3,2d+1s<4}` over `FuzzyTestData.Scattered` answers (34, 39) upstream and (26, 33) here. The first sitting's brute force ranked the candidates `finditer` returns, and those are already error-minimised per position, so the cost-2 match at (26, 33) was never in the set it ranked. **Upstream's own engine is the proof it is real**: tighten the equation to `2d+1s<3` and `regex.search` finds exactly (26, 33) with two substitutions and one free insertion; tighten to `<2` and nothing matches, so cost 2 is the floor. Measured on regex 2026.7.19. Recorded per the never-weaken-a-test rule; the evidence is in the comment on the test.
- 2026-09-13 **The `fuzzy` generator no longer pairs a weighted cost equation with `(?e)` or `(?b)`, because a differential oracle cannot judge a comparison the two engines are DEFINED to answer differently.** Adding `(?b)` to the generator turned the wave red at 4 rows of 2000 at each of three seeds, all twelve weighted-under-a-ranking-flag and all twelve this port answering more cheaply. Classifying them instead cannot be made strict: `sub`, `subf` and `split` rows record a string and no per-match counts, so for eight of the twelve there is no cost to compare and the only available predicate is "the spans differ" - which is also exactly what a real engine defect looks like. What replaces the coverage is `tools/probes/enhancematch-cost-rows.py`, extended to draw both flags and read for the INVARIANT (this port's answer is never DEARER) rather than for a green light: 252 divergences of 2500 at seed 777, 128 cheaper, **0 dearer**, 3 equal-cost-but-earlier, 9 no match at all, 112 unscorable. Turning the cost walk off leaves 19, every one an `(?e)` row.
- 2026-09-13 **Upstream `BESTMATCH` loses a match that plain fuzzy matching finds when the best fit needs two trailing insertions, and this port reproduces it - ledger entry 12.** `regex.fullmatch(r'(?b)(?:x){e<=3}', 'xyz')` is `None` where the same pattern without `(?b)` answers `(0, 2, 0)`; one trailing insertion is fine and two are not. The guard at `:15515-15517` double-counts - for a single fuzzy section `state->fuzzy_counts` already holds the merged inner counts, so *n* trailing insertions need `max_errors` above *2n-1* - and the second pass climbs only to `fewest_errors`, so it cannot re-find the match the first pass found. Fixing it is a Phase 6 inherited-bug slice, not S42's. The cost walk does not cause it and widens its reach: 9 rows of the 2500-row probe match nothing with the walk on and none with it off, all of them `fullmatch` with insertion-heavy fits. That is the stated price of the ranking change until the bug is fixed.
- 2026-09-13 **A cost budget needs TWO things upstream's error budget gets for free, and both were found by blind review rather than by reasoning.** First, the three constraint predicates bound the section CURRENTLY OPEN, not the whole match, exactly as they do for the error count - upstream's whole-match backstop is `if (state->total_errors > state->max_errors) goto backtrack` at `END_FUZZY` (`:12486`) and it has no cost twin, because upstream has no running cost. Without that twin a pattern entering one section more than once (a group call, a recursion, a repeat) spends the budget twice and `DoBestFuzzyMatch`'s cost walk HANGS: `(?b)((?:a){1i+2d+1s<=1})(?1)` over `bb`. Second, `MatchState.TotalCost` and `TotalErrors` are SNAPSHOTS written at `END_FUZZY`, and a match can succeed on a path whose last `END_FUZZY` belongs to a branch that was backtracked out of - so both can be stale where `MatchState.FuzzyCounts`, the counts the caller is given as `Match.FuzzyCounts`, is live. Ranking on a stale number scores a genuinely cheaper run as equal and hangs the same way: `(?b)((?:abc){e<=2,2i+1d+3s<=4}(?1)?)` over `bb` reported cost 4 for a match whose live counts cost 2. **The rule that came out of it: whenever this port is the one ranking, it reads the live counts**, priced by `PatternObject.SingleFuzzyNode`, which `rankByCost`'s one-section condition guarantees is the only section. Walk 1 is upstream's walk and keeps upstream's fields, stale exactly where upstream's are.
