# S56b - per-sitting notes

Spec: `docs/plan/slices/done/S56b-compile-budget-for-unrolled-repeats.md` (closing notes there).

## Sitting 1 - 2026-09-18, 15:58, Sonnet, orientation only (commit `cc465f4`)

Mislaunched on the wrong model, wrote a 15-line orientation note into `docs/plan/STATE.md` and
committed nothing else. Its plan (thread `maxCompiledNodes` like `matchTimeout`, check in
`NodeCompiler.CreateNode` before `NodeList.Add`, throw `FuzzyRegexParseException`) was correct and
was followed. Its one gap: it did not notice that adding a trailing optional `int` to the public
constructor would capture the internal constructor's `defaultVersion` call sites - see sitting 2.

## Sitting 2 - 2026-09-18, Opus, the slice

Order of work: tests first (they failed by exhausting the heap, which is the failure the slice
predicted), then `PatternObject.MaxNodes`/`PatternText`, then the `CreateNode` check, then the
constructor thread-through, then docs.

Gates, all after the last code change:

- Ratchet GREEN, 6351/6351, baseline updated to 6243 distinct ids (8 added, 0 removed).
- Oracle GREEN at all three default seeds - 7, 4242, 20260918 - `diverge 0` in each. It was RED at
  all three before the `WithDefaultVersion` fix, with 3 DIVERGE rows at seed 7 (case-folding,
  partial-sliced, fuzzy) that were all the overload capture, not engine behaviour.
- AOT smoke GREEN on win-x64.
- `CompileBudgetTests` 8/8.

Two traps worth carrying forward:

1. **The overload capture** (full account in the closing notes). Cost about an hour of chasing
   what looked like two engine regressions and three oracle divergences.
2. **`System.GC.HeapHardLimit` in the built `runtimeconfig.json` breaks unrelated tests.** It is
   the only way to cap the heap here without an env-var approval prompt, and it is right for a
   pathological-pattern probe, but `FuzzyRecursionTests` and
   `InheritedIssueTests.A_long_repeated_capture_group...` fail with `OutOfMemoryException` under a
   1 GB cap. Remove the line before any full-suite run; the file is regenerated by a build, so a
   rebuild also clears it.

### Safety

Every probe of a nested counted repeat ran with the 1 GB heap cap, a wall-clock `timeout`, and
subjects under 100 characters, per the slice's safety rule and the orchestrator's instruction. The
largest product compiled unbounded was `(a{1000}){100}`; `((a{1000}){1000}){1000}` was only ever
run against a budget, where it is refused after 249 MB. The machine did not swap.

### Independent verifier

Fresh Opus subagent, briefed with the commit-ready tree and the same safety limits, re-running
every number this slice quotes. **Ten claims, all CONFIRMED, one correction.** Its report:

- `((a{150}){150}){150}` refused at the default budget: 249 MB, 701 ms cold and 537 ms on a second
  call in the same process (this sitting measured 763 ms, the blind reviewer 681 ms - hence
  "about 0.7 s" rather than a single pinned number).
- Created counts re-bisected: `a{50}`=106, `a{200}`=406, `(a{100}){100}`=20,913,
  `(a{1000}){100}`=202,713. Boundary: 20,913 compiles, 20,912 throws.
- Kept counts: `(a{100}){100}`=10,509 measured by reflection over `PatternObject.NodeList`, and
  `(a{1000}){100}`=101,409 as the test comment says. `(a{1000}){1000}`'s 1,005,009 could not be
  measured directly under the memory limits - it is the investigation document's figure, and it
  fits `kept = XY + X + 4Y + 9` derived from the measured points; the pattern is refused at the
  default budget in 723 ms / 248 MB.
- **Correction, applied before the commit: the optimiser's pruning method is
  `Optimiser.DiscardUnusedNodes` (`src/FuzzyRegex/Engine/Optimiser.cs:459`), not
  `RemoveUnusedNodes`.** Four places named it wrongly - `CompileBudgetTests.cs`, `docs/PORTMAP.md`,
  `docs/plan/DECISIONS.md` and the closing notes - and all four now name the real method.
- Upstream answers re-run against regex 2026.9.10 on Python 3.14.6: all three pinned answers
  CONFIRMED. The probe prints a fourth line (`a{200}` over `'a'*99` -> None) that no comment
  quotes, which is fine but worth knowing if the file is read as a one-to-one pairing.
- Gates re-run from the tree: ratchet "Tests: 6351 passing: 6351 (6243 distinct ids) baseline:
  6243", GREEN, baseline diff 8 added / 0 removed, all eight `CompileBudgetTests` ids; oracle GREEN
  at all three seeds (`diverge 0` at 7, 4242 and 20260918); AOT smoke GREEN on win-x64;
  `CompileBudgetTests` 8/8.
- Structure: the only `NodeList.Add` in `src/` is `NodeCompiler.cs:230` inside `CreateNode`, with
  the budget check at `:215-218`; zero `catch` clauses anywhere on the path from the public
  constructor to `CreateNode`; `WithDefaultVersion` has exactly two call sites, and a
  balanced-paren scan of all 600 `new FuzzyRegex(` sites found none passing five or more
  positional arguments.
- Working tree left byte-identical (`git status --porcelain` unchanged).
