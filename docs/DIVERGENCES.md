# Deliberate differences from upstream mrab-regex

The running list of every place this port intentionally behaves differently from the pinned
upstream release, kept so that Phase 8's user documentation is written from a record rather than
from memory. **A slice that introduces or removes a deliberate difference appends to this file in
the same commit** (port-slice skill rule, 2026-09-14). Accidental differences are not listed here:
they are oracle divergences, and they are either fixed or pinned in
`tests/FuzzyRegex.OracleTests/ExpectedDivergences.cs` with a ledger entry in
`docs/plan/upstream-reports/LEDGER.md`.

Each entry: what differs, why, where decided, whether it is in the shipped code, and what a user
does to get upstream's behaviour if they can. "Owner" means the project owner decided it; the
DECISIONS.md date is the trail.

**The Status column is load-bearing.** `SHIPPED` means the behaviour is in `main` today, with the
slice in `docs/plan/slices/done/` or the decision already applied to the code. `PLANNED (Snn)`
means the difference has been designed and approved but the slice has not run, so the shipped code
still does what upstream does. Phase 8 must document only the SHIPPED rows as behaviour, and must
re-check every PLANNED row before writing about it.

## Defaults

| Difference | Why | Decided | Status | Upstream behaviour available? |
|---|---|---|---|---|
| **Version 1 is the default** (nested sets and set operations; full case-folding under `IgnoreCase`). Upstream defaults to `VERSION0` for `re` compatibility. | No `re` users to protect; both V1 behaviours are the library's reason to exist. Measured on regex 2026.9.10 and .NET 10 (`tools/probes/upstream-version-defaults.py` and `dotnet-version-defaults.ps1`): zero-width handling and inline-flag scoping are already identical in V0 and V1, so nested sets, full case-folding and a backreference to an open group are the whole of the live difference. | Owner, spec amendment 24, slice S50b | **SHIPPED** | Yes: `FuzzyRegexOptions.Version0` or `(?V0)`. |
| **`(?V0)` really means version 0, where upstream's own algorithm would leave version 1's `FULLCASE` on.** A leading global flag makes upstream parse the pattern twice, and the first attempt has already OR-ed the default version's `DEFAULT_FLAGS` into `info.global_flags`, which seeds the second. Measured on regex 2026.9.10 with `DEFAULT_VERSION = VERSION1`: `compile('a', regex.V0).flags` is `U\|V0` and `compile('(?V0)a').flags` is `F\|U\|V0` - the same version folding two ways (`tools/probes/upstream-inline-v0-under-a-v1-default.py`). This port puts the version-implied defaults in `Info.Flags` and not in `Info.GlobalFlags`. | Unreachable in upstream's shipped configuration, where `DEFAULT_FLAGS[VERSION0]` is `0`; reachable here the moment the default moved, and shipping a `(?V0)` that does not mean V0 is indefensible. Under `Version0` the change is bit-for-bit upstream's behaviour, which all 1,659 compile-parity rows hold it to. | Slice S50b; ledger entry 22 | **SHIPPED** | No, and upstream has no such behaviour to reproduce: its own default makes the bug unreachable. |
| **The "unterminated character set" parse error names `FuzzyRegexOptions.Version0` and the `\[` escape**, but only for a pattern that version 0 actually accepts. Upstream raises the bare message. | Under version 1, `[[]` and `[a[b]` - accepted by `re`, by `System.Text.RegularExpressions` and under `Version0` - stop compiling, and the bare text tells a caller arriving from `Regex` nothing about why. The condition is decided by recompiling the failed pattern under `Version0`, because nothing cheaper is true: two blind reviews killed two "a nested set was opened" flags that each advised `Version0` on a pattern version 0 rejects as well (`[[a-z]--[aeiou]]x[`, `[[a]--[b`). A plainly unclosed `a[` keeps upstream's text. | Slice S50b | **SHIPPED** | The message text only; the rejection itself is upstream's, and `Version0` removes it - which is exactly what the message now checks before saying so. |
| **No `concurrent` parameter.** Upstream's `concurrent=True` releases the GIL during a match. | There is no GIL to release. The port matches without a global lock, and a compiled pattern is intended to be immutable and shareable. | Spec "runtime discipline"; PORTMAP's deliberately-not-ported table, S01 | SHIPPED (S01) | Not applicable. |
| **A `Match` may be read from any thread, where `System.Text.RegularExpressions.Match` may not.** A compiled pattern is immutable and shareable, as upstream and `Regex` both are; the port goes one step further and makes the result objects safe too. Enumerators stay single-thread, as everywhere in .NET. | .NET callers are concurrent by default, where upstream's Python-level object is GIL-protected. The built-in `Regex` documents its results as single-thread because "their implementations could delay computation of some results"; this port has exactly one such value, `Match.FuzzyChanges`, so the stronger promise cost one field. That field **was** a four-field `FuzzyChanges?` published with `??=`, which genuinely tore under 64 threads, and is now a `StrongBox<FuzzyChanges>` - one atomic reference write. Gates: a reflection walk over the whole reachable graph with a proven allowlist, a before/after snapshot of that graph and of the static tables, a static-state audit, pool-discipline tests and a parallel stress test, all PERMANENT. | Owner, spec amendment 23, slice S52b | **SHIPPED (S52b)** | Not applicable - there is no upstream behaviour to reproduce, and the difference is strictly a stronger guarantee. |
| **A `CancellationToken` on every input-dependent method.** Upstream has no counterpart at all: a Python caller interrupts a long match with Ctrl-C, which `safe_check_cancel` sees through `PyErr_CheckSignals` (`upstream/src/_regex.c:2266`), and a .NET caller has no equivalent. The token is read at that same site, behind upstream's own `state.Iterations == 0` gate, and a cancelled one raises `OperationCanceledException` carrying the token. It is also read once at the door of each public call, so the contract holds on paths that never reach the engine (`Substitution.Subx`'s min-width shortcut). | The .NET cooperative-cancellation model asks library code for exactly this: "If library code provides cancelable operations, it should also provide public methods that accept an external cancellation token" (Cancellation in Managed Threads, read 2026-09-15). Measured the same day, `tools/probes/bcl-sync-cancellationtoken-apis.ps1`: 28 of 28 synchronous BCL methods that take a token put it last, which is where these are. Cost, measured over three runs before and four after: **8 bytes per operation** - the token field on `MatchState`, which is built once per operation - and no time `bench/FuzzyRegex.Benchmarks` can resolve, the within-version spread being an order of magnitude larger than the between-version gap. | Owner, spec amendment 22, slice S51 | **SHIPPED (S51)** | No - there is nothing upstream to reproduce. A caller wanting upstream's behaviour passes no token, which is the default and costs nothing. |
| **A per-call `timeout` on every input-dependent method**, as `TimeSpan? timeout = null` where `null` means the pattern's `matchTimeout`. | **Parity, not a divergence**: upstream has taken a per-call `timeout=` on every match method all along (`upstream/regex/_main.py:253-298`) and this port had only the constructor's budget. Listed here because the *shape* is the .NET one rather than upstream's - `Regex` takes `matchTimeout` on 11 static methods and 0 instance methods (measured, same probe), so neither precedent had a per-call budget on a compiled instance and this surface now does. `Match.NextMatch` deliberately takes neither and runs under the pattern's budget, exactly as `Regex`'s does. | Owner, spec amendment 22, slice S51 | **SHIPPED (S51)** | Upstream's `timeout=` maps straight onto it. Passing nothing keeps the constructor's `matchTimeout`, which is what every caller before S51 got. |

## API shape

| Difference | Why | Decided | Status | Upstream behaviour available? |
|---|---|---|---|---|
| **`Match` means upstream's `search`; upstream's anchored `match` is `MatchAtStart`** and `fullmatch` is `FullMatch`. | Naming upstream's `match` `Match` would silently give a .NET caller anchored behaviour, which is a trap no compiler catches. Static conveniences also take `(input, pattern, options)`, `Regex`'s argument order rather than upstream's. | DECISIONS 2026-08-29 (line 30) | SHIPPED (S01) | The mapping is documented; `MatchAtStart` is upstream's `match` exactly. |
| **`Regex`-shaped surface**: `FuzzyRegex`, `Match`, `Group`, `Capture` with `(Index, Length)`; `Matches`/`Split`/`Replace` naming. Also `expand` is `Match.Result`, `expandf` is `Match.ResultFormat`, `lastindex` is `Match.LastGroupNumber` with Python's `None` rendered as `-1`, and `lastgroup` is `Match.LastGroupName` with `None` rendered as `null`. | Idiomatic .NET; the design spec's public-API sketch. | Spec section "Public API sketch", S01; DECISIONS 2026-08-31 (line 164); PORTMAP:76-77, :276 | SHIPPED (S01, S24) | Python names are documented as a mapping in the API docs (Phase 8). |
| **Indices are UTF-16 code units**, not codepoints. This includes `FuzzyRegexParseException.Offset`, which differs from upstream's `error.pos` for a pattern holding a non-BMP character before the failure. | .NET strings are UTF-16; the ported tests translate every non-BMP expectation. `Source.Get` still returns whole codepoints, so the emitted bytecode is identical to upstream's. | Spec; DECISIONS 2026-08-30 (`Source` reads codepoints, counts `char`s); PORTMAP:666 | SHIPPED (S07) | No. Documented with an example for astral characters. |
| **Upstream's `pos`/`endpos` are reshaped to `beginning`/`length`.** `length` is a length in UTF-16 code units, not an end index, and `-1` means "the rest of the subject". Upstream's Python-slice reading of a negative `endpos` is not reproduced: `regex.search('abc', 0, -1)` searches `[0, 2)` where this port reads `-1` as "no limit". | .NET convention. The oracle recorder converts upstream's codepoint pair at the boundary, so a slip shows as a divergence rather than as silent agreement. | S01; DECISIONS 2026-09-12 (line 232) | SHIPPED (S01) | Pass an explicit length; the two conventions are documented side by side. |
| **`Split` spells "no limit" as `maxSplits = -1`**; upstream spells it `maxsplit=0` and reads a negative as "no splits". | .NET convention; the inversion is applied at the boundary only. | S01; `Iteration.cs` | SHIPPED (S01) | The mapping is documented. |
| **`Replace`'s `count` is inverted the same way**: here `-1` is "no limit" and `0` is "replace nothing"; upstream's `count=0` is "no limit" and a negative count replaces nothing. | The same .NET convention as `Split`, applied at the boundary only. The inversion has two ends, and the oracle consumer had to be taught both. | S01; DECISIONS 2026-09-01 (line 191); `Gaps/Substitution/SubstitutionRulesTests.cs` | SHIPPED (S24) | The mapping is documented. |
| **There is no `findall`.** Upstream's `findall` returns the *text* of the single capturing group when the pattern has exactly one, and a tuple of group texts when it has more. `Matches` returns `Match` objects, `EnumerateMatches` returns them lazily, and `Count` returns only a count. | `Matches` and `Count` were judged to cover every ported use, and a second result shape would duplicate the scan. The group-text rule is a trap worth documenting: `regex.findall(r'(\w\w\K\w\w)', 'abcdefgh')` is `['abcd', 'efgh']` while the matches are `'cd'` and `'gh'`. | S25; DECISIONS 2026-09-01 (line 199); PORTMAP:381 | SHIPPED (S25) | Read `Match.Groups[1].Value` over the `Matches` result, or over `EnumerateMatches` for a lazy projection (S53b). |
| **`Match.Groups` is an `IReadOnlyDictionary<string, Group>` as well as a list**, keyed by every group's name - or its number as text where it has none - in ascending group number. Upstream's dictionary view (`groupdict`) is the NAMED groups only. | The built-in `GroupCollection` has had exactly this face since .NET 5, and measuring it (`tools/probes/dotnet-groupcollection-dictionary.ps1`, .NET 10.0.10, 2026-09-16) shows it is total rather than named-only: `Keys` for `(?<a>a)(b)(?<c>c)?` is `[0, 1, a, c]`. `TryGetValue` is what the port gained from it - the non-throwing way to ask about a name the pattern may not have. Upstream's `groupdict` is a filter over this one. **The cost is a source break**: a type with two `IEnumerable<T>` faces makes `Groups.Select(...)` ambiguous, exactly as it is on the built-in collection. | S53b; owner decision 2026-09-16, `docs/plan/2026-09-16-unported-members-review.md` | SHIPPED (S53b) | `Groups.Values.Select(...)` restores the list projection; `groupdict` is the dictionary filtered to the non-numeric keys. |
| **A lazy walk times each STEP, where `Matches` times the whole scan.** `EnumerateMatches` and `EnumerateSplits` take a `timeout` like every other entry point, but each match is found on an engine state of its own, so the clock restarts per match. Upstream's `finditer` scanner holds one state and so one budget for the walk. | A state owns rented buffers, and a state held across a `yield return` is one an abandoned iterator never returns. Per-step is the price of a lazy walk that cannot leak; a caller who needs the walk bounded uses `Matches`, which does hold one state. | S53b | SHIPPED (S53b) | Use `Matches`, whose budget is the whole scan. |
| **`Split` returns `string?[]` and puts `null` where a capturing group did not take part.** Upstream puts `None` there; the built-in `Regex.Split` omits the entry entirely, which both shortens the array and loses the difference between a group that matched empty and one that never ran. | Keeping upstream's shape ports all eight affected assertions in `test_re_split` and `test_qualified_re_split` with identical positions and lengths. Measured against both engines: `regex.split('(x)|(1)', 'a1b')` is `['a', None, '1', 'b']` where `Regex.Split("a1b", "(x)|(1)")` is `["a", "1", "b"]`. | DECISIONS 2026-08-29 (line 48) | SHIPPED (S25) | This *is* upstream's behaviour; the surprise is for the .NET reader, so it is documented prominently. |
| **Replacement templates speak upstream's language** (`\1`, `\g<name>`, `\n`, `\x41`, `\N{...}`), not `Regex`'s `$1`. | Fidelity to upstream's `sub` semantics; `$` is ordinary text. | DECISIONS 2026-08-29 (line 47) | SHIPPED (S12, S24) | Not applicable; documented prominently because .NET users will expect `$1`. |
| **Exception mapping.** Upstream's `error` is `FuzzyRegexParseException`, and so are its bare `ValueError` rejections (the ASCII/LOCALE/UNICODE incompatibility, a cost equation with no digits). Upstream's `IndexError("unknown group")` from `Match.expand` becomes `ArgumentException`, while the same mistake in `sub` stays `FuzzyRegexParseException`, because upstream raises two different errors there too. The C compiler's `RuntimeError("invalid RE code")`, and every pattern on which upstream lets a Python internal error escape (`AttributeError`, `TypeError`, and `KeyError` from `V0\|V1`), become `NotSupportedException` or `ArgumentOutOfRangeException`. The 1 GB backtracking bound and a runaway recursion raise `InvalidOperationException`, never `OutOfMemoryException`, where upstream raises `MemoryError`. A matching timeout raises `System.Text.RegularExpressions.RegexMatchTimeoutException` where upstream raises `TimeoutError`. `\p{Infinity}` raises `OverflowException`, carrying Python's own message. | .NET exception idiom. `FuzzyRegexParseException` means upstream's `error` and nothing else, which is what the compile-parity corpus pins row by row. CA2201, S112 and MA0012 all reserve the memory exceptions for real allocation failures, so reusing one would make a genuine failure indistinguishable from this library's own limit. `RegexMatchTimeoutException` already carries the input, the pattern and the timeout, so a caller who handles `Regex` timeouts needs no new `catch`. | DECISIONS 2026-08-30 (lines 89, 117), 2026-08-31 (lines 121, 126, 128, 145, 152, 153), 2026-09-01 (line 193); PORTMAP:669, :670 | SHIPPED (S07-S16) | Not applicable; documented per method. |
| **`Match.allcaptures`, `allspans`, `groupdict` and `capturesdict` are not on the public surface, and will not be.** | The fifteen upstream lines that use them are already ported through `Groups` and `Group.Captures`, so unlike S04's named lists nothing is unwriteable without them. Owner decision D settled it for good. | DECISIONS 2026-08-30 (lines 68, 81); owner decision D, `docs/plan/2026-08-30-phase2-decisions.md` section 6; PORTMAP:653-655 | SHIPPED (decided S12) | Equivalent data through `Groups` and `Group.Captures`. |
| **The "unused keyword argument" message interpolates the name as written**, where upstream formats it with Python's `ascii()` repr: for a named list called `é` upstream says `unused keyword argument '\xe9'` and this port says `unused keyword argument 'é'`; for `a'b` upstream switches to double quotes and this port does not. ASCII names agree exactly. | `ascii()` is a Python terminal convention, not behaviour, and no test on either side pins the message, so implementing Python's `repr` for one string would be unverified code. | DECISIONS 2026-08-30; PORTMAP:667 | SHIPPED (S07) | Not applicable. |
| **Not ported at all**: the members in `docs/PORTMAP.md`'s deliberately-not-ported table. Beyond the CPython object plumbing, the `concurrent` GIL dance, pickling and the pattern cache, this also drops `Pattern.scanner` and `regex.Scanner`, `Match.pos`, `endpos`, `string`, `re` and `regs`, `Match.detach_string`, `regex.template` and the `TEMPLATE`/`T` flag, the `LOCALE` and `DEBUG` flags as public options, `_compile`'s `ignore_unused`, `regex.compile`'s `cache_pattern`, `regex.prefixmatch`, and the `__copy__`/`__deepcopy__` protocol on patterns and matches. | No .NET meaning, covered by the runtime, or expressible through a member the port already has. Each has its own row and reason in PORTMAP. | PORTMAP:493-509, :597, :649-655 | SHIPPED | See the "Upstream members with no port equivalent" section below for the member-by-member answer. |

## Behaviour where the port answers differently on purpose

| Difference | Why | Decided | Status | Upstream behaviour available? |
|---|---|---|---|---|
| **`(?e)` and `(?b)` rank candidates by fuzzy COST** (cheapest, then fewer errors, then earliest); upstream ranks by error count. **The divergence applies only where the pattern has exactly one fuzzy section and a weighted cost equation.** With unit costs, or with a nested second section, the port falls back to upstream's error-count budget and the two engines agree exactly, which is why the change costs one ported test and no more. | Upstream issue 470 reproduced on 2026.9.10; pre-2015.09.28 upstream used `max_cost`; TRE/agrep rank by lowest cost; the user's own cost weights are otherwise ignored by the ranking. `BESTMATCH` reaches the rule with a second walk over the slice, because one scalar budget cannot express a lexicographic order; a unit equation makes that walk provably unable to change the answer and costs about 23% more `BasicMatch` runs, and a second section makes it hang. | Owner, S41/S42, DECISIONS 2026-09-13 (lines 263, 313, 314); PORTMAP:690, :692 | SHIPPED (S41, S42) | No. One ported test expectation changed (`test_fuzzy` row 44, `Gaps`/`Ported/Fuzzy/FuzzyBestMatchTests.cs`), justified in DECISIONS. |
| **The Turkic `I` pairings are not applied by default.** `(?i)` and `(?fi)` here do not pair `I` with `ı`, or `i` with `İ`; upstream does, so `regex.fullmatch('aI', 'aı', I)` matches upstream and not here, and a `\L<name>` case-folded search finds `kit` in `SKİTS` upstream and not here. **No Turkic mode is offered.** | `upstream/tools/build_regex_unicode.py` merges `CaseFolding.txt`'s Turkic-only `T` rows into both default folding tables and hard-codes the pairing into the all-cases table, although the file's own header says the `T` mappings are excluded by default. So upstream applies a Turkish locale rule with no locale asked for. PCRE2, Perl and .NET agree with this port. Upstream exposes no locale, so a flag this port's API does not have would be new surface rather than a fix. The default data lives in the hand-written `Unicode/TurkicDefaults.cs`, in front of the generated tables, so the next sync cannot silently undo it. | Owner, ledger entry 7, spec amendment 16 outcome (c); DECISIONS 2026-09-14 (lines 348, 349) | SHIPPED (S45) | No. Two ported assertions assert this port's non-match on purpose (`Ported/CaseFolding/TurkicTests.cs`, `Ported/NamedLists/NamedListTests.cs`). |
| **The search prefilters are not ported**, so this port's `search`, `finditer` and partial matching answer what upstream's own anchored `match` answers on several families. `regex.search(r'(?ai)\p{Ll}', 'A')` is `None` upstream and a match here; `regex.match('(?fi)fi', 'fı')` matches upstream and not here; `regex.compile(r'(?r)\b$').search('', partial=True)` is a partial upstream and `None` here; `(*SKIP)` scans and several bounded-partial shapes differ. | `locate_required_string` and the `search_start` family are Phase 7 work. They are not semantically transparent: they skip start positions the slow path would accept, and once a `(*SKIP)` moves the slice mid-attempt the two halves of upstream disagree with each other. On every family judged so far the port is the right answer, decided against PCRE2 10.47 run directly and against upstream contradicting itself. Under the owner's no-known-bugs rule (2026-09-12) these are **permanent pins, never inverted when Phase 7 lands**: Phase 7 ports the prefilters without importing their answers, and a pinned port-is-right test changing is a regression. | Owner, spec amendment 16; DECISIONS 2026-08-31 (lines 182, 184), 2026-09-11 (lines 219-221), 2026-09-12 (lines 231, 236, 237, 239); ten entries in `ExpectedDivergences.cs` | SHIPPED (S29-S43) | No. Each family is a permanent pinned test in `Gaps/Engine/`. |
| **The Unicode data is version 17.0.0**, where upstream resolves `\N{...}` through whatever `unicodedata` the host CPython ships (16.0.0 on the machine this was built on). So 4,803 codepoints resolve here and raise upstream, and a group name written in a decimal digit new in 17.0 is a group *number* here where upstream degrades `\g<...>` to literals and rejects `(?P=...)`. | Upstream's own tables *are* 17.0.0; its reliance on the host module is an accident of implementation, not intent, and a port that copied the accident would be pinned to whichever Python it ran under. Every 16.0.0 name is still a 17.0.0 name under Unicode's name-stability policy, proven for all 148,853 of them on every run of `tools/build-character-names.py`. | Owner decision B, `docs/plan/2026-08-30-phase2-decisions.md`; DECISIONS 2026-08-30 (lines 79, 109), 2026-08-31 (line 122); PORTMAP:674, :688 | SHIPPED (S09, S11) | No. CPython 3.15 moves to 17.0.0 and the `\N{...}` half of the divergence disappears there. |
| **A digit's decimal value is derived from upstream's own tables**, by the codepoint's offset within its maximal run of `Numeric_Type=Decimal` codepoints modulo ten, rather than read from .NET's `CharUnicodeInfo`. | Mixing the two makes the port internally inconsistent: `CharUnicodeInfo` is an older Unicode version, so the ten codepoints U+11DE0-U+11DE9 are a digit to the `isdigit` gate and unknown to the value lookup, and the port would throw where upstream compiles. Verified against `CharUnicodeInfo` for every codepoint it knows. | DECISIONS 2026-08-31 (line 122); PORTMAP:675 | SHIPPED (S11) | No; it is the same 17.0.0 choice as the row above. |
| **`\N{...}` named sequences are not carried.** `\N{KEYCAP DIGIT ZERO}` reports "undefined character name" here where upstream raises `TypeError`. | Upstream cannot use a named sequence either: `parse_named_char` calls `ord()` on the three-codepoint result. So the only observable difference is which error the caller gets, and carrying about a thousand more names to change the error text is not worth the assembly size. Measured 2026-08-30. | DECISIONS 2026-08-30 (line 110); PORTMAP:687 | SHIPPED (S09) | No; both engines reject the pattern. |
| **`(?L)` is rejected at match time.** A run of two or more case-insensitive characters under `(?L)` throws `needs:locale-flag`, where upstream compiles and matches it. `(?L)` on its own, and `(?Li)` over a single character, compile identically to no flag at all. | The `LOCALE` encoding reads the process C locale, which has no .NET equivalent and is not surfaced on `FuzzyRegexOptions`. Settled against the oracle in S10: the only locale-sensitive call the parser makes is `is_cased_i`, reached only from `Sequence._flush_characters`, and ten measured patterns are pinned by `Gaps/Parsing/LocaleFlagTests.cs`. | DECISIONS 2026-08-30 (line 116); PORTMAP:509; `Unicode/Encodings.cs:44` | SHIPPED (S09, settled S10) | No. |
| **CPython's 4300-digit cap on `int(str)` is not ported**, so `\g<` followed by 4301 nines compiles here where upstream raises `ValueError`. | The cap is an interpreter setting, changeable at runtime with `sys.set_int_max_str_digits`, raised as `ValueError` rather than `regex.error`, and with no .NET equivalent. It is a property of the host Python, not of the regex grammar this port mirrors. Measured 2026-08-30. | DECISIONS 2026-08-30 (line 92); `S07-parser-skeleton.md:149` | SHIPPED (S07) | No. |
| **A group call that would re-enter the same group at the same text position fails that PATH**, not the match. Upstream has no guard at all and allocates until `MemoryError`; PCRE2 10.47 raises `PCRE2_ERROR_RECURSELOOP` and fails the whole match. So `(?P<g1>(?:ab)?(?&g1)?)` over `'abab'` is an error in PCRE2 and `(0, 4)` here, and left recursion keeps its one-step unrolling. | Ledger entry 14, an inherited bug fixed here (spec amendment 16's third outcome). A call that arrives back where it started cannot have consumed anything, so the path is infinite; refusing one infinite path cannot cost an answer another path reaches, which is why this is strictly less destructive than PCRE2's rule. The 1 GB backtracking bound stays as the backstop for the branching the guard cannot see. **S49's issue sweep found this divergence already covers an open upstream issue nobody had connected to it: 397, the same defect with no fuzzy section at all.** `(?(DEFINE)(?P&lt;e&gt;[cd]\|(?&amp;e))(?P&lt;t&gt;(?&amp;e):(?&amp;e)))(?&amp;t)` over `'a[14]'` raises `MemoryError` in ~0.8s upstream, for both of the 2021 reporter's patterns including the one he said was fine, and answers `None` here. It is not a silent cap: the same left-recursive DEFINE still matches `'c:d'`. | Owner, ledger entry 14; DECISIONS 2026-09-14 (lines 370, 373, 374); PORTMAP:694 | SHIPPED (S47) | No. Pinned by `Gaps/Engine/GroupCallTests.cs` and `Gaps/Engine/FuzzyRecursionTests.cs`. |
| **A branch-reset branch skips a group number a reused name has already claimed.** `(?|(?P<bug>xxx)(!)|(?P<bug>BUG)(!))` over `'BUG!'` answers `bug='!'` upstream and `bug='BUG'` here. `ParseCommon` rolls the counter back per branch, and a name already seen in an earlier branch resolves to its old number without moving it, so the next group in the same branch is handed that number a second time and the later write wins. Fixes upstream issue **425**. | Ledger entry 17. Under upstream's answer `(?P<bug>BUG)`'s text is unreachable through any API and `groupindex` maps the NAME onto text a different group matched - which the maintainer calls "the problem" himself. This is his own **option 2**, "number consecutively, but skip group numbers that have been used up to that point in the branch" (2021-09-28); he has not chosen between options 2 and 3, so the pinned test asserts only what the two agree on. It is a no-op outside a branch reset, and no compile-parity corpus row changes. **The two orderings option 2 cannot reach are parked**, not fixed: where the UNNAMED group comes first (`(?\|(?P<bug>xxx)(!)\|(!)(?P<bug>BUG))`) the name's number is already fixed, so only option 3's lookahead over the whole branch would help. | Owner rule 2026-09-12; ledger 17; DECISIONS 2026-09-14 | SHIPPED (S50), partially - see STATE.md's blocker | No. Pinned by `Gaps/UpstreamIssues/InheritedIssueTests.cs`. |
| **Reversed partial matches run out of text at the slice start.** A pattern matched with `(?r)` and `partial: true` reports a partial at `beginning` when it still needs characters and `beginning` is where the matchable text ends; upstream reports one there on some shapes and answers no match on others. So `regex.compile('(?r)ya').match('xya', 2, 3, partial=True)` is `None` upstream and a partial at (2, 3) here, and `regex.compile(r'(?r)ya(.*)\b')` on the same slice is `None` upstream where its own lazy twin `(.*?)` is a partial on both engines. **Nothing else about `beginning` moves**: `^` and `\A` still refuse a non-zero one, and `\b`, `\B` and lookbehind still see the character before it, exactly as Python `re` documents and as upstream does. | Ledger entry 24. Upstream holds two rules and picks between them by optimisation path - its node handlers ask `text_start`, which is always 0, while `search_start` (`_regex.c:8400-8405`) and its three reversed string helpers (`:8335-8382`) ask `slice_start` - so it contradicts itself on a grid of 33 cells and breaks two of the metamorphic invariants S52c added: greedy and lazy disagree on whether a match EXISTS, and a pattern needing more text runs out less than one needing less. Its own comment beside the bounds says "the end of the slice behaves like the end of the string", its forward side does exactly that, and a reversed match may not consume text below `beginning` at all (measured: `regex.compile('(?r)ab').search('abc', 1)` is `None`), so the matchable text really has run out there. .NET's `Regex.Match(input, beginning, length)` and Boost.Regex both treat a caller-imposed bound as the end of the text, and PCRE2 fires a partial at the subject end it was handed. Spec amendment 16 outcome (c): fixed here through one helper, `Matcher.RanOutOnTheLeft`, so the two rules cannot part company again. | Owner, 2026-09-15, Option B of `docs/plan/upstream-reports/ledger-24-briefing.md`; ledger entry 24; DECISIONS 2026-09-16 | **SHIPPED (S52d)** | No. Pinned by `Gaps/Engine/ReversedPartialSliceStartTests.cs` (the whole 33-cell grid) and by `Gaps/Engine/PartialMatchingTests`, whose S33-era pin of upstream's other rule was rewritten to record the divergence. |
| **Inherited upstream bugs are fixed here**, each with a ledger entry and, from S47c, the mechanism traced in upstream's C: ledger 7 (the Turkic fold rows, its own row above), 9's port half (a segfault-adjacent read under `(?p)` plus fuzzy), 11 (fuzzy counts and change lists desynchronised; A to D are the four this port ever shared and all four are fixed, and S48b's second sitting found two more - E, an atomic group, and F, a reversed fuzzy lookahead - and S52's ninth a third, G, a change list whose entries are of the wrong KIND for the counts the same answer reports; none of the three is shared and all three are pinned in the oracle instead), 12 (BESTMATCH's trailing-insertion guard counting errors twice), 13 (a `(*SKIP)`-moved slice leaking across the partial retry, so the partial is never attempted), 14 (runaway recursion filling memory, its own row above), 5 (verb and partial slice bounds not restored per match - five doors closed before S48 and the sixth, a `(*SKIP)` truncating the `(?b)` candidate walk, closed by S48 itself; it is the only one of the six this port ever shared), and S35's `Sequence._fix_full_casefold` chunk-start remap, where upstream's arithmetic fails for a second expansion in one literal run so `(?fi)ﬁaﬁ` stops matching `fiafi`, `(?fi)ßaß` stops matching `ssass`, and `(?r)^İﬁ` under `I\|F` crashes upstream in `get_firstset`. | Owner rule 2026-09-12: no known bug is left in the port, inherited or not; spec amendment 16's outcomes (b) and (c), and amendment 20. | DECISIONS per entry (2026-09-12 lines 236, 255; 2026-09-13 line 320; 2026-09-14 lines 348, 355, 364, 379); `LEDGER.md` | SHIPPED (S35, S43, S45, S46, S47, S47b, S47c, S48, S48b) - S48b closed ledger 11's mechanisms C and D and ledger 9's remaining port-side count bug, which were the last three open items | No. Each fixed one is a permanent pinned test; the ledger drafts the upstream report, filed in Phase 8 only with owner approval. |

## Upstream members with no port equivalent

Every member below appears in upstream's own type tables (`match_methods`, `match_getset`,
`match_members`, `pattern_methods`, `pattern_members`, `pattern_getset` in `upstream/src/_regex.c`)
or among `_main.py`'s module-level functions, and has no member of the same name and shape here.
Each row is SHIPPED: these are the absences in `main` today. The rows that are true *absences*
rather than reshapes are the ones Phase 8 has to warn a porting user about.

| Upstream member | Port equivalent | PORTMAP row | Note |
|---|---|---|---|
| `Pattern.findall` | none | partial (:381) | `Matches` gives `Match` objects and `Count` gives a count; upstream's group-text result shape has no equivalent. See the API-shape table. |
| `Pattern.splititer` | `EnumerateSplits` | none | SHIPPED (S53b). Yields exactly the pieces `Split` returns, including the nulls and a reverse pattern's back-to-front order. |
| `Pattern.scanner`, `regex.Scanner` | none | yes (:496) | A stateful lexer-style API with no `Regex` counterpart, used by 11 lines of the upstream suite. |
| `Pattern.prefixmatch`, `regex.prefixmatch` | `MatchAtStart` | yes (:537) | An exact alias of `match` upstream; a second name for one method would be surface with no behaviour. |
| `Pattern.__copy__`, `__deepcopy__`, `__sizeof__` | none | yes (:392, :597) | Python's copy protocol. Patterns are immutable, so upstream returns the same object; .NET has no equivalent protocol. |
| `Pattern.groups` (the count) | `GroupNumbers` | none | Reshaped: a list of the numbers rather than a count. |
| `Pattern.groupindex` (the dict) | `GroupNumberFromName`, `GroupNameFromNumber`, `GroupNames` | none | Reshaped: two lookups and a name list rather than a dictionary property. A match's groups ARE a dictionary since S53b - `Match.Groups` implements `IReadOnlyDictionary<string, Group>` keyed by the same names - but the pattern's own mapping stays two lookups. |
| `Match.pos`, `Match.endpos` | none | yes (:500) | The read-back properties. The *arguments* are ported as `beginning`/`length`; only reading them back is absent. |
| `Match.string`, `Match.re` | none | yes (:500) | `System.Text.RegularExpressions.Match` has neither either. Read four times in the whole upstream suite, all in `test_getattr`. |
| `Match.regs` | none | yes (:501) | The tuple of every group's span. `Groups[n].Index`/`.Length` carries the same information. |
| `Match.groupdict`, `Match.capturesdict` | `Groups["name"]`, `Groups["name"].Captures`, and the `IReadOnlyDictionary<string, Group>` face of `Groups` | yes (:654) | Owner decision D: do not add a named-only dictionary. S53b added .NET's TOTAL one instead - every group, keyed by its name or its number as text - of which `groupdict` is a filter. |
| `Match.allcaptures`, `Match.allspans` | `Groups` plus `Group.Captures` | yes (:653) | Owner decision D: do not add. |
| `Match.starts`, `Match.ends`, `Match.spans` | `Group.Captures` with `(Index, Length)` | partial (:275) | All six of upstream's per-index accessors collapse onto one `Match.GroupAt` returning a `Group`. |
| `Match.detach_string` | none | yes (:497) | Drops the match's reference to the subject so Python can free it. .NET's GC needs no such hint. |
| `Match.__copy__`, `__deepcopy__` | none | yes (:278, :597) | `make_match_copy` exists for these and for the detached-string case, neither of which this surface has. |
| `regex.purge`, `regex.cache_all` | `FuzzyRegex.CacheSize`, PLANNED (Phase 7, first slice) | yes (:494) | Control upstream's module-global pattern cache. This port has no cache yet, so its six static conveniences compile per call; Phase 7 adds a bounded MRU behind a `CacheSize` property as `Regex.CacheSize` does (decision 2026-09-16, `docs/plan/2026-09-16-unported-members-review.md`). Until then this row is an absence. |
| `regex.template`, the `TEMPLATE`/`T` flag | none | yes (:498) | Present upstream only because Python's `re` has it; upstream implements no behaviour for it. |
| `DEBUG` flag | none | yes (:503) | Not surfaced on `FuzzyRegexOptions`: it only prints a parse tree. `ASCII`, `UNICODE` and `WORD` were in this row until S53b and are now `FuzzyRegexOptions.Ascii`, `.Unicode` and `.Word`, with upstream's bit values; `LOCALE` stays out, having no .NET meaning. |
| `LOCALE` flag | inline `(?L)` only, which then throws | yes (:499, :509) | The absence of the option is one thing; the match-time `needs:locale-flag` throw is a behaviour difference and has its own row above. |
| `regex.compile(ignore_unused=)` | none | yes (:507) | Only `subf`/`subfn` pass it true, and only for a pattern that has already compiled once. `Compile` always complains. |
| `regex.compile(cache_pattern=)` | none | **none** | There is no pattern cache to opt into or out of; a .NET caller keeps its own `FuzzyRegex` instance. The only member in this table with no PORTMAP row of its own. |
| `regex.DEFAULT_VERSION` (a settable module global) | `Info`'s constructor argument | partial (:103) | The default version is a compile-time constant here, not something a caller can reassign at runtime for every later pattern. |

## Kept identical although upstream looks odd

Listed so Phase 8 does not "fix" them: the `same_match` override in ENHANCEMATCH (a deliberate
upstream rework, 2015.11.5; honouring the dead check was measured and loses matches); the `better`
test in ENHANCEMATCH, whose *termination* half is kept exactly as upstream wrote it even though its
*ranking* half was replaced by the cost comparison; `Split` takes no `pos`/`endpos` because
upstream's does not; and issue 612's fix is ported and inert, because the required-string locator
it guards is Phase 7 work, so that a later slice adding the locator cannot silently reintroduce the
bug. See DECISIONS 2026-09-13.
